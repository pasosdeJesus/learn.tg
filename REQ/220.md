# R-#220: Cluster Formation (Simplified) — built on the existing GD cluster model

Formation of clusters of **3 churches** (lead pastor + 2 invited pastors) for the Global Disciples pilot, **building on the existing GD cluster infrastructure** in the `@learn-tg/gdcluster` engine (https://gitlab.com/pasosdeJesus/m/-/work_items/35 Phase 3, https://github.com/pasosdeJesus/learn.tg/issues/214):

- Tables **`clustergd`** + **`church_clustergd`** (church-based membership) + **`clustergd_history`** (audit) — **NO parallel `cluster`/`cluster_member` tables are created**.
- Existing **`/api/cluster/*`** routes (`POST /api/cluster`, `POST /api/cluster/join`, `GET|PATCH /api/cluster/[id]`, `POST /api/cluster/[id]/leave`) — **extended, not duplicated**.
- Funds: **`ClusterFundsV2`** (`processDonation` → `clusterFunds[cluster_wallet]`) using the `church.cluster_wallet`; cluster ranking already operational.
- Invite candidates: **#163 referral graph** (referred pastors + the referrer if they are a pastor).

**Dependencies:** #161 (GD course — content ⏳, purchase requirement already enforced in `createCluster`), #163 (referrals — ✅), #155/214 (cluster funds — ✅ operational).

---

## 1. Overview

A pastor with the GD course paid (verified in `createCluster`) forms a cluster with **their church + 2 churches of invited pastors**. The cluster is the existing `clustergd` entity; membership is churches (`church_clustergd`); the fund is the lead church's `cluster_wallet` (`ClusterFundsV2`, 80% of GD donations).

### 1.1 Pastor states (user)

| State | What they see |
|-------|---------------|
| **No cluster** | `/{lang}/cluster` with a reference to Guide 2 + "Create a cluster" button |
| **Pending invitation** | Page with the invitation + **Accept** / **Reject** buttons |
| **In a cluster** | Cluster details (name, pseudonym, members) + "Leave cluster" button |

### 1.2 Cluster states (`clustergd.status` — new column)

| State | Condition |
|-------|-----------|
| `pending` | 1–2 member churches (created, invitations open) |
| `active` | 3 member churches (cluster formed) |
| `disbanded` | Fell below 2 members after a leave (or leader decision) |

---

## 2. Creation (pastor without a cluster)

**Page:** `/{lang}/cluster`

1. The pastor sees: *"To form a cluster, you need to invite 2 trusted pastors. Read Guide 2 for more information."* + link to Guide 2 + **"Create a cluster"** button.
2. The form (reuses `POST /api/cluster` with new fields):

| Field | Type | Description |
|-------|------|-------------|
| **Cluster name** | Text | Public (unique per country — existing validation) |
| **Pseudonym** | Text (optional) | For countries with persecution; when set, it is what appears in the public ranking (privacy) |
| **Invite pastors** | Multi-select | Candidates (see §2.1) |

3. The pastor selects **2 pastors** and submits.
4. A `clustergd` is created (existing 6-char code + `pseudonym` + `status='pending'`) with the leader's church as the 1st member (`church_clustergd`), and **2 invitations** are generated (`cluster_invitation`).

### 2.1 Invite candidates (`GET /api/cluster/candidates`)

Priority (same pilot-country rule as `createCluster`):

1. **Pastors referred** by the leader (#163 referral graph: `referralrelationship` → `usuario` with `church_relationship='pastor'`).
2. The leader's **referrer**, if they are a pastor.
3. (Fallback, if candidates are missing) search by name/cluster code of verified churches in the same country — the cluster can also be completed with the existing **6-char code** (`POST /api/cluster/join`).

Per-candidate constraints: same country as the leader, declared and verified church, no current cluster, not themselves.

---

## 3. Invitation (pastor with a pending invitation)

1. The invitee receives an **in-app notification** (existing `notifications` table): *"You have been invited to join cluster [Name] by [Pastor]."*
2. Opening `/{lang}/cluster`: *"You have been invited to join cluster [Name] created by [Pastor]."* + **Accept** / **Reject**.
3. **Accept** → `POST /api/cluster/invitation/accept`: the pastor's church joins `church_clustergd`; at 3 members, `clustergd.status='active'`; the leader is notified and it is recorded in `clustergd_history`.
4. **Reject** → `POST /api/cluster/invitation/reject`: `cluster_invitation.status='rejected'`; the leader is notified (they can invite another candidate).

**Rule:** a pastor/church can only belong to one cluster at a time (existing `getChurchCluster` validation → 409).

---

## 4. Management (pastor in a cluster)

**Page:** `/{lang}/cluster`

- Name, pseudonym (if set), members (lead church + 2), cluster code.
- Text: *"The pastor who created the cluster can edit the details."*
- **"Leave cluster"** button (`POST /api/cluster/[id]/leave`, existing, extended):
  - If the **leader** leaves: offer to transfer leadership (to a member) or **disband** (`status='disbanded'`).
  - If a member leaves and fewer than 3 remain: the cluster returns to `pending` and the leader can invite a replacement (candidates §2.1).

The **leader** edits name/pseudonym: `PATCH /api/cluster/[id]` (existing, extended).

---

## 5. Admin / Verifier

Over the existing data (`clustergd` + `church_clustergd` + `church`), exposed via `/api/admin/clusters/*` (verifier/admin auth, `lib/admin-auth.ts` pattern).

| Role | Access |
|------|--------|
| **Admin** | Create, edit, disband any cluster |
| **Verifier** | View all, edit name/pseudonym/members, cannot disband |

### 5.1 List (`/api/admin/clusters`)

Columns: ID · Name (or pseudonym) · Country · Leader (lead church + pastor) · Members (3/3) · Status (pending/active/disbanded) · Fund (`cluster_wallet` + balance via `ClusterFundsV2`) · Actions (Edit / Disband).

Filters: name, country, status, leader.

### 5.2 Create / Edit (`/api/admin/clusters` POST / `/api/admin/clusters/[id]` PUT)

Fields: name, pseudonym, lead church (select), members (multi-select of verified churches), status.

### 5.3 Detail (`/api/admin/clusters/[id]`)

Details + members with roles + add/remove member + disband. Admin changes are logged in `userevent` (and `clustergd_history`).

---

## 6. Database Schema (new migration)

### 6.1 Alter of `clustergd` (existing table)

```sql
ALTER TABLE clustergd
  ADD COLUMN pseudonym VARCHAR(100),              -- privacy (persecution contexts)
  ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'pending', -- pending | active | disbanded
  ADD COLUMN leader_church_id INTEGER REFERENCES church(id);  -- lead church (optional: default = 1st member)
```

> **Funds decision:** the `cluster_wallet` (`ClusterFundsV2` fund) remains the `church.cluster_wallet` of the **lead church** (current model: the donation route resolves the destination by the member church's `cluster_wallet`). Documented in the migration; the column is not moved in this phase.

### 6.2 New table: `cluster_invitation`

```sql
CREATE TABLE cluster_invitation (
    id SERIAL PRIMARY KEY,
    clustergd_id INTEGER NOT NULL REFERENCES clustergd(id),
    invited_pastor_id INTEGER NOT NULL REFERENCES usuario(id),
    invited_church_id INTEGER NOT NULL REFERENCES church(id),
    invited_by_id INTEGER NOT NULL REFERENCES usuario(id),
    status VARCHAR(20) NOT NULL DEFAULT 'pending', -- pending | accepted | rejected | expired
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    responded_at TIMESTAMP,
    UNIQUE(clustergd_id, invited_pastor_id)
);
```

> **No new cluster tables:** the original draft's `cluster`/`cluster_member` are discarded; membership remains `church_clustergd` (churches), not users.

---

## 7. API

### User-facing (extends existing `/api/cluster/*` routes)

| Endpoint | Method | Auth | Description |
|----------|--------|------|-------------|
| `/api/cluster` | POST | Yes | Create cluster (name + pseudonym + 2 invitations) — **extends existing `createCluster`** |
| `/api/cluster/candidates` | GET | Yes | List of invitable pastors (#163 referrals + referrer + fallback) |
| `/api/cluster/invitations` | GET | Yes | Pastor's pending invitations |
| `/api/cluster/invitation/accept` | POST | Yes | Accept invitation (church → `church_clustergd`) |
| `/api/cluster/invitation/reject` | POST | Yes | Reject invitation |
| `/api/cluster/join` | POST | Yes | **Existing** — join by 6-char code (fallback) |
| `/api/cluster/[id]` | GET/PATCH | Yes | **Existing** — details / edit name+pseudonym (leader) |
| `/api/cluster/[id]/leave` | POST | Yes | **Existing, extended** — leave (transfer leadership or disband if leader) |

### Admin-facing

| Endpoint | Method | Auth | Description |
|----------|--------|------|-------------|
| `/api/admin/clusters` | GET | Admin/Verif | List with filters |
| `/api/admin/clusters` | POST | Admin | Create cluster manually |
| `/api/admin/clusters/[id]` | GET/PUT | Admin/Verif | Detail / edit |
| `/api/admin/clusters/[id]` | DELETE | Admin | Disband (soft: `status='disbanded'`) |
| `/api/admin/clusters/[id]/members` | POST/DELETE | Admin/Verif | Add/remove member church |

---

## 8. Notifications (existing `notifications` table)

| Event | Notification |
|-------|--------------|
| Invitation sent | *"You have been invited to join cluster [Name] by [Pastor]. Go to Cluster to accept or reject."* |
| Invitation accepted | *"[Pastor] has joined your cluster [Name]."* |
| Invitation rejected | *"[Pastor] has rejected your invitation to cluster [Name]."* |
| Cluster activated | *"Your cluster [Name] is complete (3 churches)!"* |
| Cluster disbanded | *"Your cluster [Name] has been disbanded."* |

---

## 9. Privacy (pseudonym)

- The **public ranking** (`/gdcluster/ranking`) and the pastors landing show the **pseudonym** when set (consistent with https://github.com/pasosdeJesus/learn.tg/issues/153 encryption and https://github.com/pasosdeJesus/learn.tg/issues/219 privacy-first).
- The real name is visible only to cluster members, verifiers, and admins.

---

## 10. Acceptance Criteria

### User
- [ ] The Cluster menu appears only for pastors with the GD course paid (existing `createCluster` validation).
- [ ] Pastor without a cluster sees "Create a cluster" + link to Guide 2.
- [ ] The form allows selecting 2 pastors from the candidates (#163 referrals + referrer if pastor).
- [ ] With fewer than 2 candidates the fallback is shown (invitation by code / search) — does not block creation.
- [ ] Invitees receive an in-app notification.
- [ ] Pastor with a pending invitation can accept or reject.
- [ ] On accept, their church joins `church_clustergd`; at 3 members the cluster becomes `active`.
- [ ] Pastor in a cluster sees the details and can leave; if the leader leaves → leadership transfer or disband.
- [ ] A pastor/church can only be in one cluster at a time (existing 409).
- [ ] The leader can edit name and pseudonym.
- [ ] The ranking shows the pseudonym when set.

### Admin
- [ ] Clusters section at `/api/admin/clusters` with a table, search and filters.
- [ ] Admin can create/edit/disband; verifier can view/edit but not disband.
- [ ] Admin changes are recorded in `userevent` and `clustergd_history`.

### General
- [ ] All routes authenticated (`authenticateUser` + `authenticateAdmin` patterns).
- [ ] Notifications on invite/accept/reject/activation/disband.
- [ ] Cluster status visible in the ranking (`/gdcluster/ranking`).
- [ ] Engine unit tests (`gd-utils`, `gd-cluster-routing`) green; new routes with tests.
- [ ] E2E: `cluster-formation.spec.mjs` (create → invite → accept → active → leave/disband) against the dev site.

---

## 11. Phases

| Phase | Content | Depends on |
|-------|---------|------------|
| **P1 — Migration + model** | Migration (`clustergd.pseudonym/status/leader_church_id` + `cluster_invitation`); `createCluster` extended (pseudonym + invitations); `GET /api/cluster/candidates` | #163 (referral graph) |
| **P2 — Invitation flow** | `invitations`/`accept`/`reject` + notifications + history; activation at 3 members | P1 |
| **P3 — Management + admin + privacy** | `leave` extended (leadership/disband), `PATCH` name/pseudonym, `/api/admin/clusters/*`, pseudonym in ranking | P2 |
| **P4 — Tests** | Unit (engine) + E2E `cluster-formation.spec.mjs` | P1-P3 |

---

## 13. Test Plan

### 13.1 Unit tests (engine `@learn-tg/gdcluster`, vitest — `packages/gdcluster/src/lib/__tests__/`)

| Suite | Cases |
|-------|-------|
| `cluster-invitation.test.ts` (nuevo) | create → `pending`; accept → church joins `church_clustergd`; accept duplicate → 400; reject → `rejected`; reject-after-accept → 400; activation at 3 members (`pending → active`) |
| `cluster-candidates.test.ts` (nuevo) | referred pastors (#163) first, then referrer-if-pastor; same-country filter; excludes self; excludes pastors already in a cluster; fallback path when < 2 candidates |
| `cluster-status.test.ts` (nuevo) | leave member → `active → pending` (< 3); leave leader → leadership transfer or `disbanded`; disband soft |
| `gd-utils.test.ts` (existente) | keep 17/17 green |
| `gd-cluster-routing.test.ts` (existente) | keep 6/6 green (donation destination by `cluster_wallet`) |
| `donation-target.test.ts` (existente) | keep 13/13 green |

### 13.2 Route tests (vitest, deps inyectadas D2 — patrón `credential.test.ts` / `premium-purchase.test.ts`)

**`POST /api/cluster` (extendido):** 401 sin auth · 403 no-pastor · 400 sin iglesia declarada · 403 país no piloto · 403 sin curso GD (ids 10/11) · 409 iglesia ya en clúster · 409 nombre duplicado en el país · 201 con `pseudonym`, `status=pending`, 2 `cluster_invitation` y `clustergd_history`.

**`GET /api/cluster/candidates`:** 401 sin auth · lista priorizada (referidos → referidor) · solo mismo país · excluye self y pastores con clúster · 200 con lista vacía → hint de fallback (código 6 chars).

**`POST /api/cluster/invitation/accept`:** 404 invitación inexistente · 400 no-pendiente (ya respondida) · 200 → iglesia en `church_clustergd` + notificación al líder + historial · 200 con activación a 3 miembros (`status=active` + notificación "clúster completo").

**`POST /api/cluster/invitation/reject`:** 200 → `status=rejected` + notificación al líder.

**`POST /api/cluster/[id]/leave` (ampliado):** miembro → `pending` si < 3 · líder → transferencia de liderazgo (o `disbanded`) + historial.

**`/api/admin/clusters*`:** 401/403 (no verificador) · GET lista 200 (verificador) · PUT 200 · DELETE 403 (verificador) / 200 (admin, soft) · cambios en `userevent`.

### 13.3 E2E `cluster-formation.spec.mjs` (Puppeteer, contra dev site)

**Prerrequisitos del sitio dev** (patrón `pastor-journey.spec.mjs` / `referral-program.spec.mjs`):
- 3 billeteras de pastores: fixture `apps/.env` + 2 en `TEST_PRIVATE_KEYS` (registradas para SIWE).
- Cada pastor: perfil completo + **iglesia verificada** por verifier + mismo país piloto (SL/CO) + **curso GD pagado**.
- Grafo de #163: los 2 invitados son **referidos** del líder (`referralrelationship` creado antes vía claim).
- Ejecución: `IPDES=learn.tg PUERTOPRU=9001 CHAIN_ID=11142220 CHROME_PATH=/usr/local/bin/chrome bin/m test:e2e cluster-formation`.

**Escenarios (ok/fail por paso):**

| # | Escenario | Verificación |
|---|-----------|--------------|
| 1 | Pastor sin clúster → `/{lang}/cluster` | Enlace a Guía 2 + botón "Create a cluster" |
| 2 | `GET /api/cluster/candidates` (líder) | Incluye a los 2 pastores referidos (mismo país) |
| 3 | `POST /api/cluster` (nombre + pseudónimo + 2 invitados) | 201; `status=pending`; 2 `cluster_invitation`; notificaciones a los invitados |
| 4 | Invitado 2 abre `/{lang}/cluster` | "invitado a unirse al clúster X por [Pastor]" + Aceptar/Rechazar |
| 5 | Invitado 2 **Acepta** | Iglesia en `church_clustergd` (2/3); clúster sigue `pending`; notificación al líder |
| 6 | Invitado 3 **Acepta** | Clúster **`active`**; notificación "clúster completo (3 iglesias)" |
| 7 | Ranking `/gdcluster/ranking` | Clúster visible con 3/3; muestra el **pseudónimo** si se definió |
| 8 | Líder `PATCH /api/cluster/[id]` (nombre/pseudónimo) | 200; cambio visible en detalle y ranking |
| 9 | Miembro **sale** | Clúster vuelve a `pending`; líder puede invitar reemplazo |
| 10 | Líder **sale** | Transferencia de liderazgo (o `disbanded`) + historial |
| 11 | Regresión fondos: donación al clúster | `POST /api/gdcluster/donations/verify` → fund recibe 80% (`cluster_wallet`) |
| 12 | Admin smoke | `GET /api/admin/clusters` 200 (verificador); disolver: 403 (verificador) / 200 (admin, `disbanded`) |

**Criterio de aceptación del spec:** 12/12 verdes (o skip explícito cuando el fixture no aplique).

### 13.4 Regresión

- `make type` (app + motor) sin errores.
- Unit del motor: 17+6+13 existentes + nuevas suites verdes.
- Suite E2E completa (23 specs) sin fallos nuevos respecto a la línea base.
- Donaciones GD existentes (`donate-gd-real`, `vault-both-donate`) siguen verdes (no romper el modelo de fondos).

---

## 14. Out of Scope

- Multiple clusters per pastor/church.
- Bulk invitations.
- Email or WhatsApp notifications (in-app only).
- Automatic activation by member count on-chain (manual backend for now).
- Cluster logo or image.
- Moving `cluster_wallet` from `church` to `clustergd` (future decision; documented in the migration).

---

**Created:** 2026-08-31 (redesign on the existing GD model)
**Supersedes:** the original draft's `cluster`/`cluster_member` (parallel tables, duplicated `/api/cluster/create|accept` routes, no fund integration) — discarded due to collision with `clustergd`/`church_clustergd`/`ClusterFundsV2` and the existing `/api/cluster/*` routes.
**Status:** Implemented (2026-08-31) — migration applied (`20260831100000_cluster_formation`), engine routes (status/candidates/invitations/accept/reject), extended create/update/leave (pseudonym, leader-only edit, leadership transfer/disband, pending→active at 3), `/api/admin/clusters/*`, ranking pseudonym, UI `/{lang}/cluster`. Unit tests: 9 new (candidates + invitations) — gdcluster suite 42/42, `make type` green. E2E `cluster-formation.spec.mjs` **verde** contra el dev site (2026-08-31): auth gating, status/candidates, admin CRUD (create 201 → update pseudonym → ranking display_name → disband → excluido del ranking), accept 404. Dos bugs encontrados y corregidos durante la verificación: join-builder `'c.id'` como valor en `getClusterCandidates` (→ `sql.ref`) y `onConflict` sobre `church_clustergd` sin constraint único (→ insert directo).
**Dependencies:** #161 ⏳ (content), #163 ✅, #155/214 ✅ (funds)
