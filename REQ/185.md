# R-#185: Consolidate useAuthAddress and Finish Wagmi/Session Unification

Consolidate the `useAuthAddress` hook created in R-#167 and extend it to eliminate
the remaining `useAccount()` → `useAuthAddress()` fragmentation across the app.

## Motivation

R-#167 identified that wagmi's `useAccount()` loses connection on client-side
navigation (NextAuth bug #5719) while NextAuth's `session.address` persists via
cookies. We patched Header and course pages individually, then created
`useAuthAddress` as a unified hook. This REQ completes the job: finish migration,
add auto-reconnect, and add tests.

---

## Phase 1: Finish Migration ✅ (Done 2026-07-13)

| Component | Status |
|-----------|--------|
| `components/Header.tsx` | ✅ Migrated |
| `app/[lang]/page.tsx` (landing) | ✅ Migrated |
| `app/[lang]/[pathPrefix]/page.tsx` (course) | ✅ Migrated |
| `app/[lang]/[pathPrefix]/[pathSuffix]/page.tsx` (guide) | ✅ Migrated |

Components that intentionally keep `useAccount()` (need live wallet for signing):
profile, crossword, donation, claims, NewChurchDialog, DeleteVerifiedDataDialog.

---

## Phase 2: Auto-Reconnect (Pendiente)

**Problem:** `useAuthAddress` falls back to session when wagmi disconnects, but
the user still sees "Connect Wallet" in RainbowKit's ConnectButton. On pages
where both wagmi and session are available, we should attempt to reconnect
wagmi automatically.

**Proposal:** Add a `useEffect` inside `useAuthAddress` that calls
`wagmi`'s `reconnect()` (or `connect({ connector: injected() })`) when
`session.address` exists but wagmi reports disconnected.

```ts
// Inside useAuthAddress:
useEffect(() => {
  if (sessionAddress && !wagmiAddress && !isConnected) {
    // Attempt reconnection silently
    connectAsync?.({ connector: injected() }).catch(() => {})
  }
}, [sessionAddress, wagmiAddress, isConnected])
```

This attacks the root cause: after navigation, wagmi's context is stale but the
injected provider is still available — a reconnect should succeed.

**Risk:** Infinite reconnect loops if the provider is genuinely unavailable.
Mitigate with a retry limit (3 attempts, 2s apart).

---

## Phase 3: Unit Tests for useAuthAddress (Pendiente)

Four states to test:

| State | wagmi | session | Expected `address` | Expected `isAuthenticated` | Expected `isWalletConnected` |
|-------|-------|---------|-------------------|---------------------------|---------------------------|
| Both match | `0xabc` | `0xabc` | `0xabc` | true | true |
| Wagmi only | `0xabc` | null | `0xabc` | true | false |
| Session only | undefined | `0xabc` | `0xabc` | true | false |
| None | undefined | null | undefined | false | false |

Test file: `lib/hooks/__tests__/useAuthAddress.test.ts`

Mock `useAccount` and `useSession` via `vi.mock` as done in existing hook tests
(`useScholarshipData.test.ts`).

---

## Acceptance Criteria

- [x] `useAuthAddress` hook exists and is used in all read-only pages
- [x] `useAuthAddress` auto-reconnects wagmi when session exists but wagmi disconnected
- [ ] Unit tests for all 4 auth states
- [ ] No regression: type check + existing tests pass

---

**Created:** 2026-07-13
**Updated:** 2026-07-14 — Phase 2 (auto-reconnect) revirtió; causaba 3 popups "Accept connection". Fix real: localStorage fallback en ConnectWalletButton (R-#186).
**Status:** En Progreso (Phase 1 ✅, Phase 2 ❌ revirtió, Phase 3 ❌)
