# R-#245: Testing plan for `pdj-wallet` packages

**Link:** https://github.com/pasosdeJesus/learn.tg/issues/245

**Status:** In progress (2026-09-15, branch `mvppwa`) — the two unit layers
are implemented and green; the learn.tg integration layer still needs its alias;
the minimal E2E spec, the manual test page and the doc are pending.
**Created:** 2026-09-15
**Type:** Testing / Documentation
**Priority:** High (blocks the R-#244 verification)
**Dependencies:** R-#236, R-#237

---

### 1. Objective

Define the testing strategy for the `pdj-wallet` packages so that tests are fast (<5 min automated), lightweight (<1 GB RAM), and runnable on OpenBSD/adJ. Avoid the 45-minute full E2E suite for wallet-only changes.

### 2. Testing layers

| Layer | Scope | Tool | Time | Memory |
|---|---|---|---|---|
| **Unit (core)** | `pdj-wallet` functions | Vitest | seconds | <1 GB |
| **Unit (next)** | `pdj-wallet-next` hooks/components | Vitest + Testing Library | seconds | <1 GB |
| **Integration (learn.tg)** | `useAuthAddress`, `WalletSelector`, `useWriteContract` | Vitest (existing) | seconds | <1 GB |
| **E2E (minimal)** | In-app wallet flow on dev site | Puppeteer (existing) | ~30 s | <1 GB |
| **Manual (PWA)** | Service worker, offline | Chrome mobile | minutes | — |

### 3. Commands

Both new packages ship their own `vitest.config.ts` (a plain object with an
alias map onto `apps/nextjs/node_modules`), so `pnpm test` is no longer the
blocker it was: the package `test` scripts call
`../../apps/nextjs/node_modules/.bin/vitest` directly and can be run even where
corepack resolves pnpm 11 (the repo is pinned to pnpm 10). Run them from
`apps/nextjs`, which owns the dependency graph:

```bash
cd apps/nextjs

# Core wallet (fast) — verified 2026-09-15: 18 passed, 1 skipped, 8.2 s
./node_modules/.bin/vitest run --root ../../packages/pdj-wallet \
  --config ../../packages/pdj-wallet/vitest.config.ts

# Next components (fast) — verified 2026-09-15: 14 passed, 7.2 s
./node_modules/.bin/vitest run --root ../../packages/pdj-wallet-next \
  --config ../../packages/pdj-wallet-next/vitest.config.ts

# Integration in learn.tg (fast) — red today, see the note below
make test-hooks test-components

# Minimal E2E (fast)
CHROME_PATH=/usr/local/bin/chrome make test-e2e-spec SPEC=in-app-wallet

# Full E2E (slow, regression only)
CHROME_PATH=/usr/local/bin/chrome make test-e2e
```

Notes (measured 2026-09-15):

- The integration layer cannot resolve `@learn-tg/pdj-wallet`, which the linked
  `packages/pdj-wallet-next/dist/useInAppWallet.js` imports as a bare specifier:
  `lib/hooks/__tests__/useScholarshipData.test.ts` fails and `make test` stops at
  `test-hooks`. Fix: add the two aliases to `apps/nextjs/vitest.config.ts`
  (same pattern as `@learn-tg/rewards/*` and the `gdcluster` engines).
- `pnpm typecheck` also needs the new fields: `tsconfig.test.json` reports 4
  errors in `lib/hooks/__tests__/useGuideData.test.ts` because its mock of
  `useAuthAddress()` lacks `inAppAddress` and `isInAppUnlocked`.
- `packages/rewards` and `packages/gdcluster` still have no `vitest.config.ts`
  and their `package.json` `test` script points to a non-existent file; the same
  fix applies.

### 4. Test data

**Fixtures (as used today):**

- Known mnemonic: `test test test test test test test test test test test junk`
  (Hardhat default) — `packages/pdj-wallet/src/__tests__/wallet.test.ts:17` and
  `provider.test.ts:8`.
- PIN used throughout: `123456`.
- Known SIWE message: built inline in `wallet.test.ts:80`
  (`learn.tg:9001 wants you to sign in with your Ethereum account:`).

**Storage:**

- `MemoryStorage` for unit tests — ✅ used.
- `IndexedDBStorage` for the browser — ✅ implemented; its unit test
  (`indexeddb.test.ts`) self-skips today because `fake-indexeddb` is declared in
  `packages/pdj-wallet/package.json` devDependencies but is **not installed**
  (`1 skipped`); running `pnpm install` in `apps/nextjs` enables it.
- `FileStorage` for Node.js E2E — ⬜ **not implemented** (post-MVP, and only
  needed if R-#239 migrates the E2E helpers to this core).

**RPC:**

- Mock RPC for `getInAppWalletProvider` unit tests — ✅ used in
  `provider.test.ts`.
- Celo Sepolia for E2E — pending (the MVP E2E spec is not written yet).

### 5. Minimal E2E spec

**File:** `e2e/specs/in-app-wallet.spec.mjs` — ✅ written (2026-09-15). It skips
with a clear message when `/en/test/wallet` is not deployed, because the dev
site serves `main` today.

The steps below are the target. They finish with a real NextAuth **session
cookie** (R-#233 Phase 2 removed the API token), and any Node-side API call must
forward that cookie (as the fixed `verified-city-gate` and `interview-date` specs
now do).

**Steps:**

1. Launch browser.
2. Go to `learn.tg:9001/en/test/wallet`.
3. Create wallet (PIN: `123456`).
4. Get the address.
5. Build SIWE message.
6. Sign with in-app wallet.
7. POST to `/api/auth/callback/credentials`.
8. Verify session cookie set.
9. Go to `/en/profile`.
10. Verify address displayed.

**Time:** ~30 s.

### 6. Test page for manual development

**File:** `app/[lang]/test/wallet/page.tsx` — ✅ created. It drives one single
`useInAppWallet()` instance: create/import with a PIN, unlock, sign in with SIWE,
lock and delete, plus a log area (the recovery phrase is shown once, for
debugging). It uses only data-testid hooks, which is what the E2E spec drives.

**Not for production:** the page renders a "disabled" notice when
`NEXT_PUBLIC_NETWORK === 'celo'`.

### 7. Documentation

**File:** `doc/pdj-wallet-testing.md` — ✅ written (2026-09-15). This is the
single doc name (R-#244 §9 and R-#240 §6 point here too).

**Content:**

- How to run unit tests (packages and integration). ✅
- How to run the minimal E2E spec (planned). ✅
- How to test manually the service worker, the manifest and installability. ✅
- How to test manually offline navigation, the `/offline` fallback and the
  offline banner. ✅
- Troubleshooting (OpenBSD/adJ). ✅

### 8. Acceptance criteria

- [x] `packages/pdj-wallet` has unit tests; the package suite passes in <10 s
  (run from `apps/nextjs`, see §3) — **18 passed / 1 skipped, 8.2 s**.
- [x] `packages/pdj-wallet-next` has unit tests; the package suite passes in
  <10 s (run from `apps/nextjs`, see §3) — **14 passed, 7.2 s**.
- [x] `e2e/specs/in-app-wallet.spec.mjs` exists and passes in <1 min
  (CHROME_PATH + IPDES/PUERTOPRU as in doc/e2e-testing.md) — written; it skips
  until the branch is deployed to the dev site (which serves `main`).
- [x] `app/[lang]/test/wallet/page.tsx` exists for manual development.
- [x] `doc/pdj-wallet-testing.md` published (2026-09-15).
- [ ] Total automated test time <5 min — the two package suites take ~15 s
  together, and the integration layer is green again (see §3).
- [x] Total memory <1 GB per test run — observed on this machine.

### 9. Related

- R-#234 (PWA umbrella)
- R-#236, R-#237, R-#238, R-#244

---

## Implementation status (2026-09-15)

| Layer | Status |
|-------|--------|
| Unit (core, `packages/pdj-wallet`) | ✅ 18 passed / 1 skipped in 8.2 s, `tsc -b` clean |
| Unit (next, `packages/pdj-wallet-next`) | ✅ 14 passed in 7.2 s, `tsc -b` clean |
| Integration (learn.tg) | ❌ `make test-hooks` cannot resolve `@learn-tg/pdj-wallet`; `tsconfig.test.json` has 4 errors in `useGuideData.test.ts` (see §3) |
| E2E (minimal) | ⬜ spec not written |
| Manual (PWA) | ⬜ pending; the PWA shell itself (R-#240) is not started |
| Docs | ⬜ `doc/pdj-wallet-testing.md` not written |

Next actions: fix the two app-side blockers (§3), then write the test page (§6),
the E2E spec (§5) and the doc (§7).

