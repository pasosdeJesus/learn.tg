# R-#152: Profiles of Church / GD Cluster

Build the foundation for Global Disciples cluster formation by enabling users to declare their church/place of worship, register as pastors, and form clusters of 3+ churches.

## Dependencies
None (base issue)

---

## MVP Checklist (Phase 1)

### 1. Church Relationship Declaration (User Profile)

Add a field to the user profile: **Church relationship** (`church_relationship`):

| Value | Description |
|-------|-------------|
| `pastor` | Pastor of a church |
| `leader` | Leader/elder |
| `member` | Member |
| *(empty)* | No relationship declared |

Only users with `church_relationship = 'pastor'` can declare a church and create/join clusters.

### 2. Profile Fields (User)

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `whatsapp` | VARCHAR(20) | ❌ No | User's personal WhatsApp |
| `telegram` | VARCHAR(50) | ❌ No | User's personal Telegram |
| `country_id` | INTEGER | ✅ Yes | References `msip_pais` |
| `department_id` | INTEGER | ❌ No | References `msip_departamento` |
| `municipality_id` | INTEGER | ❌ No | References `msip_municipio` |
| `city_id` | INTEGER | ❌ No | References `msip_centropoblado` |
| `place_of_worship_location` | VARCHAR(200) | ❌ No | OpenCage autocomplete — city, town, or village name (for all users) |
| `place_of_worship` | VARCHAR(100) | ❌ No | Free text. Name of the place of worship. Label adapts to religion: |
| `church_id` | INTEGER | ❌ No | References `church` (for Christians) |
| `id_photo_front` | TEXT | ❌ No | File path (Sierra Leone only) |
| `id_photo_back` | TEXT | ❌ No | File path (Sierra Leone only) |

**Place of worship labels by religion:**

| Religion | Name field (en/es) | Address field (en/es) |
|----------|-------------------|----------------------|
| Christianity (2) | Church / Iglesia | Address of church / Dirección de iglesia |
| Islam (3) | Mosque / Mezquita | Address of mosque / Dirección de mezquita |
| Judaism (6) | Synagogue / Sinagoga | Address of synagogue / Dirección de sinagoga |
| Buddhism (4) / Hinduism (5) | Temple / Templo | Address of temple / Dirección de templo |
| Other / None / Without info (1,7,8) | Place of worship / Lugar de culto | Address of place of worship / Dirección de lugar de culto |

For Christians, the name field becomes a church selector (existing or new) instead of free text.

**Verified fields** (used for scoring, set by pdJ admin):

| Field | Type | Notes |
|-------|------|-------|
| `verified_whatsapp` | VARCHAR(20) | Set when verified |
| `verified_telegram` | VARCHAR(50) | Set when verified |
| `verified_department_id` | INTEGER | Set when verified |
| `verified_municipality_id` | INTEGER | Set when verified |
| `verified_city_id` | INTEGER | Set when verified |
| `verified_place_of_worship` | VARCHAR(100) | Set when verified |
| `id_photo_verified` | BOOLEAN | Set when ID photos are verified |

### 2.1 ID Photo Upload (Sierra Leone)

When `country_id = 694` (Sierra Leone), the profile page shows file inputs for front and back ID photos.

| Aspect | Implementation |
|--------|---------------|
| **UI** | Two file inputs (JPEG/PNG) below the Country-Religion row, only when `profile.country === 694` |
| **Upload** | `POST /api/user/id-photo` — multipart FormData with `photo`, `side` (`front`\|`back`), `walletAddress`, `token` |
| **Delete** | `DELETE /api/user/id-photo` — body: `{ walletAddress, token, side }` |
| **Serve** | `GET /api/user/id-photo/[userId]?side=front\|back` — returns image with `Content-Type` header |
| **Storage** | `uploads/user/{userId}/id_front.{ext}` and `id_back.{ext}` — NOT in `public/`, served via API |
| **Privacy** | Photos are private; only the pdJ team can access for verification. Deleted when user clicks Delete or uses "Delete Verified Data". Deleting a photo that was verified resets `id_photo_verified` to FALSE |

### 3. Location Data for Non-Colombia/Venezuela Countries

For countries outside Colombia and Venezuela (where `msip_departamento`, `msip_municipio`, `msip_centropoblado` are not pre-populated):

| Step | Action | Notes |
|------|--------|-------|
| **1** | User enters a city/town/village name in `place_of_worship_location` | Free text input with OpenCage autocomplete |
| **2** | Backend queries OpenCage Geocoder API | Uses the user's input + country to fetch structured data |
| **3** | OpenCage returns `components.state`, `components.county`, `components.city`, `components.town`, `components.village` | These map to department, municipality, city |
| **4** | If OpenCage returns valid data, create/upsert records in `msip_departamento`, `msip_municipio`, `msip_centropoblado` | Only for the specific location the user entered |
| **5** | If OpenCage returns no data or errors, store the raw text in `place_of_worship_location` | For MVP, allow user to enter text freely |

**Notes:**
- OpenCage API key is required. The free tier allows 2,500 requests/day.
- Cache results in the database to avoid repeated API calls for the same location.
- For MVP, do not block the user if OpenCage fails — allow manual entry.
- The API key only converts text to coordinates, but OpenCage's `components` field returns structured address data.

### 4. Church Table (`church`)

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `name` | VARCHAR(100) | ✅ Yes | |
| `country_id` | INTEGER | ✅ Yes | |
| `department_id` | INTEGER | ❌ No | |
| `municipality_id` | INTEGER | ❌ No | |
| `city_id` | INTEGER | ❌ No | |
| `address` | TEXT | ❌ No | |
| `pastor_name` | VARCHAR(100) | ✅ Yes | |
| `pastor_whatsapp` | VARCHAR(20) | ✅ Yes | |
| `pastor_telegram` | VARCHAR(50) | ❌ No | |
| `pastor_id` | INTEGER | ❌ No | References `usuario` (when pastor joins) |
| `denomination` | VARCHAR(100) | ❌ No (Phase 2) | |
| `registration` | VARCHAR(50) | ✅ Yes (for pastors) | Required when a pastor creates or claims a church. **Only visible in the pastor dialogue.** |
| `registration_photo` | TEXT | ✅ Yes (for pastors) | File path to photo of registration document. **Only visible in the pastor dialogue.** |
| `registration_verified` | BOOLEAN | ❌ No | Set by pdJ admin after verifying the registration document |
| `created_by` | INTEGER | ✅ Yes | References `usuario` (who created the church record) |

**Notes:**
- `registration` and `registration_photo` are **required** when:
  - A pastor creates a church while completing their profile.
  - A pastor claims a church that was declared by another user.
- `registration_verified` does NOT give profile score points, but provides assurance that the pastor is legitimate.
- The `registration_photo` is stored in the local file system and **served via API** (not `public/`) for confidentiality — see privacy policy `en/privacy-policy`.
- `id_photo_front` and `id_photo_back` (Sierra Leone only) follow the same pattern: stored in file system, served via API.

### 4.1 Data Privacy & Visibility

| Data | Visibility | Notes |
|------|-----------|-------|
| Church name, city, pastor name | **Public** | Displayed on church profile page |
| Church address | **Public** | Displayed on church profile page |
| ID photos (`id_photo_front`, `id_photo_back`) | **Private** | Used only for identity verification by pdJ admin. A "Delete" button removes them from the server. |
| Registration documents (`registration_photo`) | **Private** | Used only for verification by pdJ admin. Deletable via "Delete" button. |
| WhatsApp, Telegram, email | **Private** | Never sold or shared. Used for: (a) verification of profile info, (b) occasional platform announcements |
| Cluster data (name, members, funds) | **Public** | Visible on cluster page |

**Deletion rules:**
- When ID documents or registration files are deleted, **profile score is recalculated** (points from verified fields are lost).
- When profile data changes (name, location, church), verified fields are reset and points are lost until re-verified.

### 5. Profile Score (Updated)

Calculated by `recalculateProfileScore()` in `lib/gd-utils.ts`, called from:
- `POST /api/update-scores` ("Update Scores" button)
- `PATCH /api/profile` (after "Save Changes")

| Component | Points | Condition |
|-----------|--------|-----------|
| **Name verified** | 26 | `nombre = passport_name` |
| **Country verified** | 26 | `pais_id = passport_nationality` |
| **Email verified** | 10 | `email = verified_email` |
| **WhatsApp or Telegram verified** | 10 | `verified_whatsapp = whatsapp` **OR** `verified_telegram = telegram` (10 max, not 20) |
| **GoodDollar verified** | 8 | `lastgooddollarverification IS NOT NULL` |
| **Location verified** | 10 | `department_id IS NOT NULL AND verified_department_id = department_id AND verified_municipality_id = municipality_id AND verified_city_id = city_id` |
| **Place of worship** | 10 | `verified_place_of_worship = place_of_worship` |
| **Total** | **100** | |

**Notes:**
- Points are **not awarded automatically**. The user must click "Update Scores" or "Save Changes" (which also recalculates).
- Verification for Christians requires manual confirmation (WhatsApp/Telegram contact with the pastor).
- ID photos (Sierra Leone) and `registration_verified` do NOT give points; they are for KYC and legitimacy.

### 6. Church/Place of Worship Declaration

A pastor declares their church from their profile page or a dedicated church page.

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| Geographic location (country/city) | Autocomplete | ✅ Yes | `place_of_worship_location` via OpenCage |
| Place of worship name | Text | ✅ Yes | `place_of_worship` (all users) |
| Church name | Autocomplete + open text | ✅ Yes (Christians only) | If not in list, opens pastor dialogue to create new entry |
| Pastor name | Text | ✅ Yes | Only in pastor dialogue |
| Pastor WhatsApp | Text | ✅ Yes | Only in pastor dialogue |
| Registration number | Text | ✅ Yes | Only in pastor dialogue |
| Registration photo | File upload | ✅ Yes | Only in pastor dialogue |

**Note:** Church declaration contributes to the "Place of worship" profile score component (10 pts) once verified — see §5.

### 7. Cluster Formation (Code-Based)

| Step | Description |
|------|-------------|
| **1. Create cluster** | Pastor creates a cluster from their church page → receives a 6-char alphanumeric code (e.g. `A3K9M2`) |
| **2. Share code** | Pastor shares the code with other pastors via WhatsApp, email, etc. |
| **3. Join cluster** | Other pastors enter the code on their church page → join the cluster |
| **4. Cluster formed** | When 3+ churches have joined, the cluster status changes to "Formed" |

**Rules:**
- A church can only belong to one cluster at a time. Joining a new cluster removes it from the previous one.
- No expiration on codes.
- No maximum member limit.
- Members can be added at any time after formation.
- All churches in a cluster must be from the same country.
- Cluster name must be unique within the same country (3-50 characters).
- Any pastor in the cluster can change the cluster name. Changes are logged in cluster history.
- Any pastor can click "Leave cluster" from their church page. Their church is removed from the cluster.
- When the last pastor leaves, the cluster is automatically dissolved.

### 8. Cluster Dissolution — Fund Handling

When a cluster dissolves (last pastor leaves), all funds held by the cluster are **transferred to the country fund** of the cluster's country. This logic is implemented in the `ClusterFunds` contract — see R-#155 §4.5 for the full specification.

| Scenario | Action |
|----------|--------|
| Pastor leaves and cluster becomes empty | Transfer all funds from cluster fund to country fund |
| Cluster is manually dissolved by admin | Transfer all funds from cluster fund to country fund |
| Country has no fund yet | Create country fund and transfer funds |

### 9. Cluster Page

| Section | Content |
|---------|---------|
| **Cluster name** | Set by any pastor in the cluster |
| **Members** | List of churches in the cluster with join dates |
| **Status** | "Formed" / "In Progress" |
| **Join code** | `A3K9M2` — for inviting more churches |
| **Leave cluster** | Button for each pastor to remove their church |
| **Cluster history** | Log of name changes, joins, and leaves |

### 10. Church Page Cluster Display

If the church belongs to a cluster, display:
```
## Church Centro de Paz
**Cluster:** Cluster Esperanza (View cluster)
```

### 11. Cluster Page Access

- **Entry point:** From church page
- **Visible to:** All users (public page)

---

### 12. Pastor Dialogue (New Church)

When a Christian user with `church_relationship = 'pastor'` selects "+ New church" from the church selector, a dialogue opens with these additional fields:

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| Church name | Text | ✅ Yes | Pre-filled from the selector's text input |
| Pastor name | Text | ✅ Yes | |
| Pastor WhatsApp | Text | ✅ Yes | |
| Registration number | Text | ✅ Yes | Registration number |
| Registration photo | File upload | ✅ Yes | Photo of registration document |

**Rules:**
- This dialogue **only appears** for users with `church_relationship = 'pastor'`.
- Non-pastors (leader, member) can select an existing church but cannot create a new one.
- Non-Christians skip the church selector entirely — they only fill `place_of_worship` as free text.

---

## Phase 2 Checklist

| Feature | Description | Priority |
|---------|-------------|----------|
| **Delegation** | Pastors can delegate administration to trusted leaders | 🟡 Medium |
| **Anonymous clusters** | Checkbox "Due to persecution, do not publish name/country" | 🟡 Medium |
| **Church reputation** | Reputation system based on activity (separate issue #164) | 🟡 Medium |
| **Pastor wallet** | Field for pastor's wallet address | 🟢 Low |
| **Registration** | Field for registration number and photo | 🟢 Low |
| **Interview scheduling (SL)** | Date proposals for manual verification | 🟢 Low |
| **Church curation** | Deduplication and notifications | 🟢 Low |
| **Change management** | Pastor change, name change, denomination change | 🟢 Low |
| **Cluster join notifications** | Notify members when a new church joins the cluster | 🟢 Low |

---

## MVP Acceptance Criteria

- [x] Users can set their church relationship (pastor/leader/member) in their profile
- [ ] Pastors can declare their church (name, location, WhatsApp, registration, registration photo) — POST /api/church exists, needs registration + photo + profile wiring
- [x] Pastors can create a cluster and receive a join code (6-char alphanumeric)
- [x] Other pastors can join the cluster by entering the code
- [x] Cluster status changes to "Formed" when 3+ churches have joined
- [x] Cluster page displays members, status, and the join code
- [x] Any pastor can leave the cluster; cluster dissolves when empty
- [ ] When cluster dissolves, funds are transferred to the country fund — depends on R-#155 (ClusterFunds contract)
- [x] Cluster name is unique within the same country
- [x] A church can only belong to one cluster at a time
- [x] All churches in a cluster must be from the same country
- [x] Cluster history logs name changes, joins, and leaves

---

## Implementation Notes

| Aspect | Decision |
|--------|----------|
| **Database** | Tables `church`, `clustergd`, `church_clustergd` (join) |
| **User field** | `usuario.church_relationship` ENUM('pastor','leader','member') |
| **Cluster code** | 6-char alphanumeric, stored in `clustergd.code` |
| **File storage** | Local: `NEXT_PUBLIC_UPLOAD_PATH/church/{church_id}/registration.jpg` |
| **Encryption** | Sensitive data (WhatsApp) handled in #153 |
| **OpenCage** | Used for non-CO/VE countries to fetch structured location data |
| **One cluster rule** | A church can belong to only one cluster; joining a new one removes from previous |
| **Country consistency** | All churches in a cluster must be from the same country |
| **Name uniqueness** | Cluster name must be unique within the same country (3-50 chars) |
| **No limits** | No expiration on codes, no max member count |
| **Dissolution** | Cluster auto-dissolves when last pastor leaves; funds → country fund |

---

## Privacy Policy Compliance

Revisado contra `app/[lang]/privacy-policy/page.tsx`:

### Problemas encontrados

| # | Problema | Impacto |
|---|----------|---------|
| 1 | **item2** dice "el resto de la información en tu perfil es privada", pero las páginas de iglesia/clúster muestran públicamente `pastor_name`, `city_name` y `church.name` | Contradicción: datos declarados privados se muestran públicos |
| 2 | **Sin mención de documentos subidos**: `registration_photo`, `id_photo_front`, `id_photo_back` se almacenan en el servidor y no hay política sobre su manejo | Los usuarios no saben cómo se tratan sus documentos de identidad |
| 3 | **Sin mención de WhatsApp/Telegram**: se recolectan en el perfil pero la política no dice si se comparten o cómo se usan | Contacto de pastores queda en zona gris |
| 4 | Las páginas de iglesia son **públicas** — cualquier persona puede ver `pastor_name` sin login. La política no aclara que los datos de iglesia son visibles públicamente | El usuario no es advertido al declarar una iglesia |

### Ajustes necesarios

| Prioridad | Acción |
|-----------|--------|
| 🔴 Alta | Agregar item a la política: "Los datos de iglesias y clústeres (nombre, ciudad, nombre del pastor) son públicos. Al declarar una iglesia, aceptas que esta información sea visible para todos los usuarios." |
| 🔴 Alta | Agregar item sobre documentos: "Las fotos de documentos de identidad y registros gubernamentales se almacenan encriptados en nuestros servidores. Solo el equipo de Pasos de Jesús puede acceder a ellos con fines de verificación. No se comparten con terceros." |
| 🟡 Media | Agregar item sobre WhatsApp/Telegram: "Tu número de WhatsApp/Telegram se usa exclusivamente para contacto relacionado con la plataforma y verificación. No se muestra públicamente." |
| 🟡 Media | Agregar advertencia en el formulario de declaración de iglesia: "Los datos de tu iglesia serán visibles públicamente." |

---
- Delegation system
- Anonymous clusters
- Church reputation (separate issue #164)
- Interview scheduling
- Church curation / deduplication (Phase 2)
- Change management (pastor change, name change)
- Cluster join notifications
- Ranking integration (#154)
- Course unlock (#160)

---

**Created:** 2026-06-28
**Updated:** 2026-07-07 — Scoring updated, profile API migrated, photo upload, church dialog, privacy policy, tests expanded
**Status:** En Progreso (2/12 MVP pending)

---

## Implementation Status

### ✅ Done

| Component | Notes |
|-----------|-------|
| DB: `church` (20 cols), `clustergd`, `church_clustergd`, `clustergd_history` | Full schema |
| DB: `usuario` columns | `church_relationship`, `whatsapp`, `telegram`, `country_id`, `department_id`, `municipality_id`, `city_id`, `place_of_worship`, `church_id`, `id_photo_*` + 7 `verified_*` + `verified_email` |
| `church_relationship` in profile | Only when Christian (id=2). Values: `pastor`, `leader`, `member`. Placed before church fields |
| `/api/geocode` city autocomplete | OpenCage, filtered by country via `msip_pais.alfa2` |
| `/api/churches/search` | Search by name + country, supports empty query |
| `/api/countries` | Reads `msip_pais` directly (Next.js, no Rails) |
| `/api/profile` GET + PATCH | Replaces Rails `NEXT_PUBLIC_API_USERS` + `NEXT_PUBLIC_API_UPDATE_USER`. Persists all R-#152 columns |
| `POST /api/church` | Any Christian can declare. Pastors require `registration` + `registration_photo`. Multiple churches allowed |
| `GET /api/church/[id]` | Public, includes cluster info |
| CRUD APIs for clusters | Create, join by code, rename, leave (auto-dissolve) |
| `DELETE /api/user/verified-data` | Clears verified fields + replaces profile info with wallet-derived pseudonym + deletes photo files |
| `POST/DELETE /api/user/id-photo` | Upload/delete ID photos (front/back), 5MB limit, error messages |
| Church + cluster pages | `app/[lang]/church/[id]`, `app/[lang]/cluster/[id]` |
| Profile UI | Religion-specific labels (Church, Mosque, Temple, etc.), Location of church, Church selector, "+ New church" dialog, photo upload with spinner, Delete Verified Data button |
| `NewChurchDialog` | Inline form for church registration with pastor note |
| `DeleteVerifiedDataDialog` | Type-to-confirm, calls API |
| `lib/gd-utils.ts` | Cluster codes (6-char), church/cluster helpers, `recalculateProfileScore` (100-pt formula), `updateProfileScore` |
| Profile scoring | 26+26+10+10+8+10+10=100 formula in `recalculateProfileScore` |
| `create-course.sh` | Kysely migration, language param (`en`/`es`), optional course ID, logical framework (objective + result + guides) |
| Footer | Compact: `Privacy Policy \| Terms \| Telegram` left, `Follow Us` + icons right |
| Header | ☰ hamburger menu (3 items, wallet-only), 180px compact toasts |
| Privacy Policy | 7 items covering church data, documents, WhatsApp/Telegram, delete flow |
| Tests | 31 unit tests (18 gd-utils + 6 profile API + 5 update-scores + 2 verified-data) |

### 🔧 Pending

| Component | Depends On |
|-----------|------------|
| §3 OpenCage → upsert `msip_departamento`/`municipio`/`centropoblado` | Post-MVP |
| §8 Cluster dissolution → country fund | R-#155 (ClusterFunds contract) |
| Phase 2 features | R-#153, R-#154, R-#155, R-#157, etc. |

