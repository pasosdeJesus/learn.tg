# R-#163: Referral Program for learn.tg (Actualizado)

Implement a general referral program for the learn.tg platform that:
1. Activates when a user purchases **any premium course** (including Global Disciples)
2. Rewards referrers when referred users complete courses (gratuitos, misionales, or premium)
3. Provides a dedicated referral menu with: code, balance, history, and referral status
4. Includes a **testimony system** for Global Disciples course completers to attest to their leader's impact

## Dependencies
- Existing course system (`cor1440_gen_proyectofinanciero`)
- Existing USDT and SLEARN contracts
- #161 (Global Disciples Course — activates referrals on premium purchase)
- #155 (Contract for Cluster/Country Funds — referral fund source)
- `transaction` table — referral rewards are value movements, must be recorded
- **LearnTGVaultsV5** — modified contract with flexible split (#196)

**Note:** Referral rewards are now handled by **LearnTGVaultsV5** where the backend passes the referrer's share as parameters (`referrerUSDT`, `referrerSlearn`). The contract does not calculate percentages.

---

## 1. Referral Program Overview

### 1.1 Activation
The referral program is activated when a user **purchases any premium course**. Premium courses include:
- Global Disciples Course
- Any other premium course added in the future

### 1.2 Referral Reward Source

**The reward for the referrer comes directly from the scholarship of the referred student.**

| Aspect | Decision |
|--------|----------|
| **Source** | 10% of the scholarship (USDT and SLEARN) is deducted and sent to the referrer |
| **Timing** | Every time the student completes a guide and receives a scholarship |
| **Visibility** | The student sees the deduction in the UI |
| **Permanence** | Applies to every scholarship the student ever receives |

### 1.3 Reward Formula

```
If scholarship USDT = 1.00 and scholarship SLEARN = 5

Referrer receives:
  USDT: 0.10 (10% of 1.00)
  SLEARN: 0.50 (10% of 5)

Student receives:
  USDT: 0.90
  SLEARN: 4.50
```

### 1.4 Example by Course

| Course | Scholarship per guide | Referrer (10%) | Student (90%) |
|--------|-----------------------|----------------|---------------|
| **Web3 & UBI** | 1 USDT + 1 SLEARN | 0.10 USDT + 0.10 SLEARN | 0.90 USDT + 0.90 SLEARN |
| **Global Disciples** | 1 USDT + 5 SLEARN | 0.10 USDT + 0.50 SLEARN | 0.90 USDT + 4.50 SLEARN |
| **Other course** | 1 USDT + 1 SLEARN | 0.10 USDT + 0.10 SLEARN | 0.90 USDT + 0.90 SLEARN |

---

## 2. Referral Code and Tracking

### 2.1 Code Generation
- Each user receives a **unique referral code** upon purchasing a premium course
- The code is generated using `crypto.randomUUID()` and takes the first 8 characters
- The code is visible in the referral menu
- The code can be shared via link, QR code, or text
- Codes do not expire (`expires_at` can be NULL)

### 2.2 Referral Link Format
```
https://learn.tg/ref/{CODE}
```

### 2.3 Tracking
When a new user connects their wallet for the first time:
- If they used a referral link, the referrer is recorded
- If not, they can enter a referral code manually
- The referral relationship is stored in the database
- Each user can only claim a referral code once (`referral_claimed_at` tracks this)

---

## 3. Testimony System (For Global Disciples Only)

### 3.1 Purpose

When a referred user completes the Global Disciples course, they can fill out a **testimony form** attesting to the leader's impact. This is used to:
1. Calculate the leader's score for the **Program Director** role
2. Verify the leader's missionary activity
3. Reward leaders with additional points

### 3.2 Testimony Form

When a referred student completes the Global Disciples course, they receive a notification to "Testify for your leader."

**Fields:**

| Field | Type | Description |
|-------|------|-------------|
| `presented_christ` | Checkbox | The leader introduced me to Christ for the first time |
| `discipled` | Checkbox | The leader discipled me (taught me to follow Christ) |
| `came_mission` | Checkbox | The leader came on a mission trip to my area |
| `invited_event` | Checkbox | The leader invited me to a Bible study/event |
| `helped_faith` | Checkbox | The leader helped me grow in my faith |
| `relationship` | Select | Pastor, Group Leader, Friend, Family, Other |
| `other_relationship` | Text | If "Other" is selected |
| `comments` | Textarea | Additional comments (optional) |

### 3.3 Testimony Flow

```mermaid
flowchart LR
    A[Referred user completes GD course] --> B[Receives notification to testify]
    B --> C[Fills out form with checkboxes]
    C --> D[Signs with wallet]
    D --> E[Testimony saved]
    E --> F[Leader accumulates points]
```

### 3.4 Testimony Database

```sql
CREATE TABLE testimonies (
    id SERIAL PRIMARY KEY,
    leader_id INTEGER REFERENCES usuario(id) NOT NULL,
    witness_id INTEGER REFERENCES usuario(id) NOT NULL,
    course_id INTEGER,                      -- Which GD course (for future multi-course support)
    presented_christ BOOLEAN DEFAULT FALSE,
    discipled BOOLEAN DEFAULT FALSE,
    came_mission BOOLEAN DEFAULT FALSE,
    invited_event BOOLEAN DEFAULT FALSE,
    helped_faith BOOLEAN DEFAULT FALSE,
    relationship VARCHAR(50),
    other_relationship TEXT,
    comments TEXT,
    signature_wallet VARCHAR(42) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(leader_id, witness_id)
);
```

### 3.5 Testimony Score (For Program Director Selection)

| Criterion | Points |
|-----------|--------|
| Presented Christ | +10 |
| Discipled | +8 |
| Came on mission | +6 |
| Invited to event | +4 |
| Helped grow in faith | +4 |
| Is my pastor | +5 |
| Is my group leader | +3 |

```typescript
// lib/leader-score.ts
export function calculateLeaderScore(testimonies: Testimony[]): number {
  let score = 0
  for (const t of testimonies) {
    if (t.presented_christ) score += 10
    if (t.discipled) score += 8
    if (t.came_mission) score += 6
    if (t.invited_event) score += 4
    if (t.helped_faith) score += 4
    if (t.relationship === 'pastor') score += 5
    if (t.relationship === 'group_leader') score += 3
  }
  return score
}
```

---

## 4. LearnTGVaultsV5 — Integration (Reference)

The referral program uses **LearnTGVaultsV5** (#196) where:

| Parameter | Description |
|-----------|-------------|
| `referrer` | Address of the referrer (or `address(0)` if none) |
| `referrerUSDT` | USDT amount to send to referrer (backend-calculated) |
| `referrerSlearn` | SLEARN amount to send to referrer (backend-calculated) |

The backend calculates:
- 10% of the scholarship for USDT and SLEARN
- Passes these amounts to the contract
- The contract transfers them to the referrer

**Note:** The contract does not calculate percentages. The backend handles all percentage logic.

---

## 5. Referral Menu

### 5.1 Menu Access
- **Desktop:** Visible in the main navigation (expanded)
- **Mobile:** Visible in the dropdown menu
- **Visibility:** Only visible after the user has activated the referral program

### 5.2 Menu Sections

#### 5.2.1 Referral Code
```
## Tu código de referido
`GD-ABC123`

[Compartir código] [Copiar enlace] [Ver QR]
```

#### 5.2.2 Referral History
```
## Historial de referidos

| Fecha | Referido | Curso completado | Recompensa | Estado |
|-------|----------|------------------|------------|--------|
| 2026-06-28 | Juan Pérez | Web3 & UBI | 0.10 USDT + 0.10 SLEARN | ✅ Pagado |
| 2026-06-27 | María Gómez | Global Disciples | 0.50 USDT + 0.50 SLEARN | ✅ Pagado |
```

#### 5.2.3 Referral Status
```
## Estado de mis referidos

| Referido | Código | Curso completado | Estado |
|----------|--------|------------------|--------|
| Juan Pérez | REF-001 | Web3 & UBI | ✅ Completado |
| María Gómez | REF-002 | Global Disciples | ✅ Completado |
| Pedro López | REF-003 | — | ⏳ Pendiente |
```

#### 5.2.4 Testimony Requests (for GD leaders)
```
## Atestiguamientos pendientes

| Referido | Curso completado | Acción |
|----------|------------------|--------|
| María Gómez | Global Disciples | [Solicitar atestiguamiento] |
| Carlos Ruiz | Global Disciples | [Solicitar atestiguamiento] |
```

---

## 6. Database Schema

### 6.1 Referral Codes

```sql
CREATE TABLE referralcode (
    id SERIAL PRIMARY KEY,
    usuario_id INTEGER REFERENCES usuario(id) NOT NULL,
    code VARCHAR(20) UNIQUE NOT NULL,
    activated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP,                     -- NULL = never expires
    active BOOLEAN DEFAULT TRUE
);
```

### 6.2 Referral Relationships

```sql
CREATE TABLE referralrelationship (
    id SERIAL PRIMARY KEY,
    referrer_id INTEGER REFERENCES usuario(id) NOT NULL,
    referred_id INTEGER REFERENCES usuario(id) NOT NULL,
    referral_code VARCHAR(20),                -- The code used
    referral_claimed_at TIMESTAMP,            -- When the code was claimed
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(20) DEFAULT 'pending'     -- 'pending', 'completed', 'paid'
);
```

### 6.3 Testimonies

```sql
CREATE TABLE testimonies (
    id SERIAL PRIMARY KEY,
    leader_id INTEGER REFERENCES usuario(id) NOT NULL,
    witness_id INTEGER REFERENCES usuario(id) NOT NULL,
    course_id INTEGER,                        -- Which GD course (for future multi-course support)
    presented_christ BOOLEAN DEFAULT FALSE,
    discipled BOOLEAN DEFAULT FALSE,
    came_mission BOOLEAN DEFAULT FALSE,
    invited_event BOOLEAN DEFAULT FALSE,
    helped_faith BOOLEAN DEFAULT FALSE,
    relationship VARCHAR(50),
    other_relationship TEXT,
    comments TEXT,
    signature_wallet VARCHAR(42) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(leader_id, witness_id)
);
```

### 6.4 Transaction Table

Every referral reward payment creates entries in the `transaction` table:

```sql
-- One entry per crypto per recipient per reward
INSERT INTO transaction (
    usuario_id, wallet, crypto, type, amount, balance_impact, date, hash,
    categoria, subcategoria, metadata
) VALUES (
    :referrer_id, :wallet, 'USDT', 'referral_reward', 0.10, 0.10, NOW(), :hash,
    'referral', 'referrer',
    '{"referred_id": 456, "course_id": 1, "guide_id": 2}'
);
```

**CHECK constraint update:** Add `'referral_reward'` to the `transaction` type CHECK constraint.

---

## 7. API Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/referral/code` | GET | Get user's referral code |
| `/api/referral/stats` | GET | Get referral statistics |
| `/api/referral/history` | GET | Get referral history |
| `/api/referral/claim` | POST | Enter referral code (first wallet connection) |
| `/api/referral/share` | POST | Generate shareable link |
| `/api/testimony` | POST | Submit testimony for a leader |
| `/api/testimony/:id` | GET | Get testimony details |

---

## 8. Events

All referral events are logged in `userevent`:

| Event | When | Data |
|-------|------|------|
| `referral_activated` | User purchases premium course | `{ user_id, code }` |
| `referral_claimed` | New user enters referral code | `{ referrer_id, referred_id, code }` |
| `referral_reward_paid` | Referrer receives reward | `{ referrer_id, referred_id, course_id, guide_id, usdt_amount, slearn_amount }` |
| `testimony_submitted` | User submits testimony | `{ leader_id, witness_id }` |

---

## 9. Security

### 9.1 Validation Checks

| Check | Description |
|-------|-------------|
| **Code uniqueness** | Each referral code must be unique |
| **Valid course** | The completed course must be valid |
| **One time per relationship** | Each referral relationship pays only once per course |
| **No self-referral** | A user cannot refer themselves (enforced by backend) |
| **Referral claimed once** | A user can only claim one referral code |

### 9.2 Anti-Fraud Measures

| Measure | Description |
|---------|-------------|
| **Unique wallets** | A user cannot refer themselves |
| **Course completion** | Rewards only paid after course completion |
| **Manual review** | Suspicious activity flagged for review |
| **Rate limiting** | Limited referral claims per day |

---

## 11. Implementation Phases

### Phase 1 — DB + Core Logic

**Goal:** Tables and API for referral codes and relationships. No rewards yet.

| # | Task | Depends on |
|---|---|---|
| 1.1 | Migration: `referralcode` + `referralrelationship` tables | — |
| 1.2 | `/api/referral/code` — generate & return user's referral code | 1.1, premium purchase trigger |
| 1.3 | `/api/referral/claim` — new user enters referral code on first wallet connect | 1.1 |
| 1.4 | `/api/referral/stats` — referral statistics (count, pending, completed) | 1.1 |
| 1.5 | `/api/referral/history` — referral history for the referrer | 1.1 |
| 1.6 | `/api/referral/share` — generate shareable link + QR | 1.2 |
| 1.7 | Referral menu UI (code, share, history, status) | 1.2–1.6 |

**Delivers:** Users can get referral codes, share them, and track referrals. No payments.

---

### Phase 2 — Rewards (requires V5)

**Goal:** Automatic 10% referral rewards via LearnTGVaultsV5.

| # | Task | Depends on |
|---|---|---|
| 2.1 | Add `'referral_reward'` to `transaction` type CHECK constraint | — |
| 2.2 | Modify `check-crossword` to look up referrer and pass `referrer*` params to V5 | #196 (V5), 1.1 |
| 2.3 | Record `referral_reward` entries in `transaction` table | 2.2 |
| 2.4 | Update referral history UI to show rewards paid | 2.3 |

**Delivers:** Referrers earn 10% automatically on every scholarship their referrals receive.

---

### Phase 3 — Testimony System

**Goal:** GD course completers can testify about their leader.

| # | Task | Depends on |
|---|---|---|
| 3.1 | Migration: `testimonies` table | — |
| 3.2 | `/api/testimony` — submit testimony (wallet-signed) | 3.1, #161 |
| 3.3 | `/api/testimony/:id` — get testimony details | 3.1 |
| 3.4 | `lib/leader-score.ts` — calculate leader score from testimonies | 3.1 |
| 3.5 | Testimony form UI (checkboxes + relationship + comments) | 3.2 |
| 3.6 | Testimony notification after GD course completion | #161 |
| 3.7 | Leader dashboard showing testimonies + score | 3.4 |

**Delivers:** Complete testimony pipeline for GD leaders.

---

## 12. What's Already Done

Nothing — this REQ depends entirely on #161 (GD course payment), #196 (V5 contract), and #155 (ClusterFunds), none of which are deployed yet. The `transaction` table exists but needs a CHECK constraint update.

---

## 13. Acceptance Criteria

### Referral Program
- [ ] Referral program activates when user purchases a premium course
- [ ] Unique referral code is generated upon activation (UUID first 8 chars)
- [ ] Referral menu is visible after activation
- [ ] Referral menu shows: code, history, status
- [ ] Referral rewards are paid as 10% of the scholarship (USDT + SLEARN)
- [ ] Users can enter a referral code on first wallet connection
- [ ] Referral rewards recorded in `transaction` table with `type='referral_reward'`
- [ ] `transaction` CHECK constraint updated to include `'referral_reward'`
- [ ] All validation checks work
- [ ] Referral claimed once per user (`referral_claimed_at`)

### Testimony System
- [ ] Referred users who complete Global Disciples can fill out testimony form
- [ ] Testimony form includes checkboxes, relationship select, and comments
- [ ] Testimony is signed with wallet
- [ ] Leader accumulates points based on testimonies
- [ ] Leader score is calculated correctly
- [ ] Leaders can view their testimonies and score
- [ ] All testimony events logged in `userevent`
- [ ] `course_id` included in testimonies (for future multi-course support)

### LearnTGVaultsV5 Integration
- [ ] Backend calculates 10% of scholarship for referrer
- [ ] `payScholarship` called with `referrer`, `referrerUSDT`, `referrerSlearn` parameters
- [ ] Works with all courses (free, missional, premium)
- [ ] No self-referral prevention enforced

---

## 14. Out of Scope

- Automatic email notifications for referral status changes
- Referral leaderboard
- Referral competitions
- WhatsApp notifications
- Multi-level referrals (only direct referrals)

---

> *"Freely you have received; freely give."* (Matthew 10:8)

---

**Created:** 2026-06-29
**Updated:** 2026-08-06 — Divided into 3 phases. Nothing implemented (blocked by #161, #196, #155).
**Status:** Blocked — depends on V5 contract + premium course payment flow.