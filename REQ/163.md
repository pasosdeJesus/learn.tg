# R-#163: Referral Program for learn.tg

Implement a general referral program for the learn.tg platform with **three reward forms**:

1. **Form 1 — course purchases:** the referrer earns 10% of the course price when a referred user buys **any premium course** (including Global Disciples).
2. **Form 2 — missional scholarships:** the referrer earns 10% of the scholarship value when a referred user completes a crossword in a **missional course** (only "Una relación con Jesús"); the student keeps 100% of their scholarship.
3. **Form 3 — pastor bonus:** the referrer earns an additional **1 USDT** when a referred **pastor** purchases the **Global Disciples course**.

Provides a dedicated referral menu with: code, balance, history, and referral status.

> **Separado (2026-08-29):** el **testimony system** se movió a **REQ/219**
> (privacidad/seudonimato, moderación, consentimiento) — no bloquea las
> recompensas de referidos. Referencia mutua: REQ/219 §8 fase 4 depende del
> flujo de 163 (notificación "Testify for your leader").

> **Tests TDD (2026-08-29):** la lógica pura de recompensas vive en
> `apps/nextjs/lib/referral-rewards.ts` con tests en
> `apps/nextjs/lib/__tests__/referral-rewards.test.ts` (11 tests: Form 1-3 +
> funding rule `canPayFromWallet`). El backend de atribución (DB/on-chain) debe
> construirse sobre esta lógica manteniendo los tests verdes.

**Funding rule:** all rewards are paid **from the referral wallet** (`NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS`), never from scholarships or student funds. If the wallet has no funds, the reward is skipped.

**Architecture decision (REQ/35):** The referral program is part of the **core** of learn.tg, not a separate engine. It is a cross-cutting feature that affects all courses and users, has no contracts of its own, and does not reduce build time. It will only be extracted if a second consumer (e.g., sivel3) requires it.

---

## Dependencies

| REQ | Description | Status |
|-----|-------------|--------|
| Existing course system | `cor1440_gen_proyectofinanciero` | ✅ |
| Existing USDT and SLEARN contracts | Deployed on Celo | ✅ |
| #161 | Global Disciples Course — activates referrals on premium purchase | ⏳ DB ✅, content ⏳ |
| #155 | Cluster/Country Funds — referral fund source (optional) | ⏳ Phase 1 ✅, rest ⏳ |
| #128 | Premium course payments — referral percentage (10%) | ✅ |
| `transaction` table | Referral rewards are value movements, must be recorded | ✅ |
| #35 | Web3 engines — referral program is **core**, not engine | ✅ Decided |

**Note:** Referral rewards are paid off-chain from the referral wallet (`PRIVATE_KEY_REFERRAL_WALLET`), not calculated by the contract. The `SLEARN.processPayment` only routes the 10% (from pdJ) into the referral wallet; the backend then attributes and pays each referrer.

---

## 1. Referral Program Overview

### 1.1 Activation
The referral program is activated when a user **purchases any premium course** **and has a profile score > 90** (verificado en `GET /api/referral/code` → `activated`). Premium courses include:
- Global Disciples Course (id 10 EN, 11 ES)
- Any other premium course added in the future (`porPagar > 0`)

| Requisito | Verificación |
|-----------|--------------|
| Compra de un curso premium | fila en `premium_course_usuario` |
| Puntaje de perfil > 90 | `usuario.profilescore > 90` |

### 1.2 Referral Reward Source

**All rewards are paid from the referral wallet, never from the student's scholarship.**

| Aspect | Decision |
|--------|----------|
| **Source** | Referral wallet (`NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS`), funded by 10% of premium purchases (from pdJ reduction 55% → 50%) |
| **Form 1** | 10% of the course price on premium purchase, paid 50% USDT + 50% SLEARN |
| **Form 2** | 10% of the scholarship value on missional crossword; student keeps 100% |
| **Form 3** | 1 USDT bonus when referred pastor buys GD course (paid in USDT) |
| **No funds** | If the referral wallet is empty, the reward is skipped (logged, not silently swallowed) |
| **Atomicity** | Off-chain attribution: read `ReferralReward` event, credit referrer, pay from referral wallet |

### 1.3 Reward Formula

```
If the referred user earns scholarship USDT = 1.00 and SLEARN = 5 (missional course):

Referrer receives (paid from the referral wallet):
  USDT: 0.10 (10% of 1.00)
  SLEARN: 0.50 (10% of 5)

Student receives (100% of their scholarship — no deduction):
  USDT: 1.00
  SLEARN: 5.00

If the referred user is a pastor purchasing the GD course:
  Additional bonus: 1.00 USDT
```

### 1.4 Example by Course

| Course | Scholarship per guide | Referrer (10% + bonus) | Student (100%) |
|--------|-----------------------|------------------------|----------------|
| **Web3 & UBI** | 1 USDT + 1 SLEARN | 0.10 USDT + 0.10 SLEARN | 1.00 USDT + 1.00 SLEARN |
| **Global Disciples (missional)** | 1 USDT + 5 SLEARN | 0.10 USDT + 0.50 SLEARN + **1 USDT** (if referred is pastor) | 1.00 USDT + 5.00 SLEARN |
| **Other missional course** | 1 USDT + 1 SLEARN | 0.10 USDT + 0.10 SLEARN | 1.00 USDT + 1.00 SLEARN |

### 1.5 Pastor Referral Bonus (GD Course Only)

When a referred user who is a **pastor** purchases the **Global Disciples course**, the referrer receives an additional **1 USDT** bonus on top of the standard 10% reward.

| Condition | Bonus |
|-----------|-------|
| Referred user is a pastor (`church_relationship = 'pastor'`) | +1 USDT |
| Referred user purchases the GD course (`course_id = 10` or `11`) | +1 USDT |
| Both conditions met | **+1 USDT** |

**Funding:** Paid from the referral wallet (`NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS`), same as the 10% reward. If the referral wallet has insufficient funds (< 1 USDT), the bonus is skipped (logged but not paid).

**Purpose:** Incentivize church members and other users to invite pastors to join the GD pilot. The 1 USDT bonus is a small but tangible reward that can be communicated clearly ("Invita a tu pastor y gana 1 USDT").

---

## 2. Referral Code and Tracking

### 2.1 Code Generation
- Each user receives a **unique referral code** upon purchasing a premium course
- The code is generated using `crypto.randomUUID()` and takes the first 8 characters
- The code is visible in the referral menu
- The code can be shared via link, QR code, or text
- Codes do not expire (`expires_at` can be NULL)

### 2.2 Referral Link Format
```
https://learn.tg/{lang}/ref/{CODE}        // idioma explícito (i18n)
https://learn.tg/ref/{CODE}               // legacy — redirige a /{lang}/ref/{CODE}
```

### 2.3 Tracking
When a new user connects their wallet for the first time:
- If they used a referral link (`/ref/{CODE}`), the code se guarda en `localStorage.pendingReferralCode` y se reclama automáticamente al conectar (`ConnectWalletButton`) — implementado
- If not, they can enter a referral code manually desde su **perfil** (sección Referidos) — implementado, **una sola vez**; tras reclamarlo solo se muestra quién lo refirió (solo lectura)
- The referral relationship is stored in the database (`referralrelationship`)
- Each user can only claim a referral code once (`referral_claimed_at` tracks this)
- **Landing `/ref` con i18n (2026-08-31):** `/en/ref/{CODE}` y `/es/ref/{CODE}` en su idioma; valida el código vía `/api/referral/lookup` (público): si no existe → "El código de referido no existe" (+ enlace a perfil); si existe → **"Fuiste invitado por {nusuario}"**; CTA según estado: **Connect Wallet** (billetera presente) o invitación a instalar billetera con enlace al curso Web3 & UBI (Guía 2). `/ref/{CODE}` legacy redirige según el idioma del navegador.
- **Validación de antigüedad (2026-08-31):** un usuario **más antiguo no puede ser referido por uno más nuevo** — el claim compara `usuario.fechacreacion` del referidor vs del referido y rechaza si el referidor se creó después (`400 "Referrer account is newer than yours"`).

---

## 3. Testimony System → **REQ/219** (separado 2026-08-29)

El sistema de testimonios (formulario, flujo, tabla `testimonies`, score del
Program Director, API `/api/testimony`) se movió a **REQ/219** con requisitos
**privacy-first** (seudonimato, consentimiento, verificación sin exposición en
países con persecución). Aquí solo queda la referencia:

- **REQ/219** contiene: formulario (checkboxes + `public`), flujo
  wallet-signed, migración `testimonies`, `calculateLeaderScore`, API y fases.
- Este REQ (163) usa el testimonio como **gatillo de posventa**: al completar
  el curso GD, el referido recibe la notificación "Testify for your leader"
  (REQ/219 §8 fase 4).

---

## 4. Reward Payment (off-chain)

> **Superseded:** the earlier design routed referral rewards through
> `LearnTGVaultsV5` (`referrer`, `referrerUSDT`, `referrerSlearn`). The new
> design pays off-chain from the referral wallet — no contract change needed.

The `SLEARN.processPayment` sends `percentageReferral=10` to the single
`referralWallet` (50% USDT + 50% SLEARN minted via `mintAndReserve`). The backend
then:
1. Reads the `ReferralReward(referralWallet, usdtAmount, slearnAmount)` event.
2. Attributes the reward to the referrer of the paying user (from the DB).
3. Transfers the USDT + SLEARN from the referral wallet to the referrer using
   `PRIVATE_KEY_REFERRAL_WALLET`.

The contract does not calculate the per-referrer split; the backend handles all
attribution and payouts.

### Pastor Bonus (GD Course Only)

After the standard 10% reward is paid, the backend checks:

1. Is the referred user a pastor? (`church_relationship = 'pastor'` on the referred user's church)
2. Did they purchase the GD course? (`course_id = 10` or `11`)

If both conditions are met, the backend transfers an additional **1 USDT** from the referral wallet to the referrer.

```typescript
// lib/referral/pastor-bonus.ts
async function awardPastorReferralBonus(referrerId: number, referredId: number, courseId: number) {
  // 1. Check if referred is a pastor
  const referred = await db.selectFrom('usuario')
    .innerJoin('church', 'church.id', 'usuario.church_id')
    .where('usuario.id', '=', referredId)
    .select(['church.church_relationship'])
    .executeTakeFirst()
  
  if (referred?.church_relationship !== 'pastor') return

  // 2. Check if course is GD (id 10 or 11)
  if (![10, 11].includes(courseId)) return

  // 3. Check referral wallet has ≥ 1 USDT
  const balance = await getReferralWalletBalance()
  if (balance.usdt < 1) {
    logger.warn('Pastor referral bonus skipped: insufficient USDT in referral wallet')
    return
  }

  // 4. Transfer 1 USDT
  await transferFromReferralWallet(referrerId, 1, 'USDT')
  await recordReferralBonus(referrerId, referredId, courseId, 1, 'USDT', 'pastor_bonus')
}
```

**Idempotency:** The bonus is recorded in the `transaction` table with `type='referral_bonus'` and `subcategoria='pastor_bonus'` to prevent double payments.

**Transaction entry:**
```sql
INSERT INTO transaction (
    usuario_id, wallet, crypto, type, amount, balance_impact, date, hash,
    categoria, subcategoria, metadata
) VALUES (
    :referrer_id, :wallet, 'USDT', 'referral_bonus', 1.00, 1.00, NOW(), :hash,
    'referral', 'pastor_bonus',
    '{"referred_id": 456, "course_id": 10}'
);
```

---

## 5. Referral Menu

### 5.1 Menu Access
- **Desktop:** Visible in the main navigation (expanded)
- **Mobile:** Visible in the dropdown menu
- **Visibility:** Only visible after the user has activated the referral program (purchased a premium course)

### 5.2 Menu Sections

#### 5.2.1 Referral Code
```
## Mi referidor (tarjeta 1)
Nombre / Usuario / Billetera  del usuario que te invitó
(si no hay referidor: "ingresa el código desde tu perfil")

## Mis referidos (tarjeta 2)
`https://learn.tg/{lang}/ref/CODE`

[Copiar enlace]  ·  Referidos (total)  ·  Recompensas (USDT · SLEARN)
Listado de usuarios referidos (nombre + estado) · Historial de recompensas

## Promociona learn.tg (CTA múltiple, solo con código activo)
[Invitar pastores → /es/redgd/pastores]  [Invitar por WhatsApp (invitación personal con enlace)]
```

#### 5.2.2 Referral History
```
## Historial de referidos

| Fecha | Referido | Curso completado | Recompensa | Estado |
|-------|----------|------------------|------------|--------|
| 2026-08-28 | Pastor Juan Pérez | Global Disciples | 0.10 USDT + 0.50 SLEARN + 1.00 USDT | ✅ Pagado |
| 2026-08-27 | María Gómez | Web3 & UBI | 0.10 USDT + 0.10 SLEARN | ✅ Pagado |
```

#### 5.2.3 Referral Status
```
## Estado de mis referidos

| Referido | Código | Curso completado | Estado |
|----------|--------|------------------|--------|
| Pastor Juan Pérez | REF-001 | Global Disciples | ✅ Completado |
| María Gómez | REF-002 | Web3 & UBI | ✅ Completado |
| Pedro López | REF-003 | — | ⏳ Pendiente |
```

#### 5.2.4 Testimony Requests (for GD leaders) → REQ/219
```
## Atestiguamientos pendientes

| Referido | Curso completado | Acción |
|----------|------------------|--------|
| Pastor Juan Pérez | Global Disciples | [Solicitar atestiguamiento] |
| Carlos Ruiz | Global Disciples | [Solicitar atestiguamiento] |
```

---

## 6. Database Schema

### 6.1 Referral Codes

```sql
CREATE TABLE referralcode (
    id SERIAL PRIMARY KEY,
    usuario_id INTEGER REFERENCES usuario(id) NOT NULL,
    code VARCHAR(20) UNIQUE NOT NULL,
    activated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP,                     -- NULL = never expires
    active BOOLEAN DEFAULT TRUE
);
```

### 6.2 Referral Relationships

```sql
CREATE TABLE referralrelationship (
    id SERIAL PRIMARY KEY,
    referrer_id INTEGER REFERENCES usuario(id) NOT NULL,
    referred_id INTEGER REFERENCES usuario(id) NOT NULL,
    referral_code VARCHAR(20),                -- The code used
    referral_claimed_at TIMESTAMP,            -- When the code was claimed
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(20) DEFAULT 'pending'     -- 'pending', 'completed', 'paid'
);
```

### 6.3 Testimonies → REQ/219

La tabla `testimonies` (con `public` para consentimiento privacy-first) se movió
**REQ/219 §5**; este REQ no define tablas de testimonios.
    comments TEXT,
    signature_wallet VARCHAR(42) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(leader_id, witness_id)
);
```

### 6.4 Transaction Table

Every referral reward payment creates entries in the `transaction` table:

```sql
-- One entry per crypto per recipient per reward
INSERT INTO transaction (
    usuario_id, wallet, crypto, type, amount, balance_impact, date, hash,
    categoria, subcategoria, metadata
) VALUES (
    :referrer_id, :wallet, 'USDT', 'referral_reward', 0.10, 0.10, NOW(), :hash,
    'referral', 'referrer',
    '{"referred_id": 456, "course_id": 1, "guide_id": 2}'
);

-- Pastor bonus (additional 1 USDT)
INSERT INTO transaction (
    usuario_id, wallet, crypto, type, amount, balance_impact, date, hash,
    categoria, subcategoria, metadata
) VALUES (
    :referrer_id, :wallet, 'USDT', 'referral_bonus', 1.00, 1.00, NOW(), :hash,
    'referral', 'pastor_bonus',
    '{"referred_id": 456, "course_id": 10}'
);
```

**CHECK constraint update:** Add `'referral_reward'` and `'referral_bonus'` to the `transaction` type CHECK constraint.

```sql
ALTER TABLE transaction DROP CONSTRAINT IF EXISTS transaction_type_check;
ALTER TABLE transaction ADD CONSTRAINT transaction_type_check CHECK (
    type IN ('scholarship', 'donation', 'donation_reward', 'pay-course', 
             'ubi-claim', 'conversion', 'referral_reward', 'referral_bonus')
);
```

---

## 7. API Endpoints

| Endpoint | Method | Auth | Description |
|----------|--------|------|-------------|
| `/api/referral/code` | GET | Yes | Get user's referral code |
| `/api/referral/stats` | GET | Yes | Get referral statistics |
| `/api/referral/history` | GET | Yes | Get referral history |
| `/api/referral/claim` | POST | Yes | Enter referral code (first wallet connection) |
| `/api/referral/share` | POST | Yes | Generate shareable link |
| `/api/referral/lookup` | GET | No (public) | Resolve a code for the public landing: `{ valid, nusuario, nombre }` (no wallet exposure) |
| `/api/referrals/fund` | GET | No (public) | Get referral wallet balance (USDT + SLEARN) — ✅ already done |
| `/api/testimony*` | — | — | **→ REQ/219** (sistema de testimonios separado) |

---

## 8. Events

All referral events are logged in `userevent`:

| Event | When | Data |
|-------|------|------|
| `referral_activated` | User purchases premium course | `{ user_id, code }` — ✅ emitido en `app/api/courses/premium/purchase` (una sola vez por usuario, 2026-09-01) |
| `referral_claimed` | New user enters referral code | `{ referrer_id, referred_id, code }` |
| `referral_reward_paid` | Referrer receives 10% reward | `{ referrer_id, referred_id, course_id, guide_id, usdt_amount, slearn_amount }` |
| `referral_bonus_paid` | Referrer receives 1 USDT pastor bonus | `{ referrer_id, referred_id, course_id, usdt_amount }` |
| `testimony_submitted` | User submits testimony | `{ leader_id, witness_id }` — **→ REQ/219** |

---

## 9. Security

### 9.1 Validation Checks

| Check | Description |
|-------|-------------|
| **Code uniqueness** | Each referral code must be unique |
| **Valid course** | The completed course must be valid |
| **One time per relationship** | Each referral relationship pays only once per course |
| **No self-referral** | A user cannot refer themselves (enforced by backend) |
| **Referral claimed once** | A user can only claim one referral code |
| **Referrer older than referred** | Un usuario más antiguo no puede ser referido por uno más nuevo: el claim compara `fechacreacion` y rechaza si el referidor se creó después (`400 "Referrer account is newer than yours"`) |
| **Pastor check** | Pastor bonus only applies if referred user's `church_relationship = 'pastor'` |
| **GD course check** | Pastor bonus only applies to GD course (id 10 or 11) |

### 9.2 Anti-Fraud Measures

| Measure | Description |
|---------|-------------|
| **Unique wallets** | A user cannot refer themselves |
| **Course completion** | Rewards only paid after course completion |
| **Manual review** | Suspicious activity flagged for review |
| **Rate limiting** | Máx. `REFERRAL_CLAIM_DAILY_LIMIT` claims/día por IP (default 5; 0 desactiva) — ✅ 2026-09-01: el evento `referral_claimed` guarda la IP (`metadata.ip`) y el claim cuenta la ventana de 24h → 429 |
| **Idempotency** | No double payments (DB constraints + transaction checks) |

---

## 10. Implementation Phases

### Phase 0 — Landing page + fund visibility (done)

| # | Task | Status |
|---|---|---|
| 0.1 | `GET /api/referrals/fund` — referral wallet balance (USDT + SLEARN) | ✅ |
| 0.2 | Landing page `/referrals` (EN/ES) explaining the program + fund balance, linked from header | ✅ |
| 0.3 | Env vars: `NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS` + `PRIVATE_KEY_REFERRAL_WALLET` | ✅ |
| 0.4 | CTA is state-dependent: no wallet → Web3 & UBI course link; score < 90 → profile + verification; score > 90 → referral link/code | ✅ |
| 0.5 | CTA button label visible without hover (contrast fix) | ✅ `bg-blue-600 text-white` |

**Delivers:** The program is explained and the referral wallet balance is visible.

### Phase 1 — DB + Core Logic

**Goal:** Tables and API for referral codes and relationships. No rewards yet.

| # | Task | Depends on |
|---|---|---|
| 1.1 | Migration: `referralcode` + `referralrelationship` tables | ✅ `20260829120000_referral_program` |
| 1.2 | `/api/referral/code` — generate & return user's referral code | ✅ (crea al primer uso) |
| 1.3 | `/api/referral/claim` — new user enters referral code on first wallet connect | ✅ |
| 1.4 | `/api/referral/stats` — referral statistics (count, pending, completed) | ✅ |
| 1.5 | `/api/referral/history` — referral history for the referrer | ✅ |
| 1.6 | `/api/referral/share` — generate shareable link + QR | ✅ enlace compartible (`https://learn.tg/{lang}/ref/{CODE}`) + **QR** (2026-09-01: `qrcode.react` + componente core `components/ReferralQr.tsx` inyectado en la página de referidos vía deps D2) |
| 1.7 | Referral menu UI (code, share, history, status) | ✅ (sección "Mi programa de referidos" en /referrals: código, copiar enlace, stats, historial) |

**Delivers:** Users can get referral codes, share them, and track referrals. No payments.

### Phase 2 — Rewards (off-chain from referral wallet)

**Goal:** Automatic referral rewards paid from the referral wallet.

| # | Task | Depends on |
|---|---|---|
| 2.1 | Add `'referral_reward'` and `'referral_bonus'` to `transaction` type CHECK constraint | ✅ (en `20260829120000_referral_program`) |
| 2.2 | Form 1: raise `PREMIUM_PCT.referral` 5% → 10% (and `pdJ` 55% → 50%) | ✅ `packages/rewards/src/routes/premium-purchase.ts` |
| 2.3 | Form 1: after purchase, read `ReferralReward` event and credit the referrer | ✅ (core `app/api/courses/premium/purchase/route.ts` usa el share 10% de la distribución) |
| 2.4 | Form 2: hook the missional crossword scholarship flow and credit the referrer (student keeps 100%) | ✅ (2026-09-01: el motor `check-crossword` expone `scholarshipUsdt`/`scholarshipSlearn`; el core `app/api/check-crossword/route.ts` → `lib/referral-crossword.ts` atribuye el 10% solo para cursos misionales 1 y 2) |
| 2.5 | Payout: transfer USDT + SLEARN from referral wallet (signed with `PRIVATE_KEY_REFERRAL_WALLET`); skip if no funds | ✅ `lib/referral-payout.ts` |
| 2.6 | Idempotency: no double payout (DB constraint); log `referral_reward` in `transaction` | ✅ (verifica `transaction` antes de pagar) |
| 2.7 | Update referral history UI to show rewards paid | ✅ (historial en la UI) |
| 2.8 | Pastor bonus: detect if referred user is a pastor (`church_relationship = 'pastor'`) | ✅ |
| 2.9 | Pastor bonus: transfer 1 USDT from referral wallet (skip if insufficient funds) | ✅ |
| 2.10 | Pastor bonus: record in `transaction` table with `type='referral_bonus'`, `subcategoria='pastor_bonus'` | ✅ |
| 2.11 | Update referral history UI to show pastor bonus (1 USDT line item) | ✅ (historial UI muestra línea de pastor bonus) |

**Delivers:** Referrers earn 10% automatically on referred purchases and missional scholarships, plus 1 USDT pastor bonus for GD course referrals, all paid from the referral wallet.

### Phase 3 — Testimony System → REQ/219

**Movido a REQ/219** (fases 1-4): migración `testimonies`, `/api/testimony`,
`calculateLeaderScore`, form UI, notificación y dashboard del líder — con
privacy-first (seudonimato/consentimiento). Este REQ solo aporta el gatillo de
posventa (notificación "Testify for your leader" al completar GD).

---

## 11. What's Already Done

- `GET /api/referrals/fund` — returns the referral wallet balance (USDT + SLEARN)
- Landing page `/referrals` (EN/ES) explaining the two forms + fund balance, linked from the header menu
- Env vars: `NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS` (existing) and `PRIVATE_KEY_REFERRAL_WALLET` (added)
- The `SLEARN` contract already routes `percentageReferral` to the single `referralWallet` (50% USDT + 50% SLEARN minted)

Pending: **E2E de pago real Form 1-3** (☐, requiere billetera de referidos con fondos + redeploy del sitio dev). Testimonios → REQ/219.

---

## 12. Acceptance Criteria

### Referral Program
- [x] Landing page `/referrals` (EN/ES) explains the program + shows referral wallet balance
- [x] `GET /api/referrals/fund` returns referral wallet balance (USDT + SLEARN)
- [x] Referrals CTA is state-dependent: no wallet → Web3 & UBI course link (same lang); score < 90 → profile + verification interview; score > 90 → referral link/code + explanation (E2E verifica rama score ≤ 90; rama > 90 no cubierta por E2E)
- [x] Referrals CTA button label is visible without hover (`bg-blue-600 text-white`)
- [x] Referrer → referred relationship tracked (/ref/{CODE} auto-claim o ingreso manual en perfil; `referralrelationship`)
- [x] Unique referral code is generated per user (UUID first 8 chars en mayúsculas, creado al primer uso)
- [x] Referral menu is visible (sección "Mi programa de referidos" en /referrals: código, copiar, stats, historial, listado de referidos)
- [x] Form 1: referrer earns 10% of the course price on a referred premium purchase (50% USDT + 50% SLEARN) — implementado (`lib/referral-payout.ts` + wrapper `app/api/courses/premium/purchase/route.ts`); lógica pura unit-testeada. **Fix 2026-09-01**: el wrapper leía `church.church_relationship` (columna inexistente → SQL error → nunca pagaba); ahora usa `usuario.church_relationship`. **E2E de pago real ☐** (spec `referral-premium.spec.mjs` listo; verificación final pendiente de redeploy del sitio dev)
- [x] Form 2: referrer earns 10% of the scholarship value on a referred missional crossword (student keeps 100%) — implementado (2026-09-01: motor `check-crossword` expone montos pagados + core `lib/referral-crossword.ts`, cursos misionales **1 y 2**); unit-testeado (`referral-crossword.test.ts` 7 tests + `referral-rewards.test.ts` Form 2). **E2E de desembolso**: spec `referral-payout.spec.mjs` escrito y ejecutado contra dev (6/6 pasos verdes + 1 skip diagnóstico); verificación final del desembolso **pendiente de redeploy** del sitio dev (☐)
- [x] Rewards paid from the referral wallet (`NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS` / `PRIVATE_KEY_REFERRAL_WALLET`); skip if no funds (`canPayFromWallet` unit-testeado)
- [x] Users can enter a referral code (perfil: sección Referidos; una sola vez, luego solo lectura)
- [x] Referral rewards recorded in `transaction` table with `type='referral_reward'`
- [x] `transaction` CHECK constraint updated to include `'referral_reward'` and `'referral_bonus'` (en `20260829120000_referral_program`)
- [x] All validation checks work (código inválido → 400, no auto-referencia, claim una vez; E2E cubre claim/once/solo-lectura)
- [x] **Referrer older than referred:** el claim rechaza si el referidor se creó después que el referido (2026-08-31)
- [x] **Landing `/ref` i18n (2026-08-31):** `/en/ref/{CODE}` y `/es/ref/{CODE}` en su idioma; código inválido → "El código de referido no existe"; código válido → "Fuiste invitado por {nusuario}"; CTA Connect Wallet / invitación a instalar billetera (curso Web3 & UBI); `/ref/{CODE}` legacy redirige por idioma
- [x] **Menú de referidos en dos tarjetas (2026-08-31):** "Mi referidor" (nombre, usuario, billetera del invitador) + "Mis referidos" (enlace para compartir, totales, listado de referidos, historial)
- [x] **CTA múltiple de promoción (2026-08-31):** con código activo → "Invitar pastores" (`/{lang}/redgd/pastores`) + "Invitar por WhatsApp" (mensaje personal con enlace de referido)
- [x] **Enlaces de compartir con idioma (2026-08-31):** `https://learn.tg/{lang}/ref/{CODE}`
- [x] Referral claimed once per user (`referral_claimed_at`)
- [x] No self-referral

### Pastor Bonus (GD Course Only)
- [x] Referrer receives 1 USDT bonus when a referred pastor purchases the GD course — implementado (`lib/referral-payout.ts` Form 3 + wrapper `premium/purchase`, check `isPastor` desde `usuario.church_relationship` tras fix 2026-09-01); unit-testeado. **E2E de pago real ☐** (spec `referral-premium.spec.mjs` listo; pendiente de redeploy)
- [x] Bonus is paid from the referral wallet (`NEXT_PUBLIC_REFERRAL_WALLET_ADDRESS`)
- [x] Bonus is skipped if the referral wallet has < 1 USDT (logged, not silently swallowed)
- [x] Bonus is recorded in `transaction` table with `type='referral_bonus'`, `subcategoria='pastor_bonus'`
- [x] Bonus is idempotent (no double payments for the same referral relationship)
- [x] Only applies to GD course (id 10 or 11)
- [x] Only applies if the referred user is the **pastor principal** (`usuario.church_relationship = 'pastor'`, verificado en DB; `'co_pastor'` NO genera el bono — decisión 2026-09-01)
- [x] Referral history UI shows pastor bonus as a separate line item

### Testimony System → REQ/219

Criterios de testimonios movidos a REQ/219 (formulario, score, dashboard, privacy-first).

### Reward Payment (off-chain)
- [x] Form 1: raise `PREMIUM_PCT.referral` 5% → 10% (and `pdJ` 55% → 50%) — `packages/rewards/src/routes/premium-purchase.ts`
- [x] Attribution after purchase: el core (`premium/purchase`) paga el 10% desde la billetera de referidos al referidor; unit-testeado
- [x] Transfer USDT + SLEARN from the referral wallet to the referrer (idempotent, no double payout)
- [x] Works with all course types (free, missional, premium)
- [x] Pastor bonus: transfer 1 USDT from referral wallet (idempotent, no double payout)

---

## 14. Plan E2E — Referidos (2026-08-29)

**Spec:** `apps/nextjs/e2e/specs/referral-program.spec.mjs` (browser, Puppeteer) — **implementado y ejecutado**.
**Prerrequisitos del sitio dev:**
- 2 billeteras: **referidor** (la de `apps/.env`) y **referido** (2ª en `TEST_PRIVATE_KEYS`; debe estar registrada en el sitio dev para SIWE — ver `doc/e2e-testing.md`).
- ⚠️ El wallet de `apps/.env` tiene **profile score 75** en dev — con el nuevo requisito **> 90** no activa el programa. El fixture debe subirlo a **> 90** antes (prerequisitos + GoodDollar +7 + entrevista +7 ≈ 91) o usar otra billetera con score > 90.

**Pasos propuestos (ok/fail por paso):**

| # | Escenario | Verificación |
|---|-----------|--------------|
| 1 | **Landing pública** `/en/referrals` sin sesión | Documentación visible (cómo funciona, Form 1-3, billetera de referidos con saldo) y CTA "Go to the Web3 & UBI course" |
| 2 | **CTA por estado** (con wallet, score ≤ 90) | CTA cambia a "Complete your profile" (`/{lang}/profile`) |
| 3 | **Requisitos checklist** (sin activar) | Tarjeta ámbar con ✖ en "Buy a premium course" y/o "> 90 profile points" según el fixture |
| 4 | **Código de referido** (activado: premium ∧ score > 90) | Código visible + botón copiar; `GET /api/referral/code` → `{ code, activated: true, purchasedPremium: true, profileScore > 90 }` |
| 5 | **Auto-claim por enlace** `/ref/{CODE}` (billetera referida) | Se guarda `pendingReferralCode`; al conectar → `POST /api/referral/claim` 200; `referralrelationship` creado |
| 6 | **Una sola vez → solo lectura** | 2º claim → 400 "Referral already claimed"; perfil muestra "Te refirió: {nombre}" sin input |
| 7 | **Listado de referidos** (referidor) | `/referrals` muestra la persona referida (estado) y stats total ≥ 1 |
| 8 | **Regresión** | `/api/referrals/fund` responde saldos USDT/SLEARN |

**Notas:** apoyarse en `setupE2EAuth` (`e2e/helpers/e2e-auth.mjs`) y el patrón de `pastor-journey.spec.mjs`; click real por coordenadas (evitar `element.click()` sintético, como en `gas-insufficient-panel`). Ejecutar con `IPDES=learn.tg PUERTOPRU=9001 CHAIN_ID=11142220`.

**Criterio de aceptación del spec:** 8/8 pasos verdes (o skip explícito cuando el fixture no aplique, p.ej. CTA si el referido ya tiene score > 90).

**Ejecución (2026-08-29):** primera corrida contra el sitio dev (`IPDES=learn.tg PUERTOPRU=9001 CHAIN_ID=11142220 CHROME_PATH=/usr/local/bin/chromium bin/m test:e2e referral-program`) → **6 failures**, todos por el mismo bug de normalización de mayúsculas: los códigos se creaban en minúsculas (`randomUUID().slice(0,8)`) mientras `pendingReferralCode` y el claim los buscaban en mayúsculas (`5F75CC53` guardado vs `5f75cc53` creado → "Invalid referral code").

**Fix aplicado (core, no motor):**
- `app/api/referral/code/route.ts` y `app/api/referral/share/route.ts`: código generado con `.toUpperCase()` (8 chars, sin guiones).
- `app/api/referral/claim/route.ts`: lookup case-insensitive `where('code', 'ilike', code.toUpperCase())`.
- `make type` verde. **Pendiente:** redeploy del sitio dev para que tome el fix + re-ejecución del spec hasta 8/8.

**Resultado final (2026-08-29):** tras el redeploy y 2 ajustes al spec (polling de hidratación del CTA/checklist con el último link `bg-blue-600`, y comparación case-insensitive de `pendingReferralCode` — el contrato de claim es `ilike` y hay códigos legacy en minúsculas en la DB dev) → **8/8 pasos verdes, 0 failures**:

```
✅ Landing pública (documentación + fund)     ✅ CTA según estado (score ≤ 90)
✅ Checklist de requisitos                    ✅ Código de referido (activated=false)
✅ /ref/{CODE} guardó pendingReferralCode     ✅ Claim 200 (referrer_id=191)
✅ 2º claim → 400 "Referral already claimed"  ✅ Solo lectura "Te refirió: DS Dev"
✅ Historial del referidor                    ✅ /api/referrals/fund
```

Nota: la DB dev conserva códigos legacy en minúsculas (`5f75cc53`); si se quiere uniformar, correr `UPDATE referralcode SET code = UPPER(code)` en el entorno real.

---

## 13. Out of Scope

- Automatic email notifications for referral status changes
- Referral leaderboard
- Referral competitions
- WhatsApp notifications
- Multi-level referrals (only direct referrals)
- Testimony system for non-GD courses → REQ/219 (futuro)

---

> *"Freely you have received; freely give."* (Matthew 10:8)

---

## Course Card Display

The student's scholarship card shows the **full amount** (`amountPerGuide`) — the referral reward is paid from the referral wallet, NOT deducted from the student's scholarship. No card adjustment is needed for the referral program.

---

**Created:** 2026-06-29
**Updated:** 2026-08-06 — Divided into 3 phases. Nothing implemented (blocked by #161, #196, #155).
**Updated:** 2026-08-14 — Redesigned: two reward forms (purchases + missional scholarships) paid from the referral wallet (never from scholarships). Added Phase 0 (landing page + fund endpoint + env vars). Rewards now off-chain (supersedes V5).
**Updated:** 2026-08-28 — Added **Form 3: pastor bonus (1 USDT)** for referring a pastor to the GD course. Added pastor bonus to transaction types, reward formula, examples, phases, and acceptance criteria. Clarified architecture decision (core vs engine per REQ/35).
**Status:** Phase 0 ✅ (landing + fund + CTA por estado + contraste). Phase 1 ✅ (migración `20260829120000_referral_program` + APIs `code`/`claim`/`stats`/`history`/`share`). Phase 2 ✅ (PREMIUM_PCT referral 10%/pdJ 50, pagos off-chain Form 1+3 en purchase, Form 2 en crossword missional cableado 2026-09-01: motor expone montos + core `lib/referral-crossword.ts` cursos 1/2, idempotente, funding rule). UI ✅ (`/referrals` adaptativa: documentación + requisitos con checklist (compra premium ∧ score > 90) + pasos + código/copiar + stats + listado de referidos + quién me refirió; sección Referidos en el perfil con ingreso de código **una sola vez** → solo lectura; `/ref/{CODE}` con auto-claim al conectar). **Testimony system separado → REQ/219** (2026-08-29). E2E `referral-program.spec.mjs` escrito y ejecutado; detectó bug de mayúsculas en códigos (creación en minúsculas vs claim en mayúsculas), corregido (generación `.toUpperCase()` + lookup `ilike`), `make type` verde, y **spec 8/8 verdes tras redeploy** (2026-08-29). E2E de desembolso `referral-payout.spec.mjs` (2026-09-01): 6/6 pasos + skip diagnóstico, pendiente redeploy del sitio dev para 7/7.

**Mejoras UX/validación (2026-08-31):**
- Landing `/ref` i18n: `/en/ref/{CODE}` + `/es/ref/{CODE}` (idioma del navegador en legacy `/ref/{CODE}`), validación pública del código (`/api/referral/lookup`), "Fuiste invitado por {nusuario}", CTA **Connect Wallet** o invitación a instalar billetera (curso Web3 & UBI Guía 2), código inválido → "El código de referido no existe".
- Claim: validación de antigüedad (el referidor no puede ser más nuevo que el referido, `fechacreacion`).
- `/api/referral/code` ahora devuelve `referredByDetails { nombre, nusuario, billetera }`.
- Menú en dos tarjetas: **"Mi referidor"** (nombre/usuario/billetera del invitador) y **"Mis referidos"** (enlace para compartir, totales, listado, historial).
- CTA múltiple con código activo: **Invitar pastores** (`/{lang}/redgd/pastores`) + **Invitar por WhatsApp** (mensaje personal con enlace de referido).
- Enlaces de compartir con idioma: `https://learn.tg/{lang}/ref/{CODE}`.
- Código compila: `make type` ✓, gdcluster dist ✓, unit 11+33 ✓. **Pendiente:** redeploy del sitio dev + re-ejecución E2E (`referral-program`) y verificación manual de la landing `/ref`.

**Veredicto (2026-08-31):** **cerrable** — Fases 0-2 + UI + unit tests + E2E 8/8 (2026-08-29) + mejoras de validación/i18n/UX (2026-08-31, pendientes de redeploy del sitio dev para su verificación E2E final). Salvedades no bloqueantes: (1) pagos Form 1-3 verificados por unit tests de lógica pura, **sin E2E de pago real** (requiere compra premium real con referido + billetera de referidos con fondos); (2) rama CTA score > 90 no cubierta por E2E (el fixture tiene score 75); (3) REQ/161 (contenido GD) sigue ⏳ como dependencia no bloqueante. **Cerrados 2026-09-01:** (4) QR del código de referido (✅ `ReferralQr`), (5) rate-limiting del claim (✅ 5/día por IP, `REFERRAL_CLAIM_DAILY_LIMIT`), (6) evento `referral_activated` (✅ en compra premium).

**Auditoría de implementación (2026-09-01):** revisión código vs REQ (a petición del propietario, "confirma que un referido que completa un crucigrama genera ganancia"). Se detectó que la **Form 2 NO estaba cableada**: `awardReferralRewards` solo se llamaba desde `app/api/courses/premium/purchase/route.ts` (Form 1+3), el route `check-crossword` era un re-export delgado y el parámetro `referrer` on-chain seguía `TODO: #163`; el ✅ anterior en §2.4/§12 era falso. **Fix implementado y testeado:**
- Motor `@learn-tg/rewards`: `check-crossword` ahora devuelve `scholarshipUsdt`/`scholarshipSlearn` (montos reales pagados; 0 si no hubo pago) — `dist/` reconstruido, `tsc` ✓.
- Core: nuevo `lib/referral-crossword.ts` con `MISSIONAL_COURSE_IDS = [1, 2]` ("Una relación con Jesús" / "A relationship with Jesus", verificado en DB) + `awardMissionalScholarshipReferral` (solo paga si curso misional ∧ monto > 0); `app/api/check-crossword/route.ts` lo invoca tras una respuesta 200 con scholarship pagado, reusando la idempotencia (`transaction.metadata.referred_id/course_id/guide_id`) y la funding rule (`canPayFromWallet`) de `referral-payout.ts`.
- Tests: `lib/__tests__/referral-crossword.test.ts` (7 tests) + `referral-rewards.test.ts` (11) → 18/18 ✓; `make type` ✓; `make test-lib` 145 ✓.
- **E2E de desembolso (Form 2):** nuevo spec `apps/nextjs/e2e/smoke/referral-payout.spec.mjs` — referidor (wallet de `apps/.env`, verificador) + referido (billetera nueva, SIWE) → claim → perfil ≥ 50 (53) → crucigrama perfecto del curso 2 → `/api/check-crossword` → `/api/referral/history` con `referral_reward` del 10%. Ejecutado contra dev: **6/6 pasos verdes + 1 skip diagnóstico** — el servidor dev SÍ pagó la beca (tx `0x7eda49e0…` en la corrida 2026-09-01) pero su versión desplegada no devuelve `scholarshipUsdt/Slearn` ni atribuye Form 2 (falta el **redeploy** del sitio dev con el motor + wrapper nuevos). Tras el redeploy, el spec debe quedar **7/7**.
- Pendientes (☐): **E2E de pago real Form 1 y Form 3** (requiere compra premium real con referido + billetera de referidos con fondos). **Cerrados 2026-09-01:** QR del código de referido (✅ `ReferralQr`), evento `referral_activated` (✅ en compra premium), rate-limit del claim (✅ 5/día por IP, `REFERRAL_CLAIM_DAILY_LIMIT`).

**Auditoría 2 (2026-09-01, Form 1 + Form 3):** al revisar la compra premium con referido se encontró otro bug real: el wrapper `app/api/courses/premium/purchase/route.ts` leía `church.church_relationship` (columna **inexistente** — el rol pastoral vive en `usuario.church_relationship`, verificado en DB) → el SELECT lanzaba error SQL → el try/catch tragaba la excepción → **Form 1 y Form 3 nunca pagaban**. **Fix:** el wrapper ahora usa `user.church_relationship` (el `usuario` viene completo de `authenticateUser`); se eliminó el join a `church` y el import `sql`.
- **E2E de desembolso Form 1 + Form 3:** nuevo spec `apps/nextjs/e2e/smoke/referral-premium.spec.mjs` — referidor (verificador) + pastor REFERIDO (billetera nueva) → claim → perfil SL verificado (score 93) → iglesia (bonus 44 SLEARN real, tx `0xee7cb237…`) → compra del curso GD (39.60 SLEARN, distribución referral 3.96) → historial del referidor. Ejecutado contra dev (2026-09-01): **16/18 pasos verdes + 2 FAIL diagnósticos** — la compra y el bonus funcionan, pero Form 1/3 no se pagan porque el sitio dev corre el wrapper roto (mismo `church.church_relationship`). Tras el redeploy con el fix, el spec debe quedar **18/18**.