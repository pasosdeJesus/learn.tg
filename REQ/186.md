# R-#186: Replace RainbowKit with Own Wallet/Auth Implementation

Replace the RainbowKit → wagmi → SIWE tower with a minimal, controlled
implementation that uses NextAuth session as the single source of truth
for user identity.

## Motivation

RainbowKit's `RainbowKitSiweNextAuthProvider` couples wallet connection state
(wagmi) with authentication state (NextAuth). When wagmi's React context loses
state on client-side navigation (NextAuth bug #5719), RainbowKit interprets
this as "user needs to sign in again" and triggers the full SIWE flow with
connection popups. This produces a broken UX: "Connect Wallet" appearing
on every page, multiple "Accept connection" dialogs, and unnecessary
re-authentication.

The root cause is **not** wagmi or NextAuth individually — it's the coupling
RainbowKit creates between them. Both wagmi and NextAuth work fine in isolation.

## Design

### Current Stack (to replace)

```
RainbowKit ConnectButton
  └─ RainbowKitSiweNextAuthProvider
       ├─ wagmi (useAccount, useConnect)
       └─ NextAuth (useSession)
```

### Proposed Stack

```
Own Header component (session.address + custom Connect button)
  ├─ NextAuth (useSession)        ← identity, always available
  └─ window.ethereum (direct)     ← signing, only when needed
```

### Key Changes

#### 1. Replace `ConnectButton` with Own UI

```tsx
// Instead of RainbowKit's <ConnectButton />
// We render our own button based on NextAuth session:

{!sessionAddress ? (
  <button onClick={handleConnect}>Connect Wallet</button>
) : (
  <span>{sessionAddress.slice(0,6)}...{sessionAddress.slice(-4)}</span>
)}
```

The `handleConnect` calls `window.ethereum.request({ method: 'eth_requestAccounts' })` directly — no wagmi, no RainbowKit.

#### 2. SIWE on First Connect Only

On first connect (or when user explicitly clicks "Connect"), run SIWE:
```
eth_requestAccounts → get address → SIWE sign → POST /api/auth/callback/credentials
```
After that, NextAuth session JWT handles identity. No reconnect on navigation.

#### 3. Sign Transactions Without wagmi

For operations that need signing (donate, claim, profile save, crossword):
```ts
const tx = await window.ethereum.request({
  method: 'eth_sendTransaction',
  params: [{ to, value, data }],
})
```

#### 4. What We Keep

| Keep | Why |
|------|-----|
| NextAuth + SIWE backend | Works perfectly, session persists across navigation |
| `billetera_usuario.token` | API auth, already solid |
| `authenticateUser()` | Already decoupled from wagmi |
| viem utilities | Address formatting, ABI encoding (lightweight, no state) |

#### 5. What We Remove

| Remove | Replaced By |
|--------|-------------|
| `@rainbow-me/rainbowkit` | Own Connect button |
| `@rainbow-me/rainbowkit-siwe-next-auth` | Direct SIWE flow |
| `wagmi` (useAccount, useConnect) | `window.ethereum` + NextAuth |
| `@rainbow-me/rainbowkit/wallets` | Direct `eth_requestAccounts` |

### Benefits

| Benefit | Impact |
|---------|--------|
| **No reconnect popups** | wagmi state loss is irrelevant — we don't use wagmi for auth |
| **Single source of truth** | `session.address` for identity everywhere |
| **Smaller bundle** | Remove ~200KB (RainbowKit + wagmi dependencies) |
| **Control** | We own every line of auth/connect logic — bugs are ours to fix |
| **PWA-friendly** | No RainbowKit modal that breaks in standalone mode |
| **Prepares for R-#184** | Own connect flow is the foundation for the built-in wallet |

### Migration Path

1. **Phase 1:** Create own `<ConnectWalletButton />` that uses `window.ethereum`
2. **Phase 2:** Replace RainbowKit `ConnectButton` in Header
3. **Phase 3:** Replace wagmi `useAccount` in write components (donate, claim, profile save) with direct `window.ethereum` calls
4. **Phase 4:** Remove RainbowKit + wagmi packages
5. **Phase 5:** Write unit/E2E tests for the new flow

### Risk Mitigation

- **Mobile wallets (OKX, OneKey):** `window.ethereum` is the standard injected provider — all Web3 wallets inject it
- **WalletConnect:** Keep `@walletconnect/ethereum-provider` for QR-based connections (if needed)
- **SIWE compatibility:** Sign with `personal_sign` via `window.ethereum` — same as current flow

## Dependencies

- Keep: `next-auth`, `siwe`, `viem`
- Remove: `@rainbow-me/rainbowkit`, `@rainbow-me/rainbowkit-siwe-next-auth`, `wagmi`

## Future: Extract to @pasosdejesus/m

If the custom implementation proves stable, extract as a portable module:

```
@pasosdejesus/m/wallet-auth
  ├── ConnectWalletButton.tsx   ← UI component (i18n-ready)
  ├── siwe-connect.ts           ← SIWE flow logic (window.ethereum → callback)
  ├── useWalletSession.ts       ← hook: session + localStorage fallback
  └── README.md                 ← usage docs
```

This would give any pdJ project (learn.tg, sivel.xyz, stable-sl.pdJ.app) a
zero-dependency wallet auth flow:
- `window.ethereum` direct (no wagmi, no RainbowKit)
- NextAuth SIWE backend (existing, battle-tested)
- localStorage fallback for session persistence across navigation
- ~5KB gzipped vs ~200KB of RainbowKit + wagmi

## Future: PWA + Built-in Wallet (R-#184 + This REQ)

Combining R-#186 (own auth) with R-#184 (client-side wallet generation) and
PWA support creates a complete self-contained onboarding flow:

1. User opens learn.tg PWA → no wallet installed
2. PWA generates a wallet client-side (BIP39 seed phrase)
3. User saves seed phrase → wallet is "connected" (no app store needed)
4. SIWE auth via `personal_sign` with the generated key (no gas required)
5. User can now browse courses, build profile score
6. Once profile score ≥ 50, claim CELO UBI → now has gas for transactions
7. Later, import seed phrase into OneKey for full wallet features

### Why SIWE (personal_sign) over On-Chain Auth (R-#140)

R-#140 proposes replacing SIWE with on-chain auth (`transfer` with nonce in
`data`). While elegant, it creates a chicken-and-egg problem:

| Auth method | Needs gas? | Works for new users? |
|-------------|-----------|---------------------|
| **SIWE** (`personal_sign`) | ❌ No | ✅ User can auth immediately |
| **On-chain** (`transfer`) | ✅ Yes (CELO) | ❌ User needs gas first |

To get gas (CELO UBI), the user needs a profile score ≥ 50. To build profile
score, the user needs to authenticate. On-chain auth would require either:
- An exception to donate CELO on first connection → scammers would exploit this
- The user to obtain CELO elsewhere → defeats the purpose of learn.tg UBI

**SIWE is the correct choice for first-time auth.** On-chain auth (R-#140)
could be added later as an **alternative** for users who already have gas,
or for re-authentication scenarios. But the primary flow should remain
gas-free SIWE via `personal_sign`.

### MiniPay Compatibility

MiniPay does not support `personal_sign` and does not operate in Sierra Leone
(our primary target). Opera has not indicated plans to expand there. Dropping
MiniPay support is an acceptable trade-off given:
- Our target users are in Sierra Leone → need OneKey or built-in wallet
- MiniPay's 14M+ users are predominantly in other markets
- The built-in wallet (R-#184) serves the same "no app store" use case better
- `window.ethereum` + SIWE works in every wallet Sierra Leoneans actually use

## Synergy with R-#184

The built-in wallet from R-#184 integrates naturally with this auth system:
- The generated wallet can sign SIWE messages (no external wallet needed)
- The same key can sign transactions later (donate, claim, crossword)
- The seed phrase works in OneKey — no lock-in
- Everything works offline/PWA — no server round-trips for auth

## Acceptance Criteria

- [x] User sees own Connect button (not RainbowKit's) in Header (i18n: en/es)
- [x] SIWE flow with `window.ethereum` direct (eth_requestAccounts → personal_sign → callback)
- [x] EIP-55 address normalization (OneKey lowercase → `getAddress()` for SiweMessage)
- [x] localStorage fallback when `useSession()` loses state after navigation (bug #5719)
- [x] Hydration-safe localStorage (useState + useEffect, no SSR mismatch)
- [x] Disconnect via `signOut()` + localStorage.clear + redirect (no cookie residue)
- [x] MiniPay auto-connect preserved (hidden button, auto-SIWE)
- [x] Profile page uses `useAuthAddress()` (no more "Partial login" on navigation)
- [ ] Donate, claim UBI, profile save, crossword submit with `window.ethereum` (Phase 3)
- [ ] Remove RainbowKit + wagmi packages, reduce bundle (Phase 4)
- [ ] E2E tests with real SIWE signing mock (Phase 5)

## Implementation Status

| Phase | Description | Status |
|-------|-------------|--------|
| 1 | `ConnectWalletButton` — window.ethereum + SIWE directo + i18n | ✅ |
| 2 | Header usa ConnectWalletButton + localStorage hydration-safe | ✅ |
| 3 | Reemplazar wagmi en componentes de escritura | 🔧 |
| 4 | Eliminar RainbowKit + wagmi | ❌ |
| 5 | Tests | 🔧 |

### ⚓ Fallback Point: Hybrid Commit (2026-07-14)

El commit actual es un híbrido funcional — si la migración completa a
`window.ethereum` para firma causa problemas, este es el punto de reversión:

- **Identidad:** `ConnectWalletButton` + `session.address` + localStorage (sin RainbowKit)
- **Firma:** wagmi (`useWriteContract`, `usePublicClient`, `useWalletClient`) intacto
- **Resultado:** Donaciones, UBI claims, crucigramas, perfil — todo funciona
- **Diferencia:** Solo la capa de UI cambió; wagmi sigue en `AppProvider`

### OKX Removal (2026-07-14)

OKX se divorció de Celo, nos tocó migrar a OneKey. Código OKX-specifico eliminado:
- `components/OKXNetworkCheck.tsx` ❌
- `components/WalletDetectionHint.tsx` ❌
- `lib/okx-switch.ts` ❌
- `auth-options.ts`: `isOKXBrowser` + patrones UA removidos
- `AppProvider.tsx`: referencias a OKXNetworkCheck y WalletDetectionHint removidas

### Key Components

| File | Purpose |
|------|---------|
| `components/ConnectWalletButton.tsx` | Botón Connect/Disconnect con SIWE vía window.ethereum |
| `components/Header.tsx` | Sin RainbowKit, usa ConnectWalletButton + session.address |
| `lib/hooks/useAuthAddress.ts` | Hook unificado: wagmi ∥ session ∥ localStorage |
| `e2e/helpers/siwe-wallet-mock.mjs` | Mock Puppeteer con firma ECDSA real vía exposeFunction |
| `e2e/specs/connect-wallet-flow.spec.mjs` | Test E2E del flujo completo Connect → SIWE → reload |

---

**Created:** 2026-07-14
**Updated:** 2026-07-14 — Phases 1-2 completos, hydration fix, disconnect con signOut, mock con firma real
**Status:** En Progreso (3/5 fases, ~60% completado)
