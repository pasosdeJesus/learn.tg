#  R-#223: Donation Page for Lensenia Water Well

## 1. Objective

Create a dedicated donation page on learn.tg for the Lensenia water well project. The page will:

1. **Display total funds raised** in USD from the Lensenia wallet balances across Celo, AVAX and Base (USDT, USDC, CELO, G$, XAUT/XAUt0)
2. **Show progress** toward the goal of $8,500
3. **Reuse the existing donation flow** of the platform (DonateModal + verify + registro en `transaction`)
4. **Replicate the GoodDollar claim button** on the page (como en el curso Web3 & UBI) y enlazar la **guía de reclamar** para la explicación completa
5. **Link to the main project page** on pasosdejesus.org for other donation methods
6. **Let each donor choose** whether to receive the SLEARN cashback for this
   campaign (opt in/out, per donation)
7. **Let each donor choose** whether a percentage of their donation goes to
   pdJ, and how much (per donation)

> **Alcance de criptos (decisión 2026-09-02, actualizada):** recepción en
> **Celo**: USDT, USDC, CELO, G$ y **XAUt0**. **SLEARN queda FUERA por ahora**
> (token de utilidad, no de recaudación en USD).
> **XAUt0 verificado en Celo:** `0xaf37E8B6C9ED7f6318979f56Fc287d76c30847ff` (ERC-20, 6 decimales).
> **Balance multi-cadena (presentación):** saldos on-chain de la billetera
> Lensenia al menos en **Celo, AVAX y Base** — Celo: USDT, USDC, XAUt0, G$ y
> CELO; AVAX: USDT, USDC, XAUt0; Base: **solo USDC** (no hay XAUt0 nativo en
> Base; el USDT bridged queda fuera por ahora) — incluyendo **ahorros
> previos**, no solo recaudación.
> G$ **sí tiene precio de mercado** (https://coinmarketcap.com/currencies/gooddollar/).
> La billetera Lensenia mantiene ahorros en **XAUT/XAUt0** y se desea poder recibirlos.

---

## 2. Design Decisions

| Decision | Rationale |
|----------|-----------|
| **URL** | `https://learn.tg/{lang}/donations/lensenia` — clean, memorable, scalable for future campaigns |
| **Flujo de donación** | Reusar el flujo EXISTENTE (DonateModal → billetera del backend → verify → `transaction`); NO envío directo a la billetera externa (rompería registro + cashback + transparencia) |
| **DonateModal** | Vive en el **core** (`apps/nextjs/components/DonateModal.tsx`, inyectado a motores por deps D2) — NO está en `packages/rewards` |
| **GoodDollar claim** | **Replicar `GoodDollarClaimButton`** en la página + enlace a la guía de reclamar del curso (**guide 3** — UBI/GoodDollar; la 2 es de billetera) para la explicación completa |
| **Criptos aceptadas (recepción)** | En **Celo**: USDT, USDC, CELO, G$ y **XAUt0** (`0xaf37E8B6C9ED7f6318979f56Fc287d76c30847ff`, 6 decimales). Sin SLEARN. La recepción queda **solo en Celo**; AVAX/Base no reciben por la plataforma (solo presentación de saldos) |
| **Balance (presentación)** | Saldos on-chain de la billetera Lensenia `0x9c7218...` al menos en **Celo, AVAX y Base**: Celo — USDT, USDC, XAUt0, G$, CELO; AVAX — USDT, USDC, XAUt0; Base — solo **USDC** (sin XAUt0 nativo; USDT bridged fuera por ahora). Incluye ahorros previos de la billetera, no solo recaudación de la campaña |
| **Precios USD** | Fuente de mercado (CoinMarketCap/CoinGecko, respaldo Blockscout `exchange_rate`) para USDT, USDC, CELO, G$, XAUt0 (Celo y AVAX) y USDC (Base) |
| **Other donation methods** | Link a `https://pasosdejesus.org/lensenia` para transferencias bancarias, Binance, Giveth (y XAUT off-chain si aplica) |
| **Cashback SLEARN** | **Opcional, por donación** (solo destino `campaign`): el donante elige recibir o no el cashback SLEARN (10% del valor donado). Si lo rechaza no se mintea SLEARN extra ni se registra `donation_reward`; la campaña recibe lo mismo en ambos casos (el cashback es un incentivo adicional del motor, no un descuento a la campaña) |
| **Donación a pdJ** | **Opcional, por donación**: el donante elige si una parte de su donación va a pdJ y qué porcentaje (0% por defecto → 100% a Lensenia). Esa parte se registra y se reenvía a `NEXT_PUBLIC_PDJ_TREASURY_ADDRESS`; la campaña recibe (100 − %) |
| **Language** | Bilingual: English (`/en/donations/lensenia`) and Spanish (`/es/donaciones/lensenia`) |

---

## 3. Page Structure

### 3.1 Sections

| Section | Content |
|---------|---------|
| **Header** | Title, project description, goal ($8,500), current total (USD) |
| **Progress** | Bar showing progress toward $8,500 (percentage + visual bar) |
| **Fund breakdown** | Saldos por cadena y por token con equivalente USD — Celo: USDT, USDC, CELO, G$, XAUt0; AVAX: USDT, USDC, XAUt0; Base: USDC |
| **Donate** | A "Donate now" button that opens the existing `DonateModal` (campaign Lensenia) |
| **GoodDollar** | `GoodDollarClaimButton` replicado + texto breve ("reclama G$ gratis cada día y done a la causa") + enlace a la guía 3 para la explicación completa |
| **Other ways** | Link to `https://pasosdejesus.org/lensenia` for bank transfers, Binance, Giveth, XAUT |

### 3.2 Wireframe (Text)

```
┌─────────────────────────────────────────────────────────────┐
│  🌊 Lensenia Water Well                                    │
│  Help build a water well for Lensenia, Sierra Leone        │
│  Goal: $8,500                                              │
│  ████████████░░░░░░░░░░░░ 45% ($3,825 raised)              │
├─────────────────────────────────────────────────────────────┤
│  Fund breakdown (wallet 0x9c72…, Celo+AVAX+Base):         │
│  Celo: USDT $1,200 · USDC $300 · CELO 150 ($450)          │
│        G$ 10,000 ($100) · XAUt0 0.10 ($448)               │
│  AVAX: USDT $100 · XAUt0 0.2 ($896)                        │
│  Base: USDC $50                                           │
│  Total: $3,544                                            │
├─────────────────────────────────────────────────────────────┤
│  [ Donate now ]  ← Opens existing DonateModal (campaign)   │
│    opciones por donación dentro del modal:                 │
│    ☑ SLEARN cashback (10%)   [toggle on/off]               │
│    ☐ Donate to pdJ: 0%  (0 ▸ 5 ▸ 10 ▸ 20 ▸ free)           │
├─────────────────────────────────────────────────────────────┤
│  💰 Reclama GoodDollar a diario y dónalo al pozo:          │
│  [Claim GoodDollar]  + enlace a la guía 3 (explicación)    │
├─────────────────────────────────────────────────────────────┤
│  Other ways to donate:                                     │
│  🔗 https://pasosdejesus.org/lensenia                      │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 Donor Options (por donación)

Solo para destinos `campaign`; el resto de flujos conserva su reparto fijo
actual (cashback automático y % pdJ implícito). Las opciones son **efímeras por
donación**: no se guardan como preferencias del usuario (fuera de alcance por
ahora). Ambas se registran en `transaction.metadata` + `userevent` para
mantener la transparencia del dashboard.

| Option | Control | Default | Effect |
|--------|---------|---------|--------|
| **SLEARN cashback** | Toggle (`receiveCashback`) | **ON** | ON: 10% del valor donado vuelve como SLEARN al donante (`donation_reward`, minteado por el motor como en los flujos existentes — no reduce el monto de la campaña). OFF: no se mintea ni se registra cashback |
| **Donation to pdJ** | Switch + percentage selector (`pdjSharePct`) | **OFF / 0%** | 0%: el 100% va a la campaña Lensenia. X% (>0): esa parte se registra y se reenvía a la tesorería de pdJ; la campaña recibe (100 − X)% |

Notas:

- El modal recalcula **en vivo** el desglose (`getTargetCopy` /
  `getDistributionBreakdown`) según `receiveCashback` y `pdjSharePct`.
- Selección de %: presets 0 / 5 / 10 / 20 / 50 o entrada libre 0–100 (paso
  mínimo 1%). El monto resultante a pdJ no debe quedar en fracciones de
  centavo: definir en implementación el mínimo donable para % > 0.

---

## 4. Technical Implementation

### 4.1 Backend

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/donations/lensenia/balance` | GET | Total USD + breakdown por cadena/token (saldos on-chain de la billetera Lensenia en Celo, AVAX y Base) |

**Balance = saldos on-chain de la billetera Lensenia (decisión):** la página
presenta los saldos REALES de la billetera `0x9c7218...` leídos on-chain
(read-only) al menos en **Celo, AVAX y Base** — incluye ahorros previos (p.
ej. XAUt0 ya en poder de la billetera), no solo la recaudación de la campaña:

| Chain | Tokens presentados |
|-------|--------------------|
| Celo (42220) | CELO (nativo), USDT, USDC, G$ (GoodDollar), XAUt0 |
| AVAX (43114) | USDT, USDC, XAUt0 |
| Base (8453) | USDC (sin XAUt0 nativo en Base; USDT bridged fuera por ahora) |

- Lectura por RPC por cadena (`balanceOf` ERC-20 + `getBalance` nativo), con
  lista de tokens **configurable por campaña** (direcciones + decimales + RPC)
  y caché con TTL corto; degradación elegante si una cadena/RPC no responde.
- Se lee la billetera **Lensenia** (destino), NO la billetera operativa del
  backend (que mezcla todas las donaciones y duplicaría/mezclaría montos).
- El `transaction` (ledger, `metadata.campaign='lensenia'`) SIGUE siendo el
  registro de auditoría de cada donación (flujo, desglose, transparencia) y
  cubre el **reenvío automático fallido/en cola**: la donación cuyo reenvío
  aún no confirmó hash se presenta como "pendiente de reenvío" hasta que el
  reenvío automático tenga hash, para no subestimar la recaudación.

**Camino del dinero (con opciones del donante):**
1. El donante envía a la **billetera del backend** vía el flujo de donación
   existente (`add-donation` / `gdcluster/donations/verify`) en **Celo**, con
   `campaign='lensenia'` en el registro y sus opciones (`receiveCashback`,
   `pdjSharePct`). Tokens: USDT, USDC y XAUt0 (ERC-20, verify existente);
   CELO nativo y G$ si se habilitan (ver §4.2).
2. **Inmediatamente** tras confirmar el pago (verify OK), el backend reenvía
   de forma **automática y server-side** (sin sweep manual ni lotes) la parte
   de la campaña ((100 − pdjSharePct)%) a la billetera Lensenia
   (`0x9c7218a253d1565fc5f2149ba51f0f55f0f27f07`) y, si pdjSharePct > 0, la
   parte pdJ (pdjSharePct%) a `NEXT_PUBLIC_PDJ_TREASURY_ADDRESS` — manteniendo
   el registro y actualizándolo con los hashes del reenvío. Si un reenvío
   falla: reintento automático (cola) + alerta; el monto queda visible como
   "pendiente de reenvío" hasta tener hash.
3. Si `receiveCashback=true`, el motor entrega el cashback SLEARN (10% del
   valor donado, `donation_reward`) como en los flujos existentes; si es
   `false`, no se mintea ni se registra cashback.

> NO enviar directo a `0x9c7218...` desde la UI: se pierde el registro en
> `transaction`, el cashback del donante y la transparencia del dashboard.

**Nuevo destino `campaign`:** extender `PaymentTarget` de gdcluster
(`lib/donation-target.ts`: hoy `course-donation | cluster | country`) con un
tipo `campaign` (slug, billetera destino, meta de USD, copy del desglose) y su
ruteo, reutilizando el motor existente.

**Payload y opciones:** el endpoint de donación recibe, además de los campos
actuales, `receiveCashback: boolean` y `pdjSharePct: number` (0–100, validado:
0 ≤ pct ≤ 100) y los propaga al registro y a los reenvíos.

**Registro en el ledger (decisión):** una sola fila `type='donation'` por pago
del donante, con `subcategoria='campaign'`, `metadata.campaign='lensenia'` y
el split **denormalizado** en metadata, de modo que la recaudación de la
campaña (y el pendiente de reenvío) sea una suma simple y exacta:

```jsonc
// transaction.metadata (ej. donación de 100 USDT con 10% a pdJ y cashback ON)
{ "campaign": "lensenia", "pdjSharePct": 10,
  "campaignAmountUSD": 90, "pdjAmountUSD": 10,
  "receiveCashback": true, "cashbackSlearn": 220 }
```

(`amount` = total donado; `campaignAmountUSD = amount × (1 − pdjSharePct/100)`;
`cashbackSlearn` presente solo si `receiveCashback=true`.) El agregado de
recaudación de la campaña (dashboard/transparencia) suma `campaignAmountUSD`,
nunca `amount`. El reenvío automático actualiza el registro con los hashes
(`lenseniaSweepHash`, `pdjSweepHash`) y emite un `userevent`
(`campaign_swept`) — **no** crea filas extra: la fila original ya registró el
pago del donante y la restricción `(crypto, hash)` UNIQUE impide duplicar
filas con el mismo hash.

**Precios USD:** fuente de mercado (CoinGecko/CoinMarketCap; respaldo
Blockscout `exchange_rate`) para USDT, USDC, CELO, G$ y XAUt0 (Celo y AVAX),
y USDC (Base); caché con TTL corto para no saturar la API de precios.

> **XAUt0 — verificado:** Celo `0xaf37E8B6C9ED7f6318979f56Fc287d76c30847ff`
> (Blockscout) y AVAX `0x2775d5105276781B4b85bA6eA6a6653bEeD1dd32` (Avascan +
> RPC: symbol `XAUt0`, 6 decimales). Celo entra al flujo de recepción; AVAX
> solo se usa en la presentación de saldos.
> **Base — sin XAUt0/XAUT nativo** (confirmado). Solo se presenta **USDC**
> (`0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`, 6 decimales, verificado por
> RPC). El USDT bridged de Base existe pero queda fuera por ahora.

### 4.2 Frontend

| Component | File | Description |
|-----------|------|-------------|
| Page | `app/[lang]/donations/lensenia/page.tsx` | Main page |
| Balance display | `components/donations/BalanceDisplay.tsx` | Shows total + breakdown |
| Progress bar | `components/donations/ProgressBar.tsx` | Visual progress toward goal |
| Donate button | `components/donations/DonateButton.tsx` | Opens existing `DonateModal` |

**DonateModal integration** (core `apps/nextjs/components/DonateModal.tsx`):
- Ya acepta `target?: PaymentTarget` (`course-donation | cluster | country`)
  + `onSuccess` + chequeo de billetera.
- Se extiende con el destino `campaign` (ver §4.1) — los montos se registran
  con `metadata.campaign`.
- Hoy maneja **USDT + SLEARN**. Para la campaña (Celo): USDT, USDC y XAUt0
  son ERC-20 y usan el verify existente; **CELO nativo y G$** no forman parte
  del verify ERC-20 actual → trabajo adicional (decidir en implementación).
- **AVAX/Base NO reciben donaciones por la plataforma** — solo se presentan
  sus saldos (lectura on-chain).
- Se añaden las **opciones por donación** para destinos `campaign` (§3.3):
  toggle `receiveCashback` (default ON) y selector `pdjSharePct` (default 0,
  con presets). El modal recalcula en vivo el desglose
  (`getTargetCopy` / `getDistributionBreakdown`) y envía ambas opciones al
  backend en el payload.

**GoodDollar claim:**
- Replicar `GoodDollarClaimButton` (componente que ya se usa en las guías).
- Enlace de explicación a la guía del curso: `/{lang}/web3-and-ubi/guide3`
  (ES: `/{lang}/web3-e-ibu/guia3`) — reclamar G$ a diario + verificación
  facial + cómo enviarlo al pozo.

---

## 5. Integration with Existing Infrastructure

| Component | Integration |
|-----------|-------------|
| **Wallet auth** | Reuse existing `useAuthAddress()` hook |
| **DonateModal** | Core `apps/nextjs/components/DonateModal.tsx` (+ destino `campaign` + opciones §3.3) |
| **Donor options** | `receiveCashback` + `pdjSharePct` en DonateModal → payload del backend → `transaction.metadata` |
| **GoodDollar claim** | Reuse `GoodDollarClaimButton` + guía 3 del curso |
| **USDT/USDC/XAUt0 (Celo)** | Reuse existing donation contract calls (verify ERC-20) — recepción solo en Celo |
| **Balances multi-cadena** | RPC read-only por cadena (Celo/AVAX/Base): `balanceOf` + `getBalance`; lista de tokens configurable por campaña |
| **Ledger** | `transaction` con `metadata.campaign` (auditoría + pendientes de reenvío) |

---

## 6. Acceptance Criteria

### Page Content
- [x] Page exists at `/{lang}/donations/lensenia` (EN + ES)
- [x] Project description matches `pasosdejesus.org/lensenia`
- [x] Goal ($8,500) and current total displayed
- [x] Progress bar shows visual progress
- [x] Fund breakdown por cadena: Celo (USDT, USDC, CELO, G$, XAUt0), AVAX (USDT, USDC, XAUt0) y Base (solo USDC), con equivalentes USD (sin SLEARN)
- [x] El total incluye los saldos previos de la billetera Lensenia (no solo la recaudación de la campaña)

### Donation Flow
- [x] "Donate now" button opens the existing `DonateModal` (destino campaign)
- [x] El modal muestra las opciones por donación: cashback SLEARN (on/off) y % a pdJ (0 por defecto)
- [x] Con cashback OFF no se registra `donation_reward` ni se muestra cashback en el resultado
- [x] El desglose final y el texto `splitInfo` reflejan el % a pdJ elegido (campaña vs pdJ)
- [x] Con `pdjSharePct=0` el 100% queda en la campaña Lensenia
- [x] Donation goes through the platform flow (backend wallet → verify →
      `transaction` con `campaign='lensenia'` → reenvío a `0x9c7218...` y a pdJ según el %)
- [x] El reenvío a `0x9c7218...` (y a pdJ si `pdjSharePct>0`) es **automático e
      inmediato** tras confirmar la donación (server-side, sin sweep manual)
- [ ] Reintento con **cola programada + alerta al operador fuera de logs**
      (inline 2 intentos y reintento oportunista en el GET del balance ya
      implementados; el fallo se registra sin hash como "pendiente de reenvío")
- [x] Modal multi-token para campañas: selector "Pay with" con USDT/USDC/XAUt0/G$
      (muestra el precio USD de los no pegados y el saldo del token activo) —
      mainnet; testnet: solo USDT
- [ ] UI nativa de CELO en el modal (recepción por valor ya implementada en el
      backend; el selector del modal es ERC-20 por ahora)
- [x] Direcciones, decimales y RPC por token/cadena configurables (registro de
      campañas §4.1; direcciones Celo/AVAX/Base verificadas en §8)
- [x] Transaction hash displayed after success
- [x] Balance updates after donation (relectura on-chain al completar + pendiente de reenvío desde el ledger)
- [x] Opciones visibles en el dashboard de transparencia (`/[lang]/transparency`
      muestra la sección de la campaña: totales, split campaña/pdJ/cashback,
      pendiente y últimas donaciones desde `transaction.metadata`)

### GoodDollar
- [x] `GoodDollarClaimButton` replicado en la página (reclama G$)
- [x] Enlace a la guía 3 (explicación completa de claim + depósito al pozo)

### Other Ways to Donate
- [x] "Other ways to donate" links to `pasosdejesus.org/lensenia`

### Future Extensibility
- [x] Campaign data is configurable (registro `CAMPAIGN_CONFIGS`, no hardcoded en la página)
- [x] URL structure supports future campaigns (`/donations/{slug}`)

---

## 6.1 Implementation status (2026-09)

**Implementado (código + pruebas):**
- `donation-target.ts` (motor gdcluster): tipo `campaign-donation`, registro de
  campañas (`CAMPAIGN_CONFIGS` con direcciones verificadas por cadena), split
  por donación (`campaignDonorSplit`, `splitRawAmount`), copy y breakdown.
- Ruta `donations/[slug]/verify` (gdcluster): verificación del transfer USDT
  on-chain → **reenvío automático e inmediato** a la billetera de la campaña y
  a pdJ según el %; cashback SLEARN (mint, requiere MINTER_ROLE) on/off;
  registro en `transaction` con split denormalizado. (6 tests)
- Ruta `donations/[slug]/balance` (gdcluster): saldos on-chain de la billetera
  destino en Celo/AVAX/Base + precios (CoinGecko con caché) + pendiente de
  reenvío desde el ledger.
- Flujo **consciente de red** en la donación: Celo mainnet (42220, tokens del
  registro) y Celo Sepolia (11142220, `cfg.testnet`: solo USDT Mock por
  ahora, misma billetera destino `0x9c72...`) — la red se detecta por
  chainId; `payToken` se valida contra las criptos permitidas de la red.
  (3 tests testnet)
- Thin routes Next.js (`app/api/donations/[slug]/{verify,balance}/route.ts`).
- `DonateModal`: destino `campaign`, opciones por donación (cashback toggle +
  % a pdJ con presets) y ocultamiento de SLEARN en campañas.
- Página `/{lang}/donations/lensenia` + componentes `BalanceDisplay`,
  `ProgressBar`, `DonateButton` (EN/ES).
- **Comentario del donante (procedencia de los fondos)**: campo opcional en el
  modal para campañas (máx 200, se normaliza y trunca), guardado en
  `transaction.metadata.comment` y en la descripción; se muestra en el
  dashboard de transparencia (columna "Comment"). (2 tests)
- Tests: 27 donation-target + 6 verify campaña + 3 testnet + 3 multi-token
  (todos verdes); `tsc` de motores y app sin errores.
- **Verificación en el sitio de desarrollo** (Celo Sepolia, MockUSDT del dev):
  `e2e/smoke/donate-campaign.spec.mjs` **9/9 OK** (validaciones + forma del
  balance multi-cadena) y `e2e/specs/donate-campaign-real.spec.mjs` **16/16
  OK** (donación real 3 USDT 100% campaña + 2 USDT 90/10 campaña/pdJ →
  auto-forward inmediato verificado on-chain en la billetera `0x9c72...`
  +4.80 USDT y filas en user-transactions sin `donation_reward` con cashback
  OFF).
- `doc/e2e-testing.md`: sección de **warmup** (`bin/warmup.mjs`, antes sin
  documentar) + filas en tablas de smoke/specs; el warmup ahora incluye las
  rutas de campaña.
- **Recepción multi-token en mainnet (USDC y XAUt0)**: `transaction.crypto`
  ampliado con `'usdc'`/`'xaut0'` (migración `20260903120000_campaign_donation_crypto.ts`),
  precio USD compartido en `lib/token-prices.ts` (pegados = 1; XAUt0 =
  CoinGecko `tether-gold` con caché; usado también por el balance multi-cadena),
  handler `verify` generalizado: registra en unidades del token donado y
  metadata en USD. (3 tests multi-token)
- **Modal multi-token de campaña**: selector "Pay with" (USDT/USDC/XAUt0 en
  mainnet, según `cfg.donationTokens`; USDT en testnet), saldo del token
  activo, precio USD de XAUt0 en vivo (deshabilita donar si el precio no
  está disponible) y `payToken` en el payload del verify.
- **Recepción de G$ (GoodDollar) en mainnet**: `donationTokens` incluye
  `'gdoll'` (ERC-20, 18 decimals, `0x62B8B11039FcfE5aB0C56E502b1C372A3d2a9c7A`,
  precio CoinGecko `gooddollar`), migración del CHECK
  `20260903130000_campaign_donation_gdoll.ts`, verificación + registro en
  `transaction.crypto='gdoll'` con split en unidades. El modal lo ofrece como
  opción de pago. (1 test; 4 tests multi-token)
- **Reenvío con reintento inline + registro pendiente**: cada reenvío
  (campaña/pdJ) se intenta 2 veces (1.5s entre intentos); si sigue fallando
  la donación se registra igual con `metadata.forwardPending` y sin hash
  (el balance la muestra como "pendiente de reenvío") y se loguea una alerta
  para el operador. La fila guarda los datos crudos (`campaignRaw`, `pdjRaw`,
  `tokenAddress`, `pdjTreasury`…). (2 tests)
- **Recepción de CELO nativo (mainnet y testnet)**: `donationTokens` incluye
  `'celo'`; el verify lee `tx.value` del receipt (sin ERC-20) y el reenvío usa
  `sendTransaction` vía `deps.backend.sendNativeTxAndWait` (nuevo método del
  backend-config) para campaña/pdJ; retry nativo incluido; filas
  `transaction.crypto='celo'` con split en CELO. El modal sigue en ERC-20
  (CELO filtrado del selector hasta la UI nativa). (5 tests: 3 verify nativo
  + 1 retry nativo + 1 registro; suite gdcluster 78/78)
- **Enlace en el menú principal**: ítem "💧 Donate · Lensenia" en el menú
  (hamburguesa, autenticado) + icono 💧 siempre visible en el header
  (`components/Header.tsx`, claves `navDonateLensenia` en
  `lib/i18n/common.ts` EN/ES).
- **Other ways to donate**: copy corregido — transferencias/Binance/Giveth en
  `pasosdejesus.org/lensenia` y las donaciones **XAUT off-chain descritas**
  (contacto por la página del proyecto para instrucciones de entrega).
- **Historial de movimientos de la billetera de la campaña**: API pública
  `GET /api/donations/{slug}/movements` (transacciones + token-transfers de
  los explorers Blockscout de Celo/Base; Avalanche best-effort; etiqueta
  contrapartes contrato con su nombre para marcar **pool (Uniswap,
  invertido)** o **XAUt0**); sección "Recent movements" al final de la página
  de donación (8) + página de **historial completo**
  `/{lang}/donations/lensenia/movements` (300) con nota de transparencia
  (las salidas fueron a contratos/piscinas/XAUt0, no retiros personales).
- **Dashboard de transparencia de campañas**: API pública
  `GET /api/donations/[slug]/transparency` (totales por cripto, split
  campaña/pdJ/cashback en USD, pendiente de reenvío y últimas donaciones con
  sus opciones desde `transaction.metadata`) y sección
  `CampaignTransparency` en `/{lang}/transparency` (EN/ES). (2 tests;
  suite gdcluster 73/73)

**Pendiente (requiere decisión/entorno):**
- Aplicar la migración del CHECK en cada entorno (dev/mainnet) y direcciones
  de test de USDC/XAUt0/G$ en Celo Sepolia (hoy testnet = solo USDT Mock).
- UI nativa de CELO en el modal (el backend ya recibe CELO por valor; el
  selector del modal sigue en ERC-20).
- Alerta al operador fuera de logs (telegram/correo) para reenvíos pendientes
  prolongados (el reintento oportunista ya reintenta en cada GET del balance).
- **P&L histórico de las inversiones** (fase futura): requiere series de
  precios históricas (CoinGecko `market_chart`) por token y ventanas de
  tiempo; mientras tanto, la sección de movimientos muestra entradas/salidas
  con etiquetas pool/XAUt0 y los saldos actuales (transparencia sin
  estimaciones de rendimiento).
- Verificación en vivo del endpoint `movements` y del explorer de Avalanche
  (actualmente no responde en `avalanche.blockscout.com`).
- Roles on-chain: `MINTER_ROLE` de SLEARN a la billetera del backend para el
  cashback (mainnet y testnet) y tesorería pdJ real en mainnet.
- Smoke test E2E/on-chain en mainnet (con montos reales pequeños, decisión de
  operación).

---

## 6.2 Verificación en el sitio de desarrollo (2026-09, learn.tg:9001)

Cada deploy del motor en el dev site (Celo Sepolia, MockUSDT `0x0d130F…`,
billetera de prueba `0x8427…`, backend dev `0x01a728…`) se valida con:
`apps/nextjs/bin/warmup.mjs` (todas las rutas, incluidas las de campaña),
`node e2e/smoke/donate-campaign.spec.mjs` y los specs reales. Resultados:

| Prueba | Resultado | Detalle |
|--------|-----------|---------|
| `warmup.mjs` | OK (varias corridas) | Rutas de campaña (`/en|es/donations/lensenia`, balance, verify) compilan; hubo 502 transitorios durante un redeploy (nginx/dev reiniciando), luego todo 200 |
| smoke `donate-campaign` | **9/9 OK** | 404 slug desconocido (balance y verify), 400 auth/hash, 401, bounds de `pdjSharePct`, forma del balance multi-cadena |
| spec `donate-campaign-real` (USDT) | **16/16 OK** (5 corridas) | Rounds 100% (3 USDT) y 90/10 (2 USDT) con auto-forward; la billetera campaña en Sepolia acumuló **24.00 USDT** (+4.80 por corrida); **10 filas** `campaign:` en user-transactions; sin `donation_reward` (cashback OFF) |
| spec `donate-campaign-celo-real` (CELO nativo) | **15/15 OK** (2 corridas) | `sendTransaction` por valor → `verify payToken='celo'`; rounds 0.5 CELO (100%) y 0.4 CELO (90/10); billetera campaña acumuló **1.72 CELO** (+0.86 por corrida); **4 filas** `crypto=celo`; sin `donation_reward` de campaña |
| `GET /api/donations/lensenia/transparency` | OK | Agregados vivos del ledger dev: `byCrypto: {usdt: 25}`, `campaignUSD: 24`, `pdjUSD: 1` (5 × 0.2), `cashback: 0`, `pending: 0`, 10 donaciones recientes |
| `/{lang}/transparency` | OK | La página muestra la sección "Campaign transparency" |
| Balance mainnet (`/api/donations/lensenia/balance`) | OK | Total ≈ **$1,488–$1,497** (varía con el precio del oro); dominado por XAUt0 0.3327 oz en Celo (~$1,486); USDC en Base $0.80; CELO ~1.38 |
| `rails-health.spec.mjs` (nuevo) | **UP** (200, 3 cursos, ~0.9s) | Smoke que detecta si el backend Rails del dev (`https://learn.tg:3500/learntg-admin/proyectosfinancieros.json`) está arriba. **Episodio:** Rails estuvo caído durante una corrida de browser specs — las páginas/cursos fallaban con network error/404 (`diag-session`, `premium-course-checkout`, `guide-claims`, `gas-insufficient-panel`, etc.); al arrancarlo, responde 200 y el smoke lo confirma |
| Reintento de browser specs (directo y vía `bin/m test:e2e <nombre>`) | Parcial | `auth-session` → **0 failures**. Persisten los 4 con el mismo resultado incluso con el runner oficial: `cluster-formation` (401 token de sesión SIWE), `gas-insufficient-panel` (SIWE 401 en ES), `premium-course-checkout` (curso GD "Loading…") y `referral-program` (referido nuevo + SIWE). Requieren depuración aparte (no tocan el flujo de campaña) |
| Movimientos (endpoint + UI) | OK | `GET /api/donations/lensenia/movements` en dev (red testnet por entorno): brief `limit=8` → 8 filas, full `limit=300` → **24 → 26 filas** (tras una corrida del spec real aparecen los 2 forwards nuevos de MUSDT; caché 60s + indexado del explorer). La página de donación muestra "Recent movements…" y el historial completo renderiza. Smoke `donate-campaign` ampliado: **14/14 OK** |

> Nota: los totales del ledger dev (filas, saldos de la billetera campaña) son
> **acumulados** entre corridas y redeploys — no deterministas.

---

## 7. Out of Scope (for now)

- SLEARN donations (fuera por decisión 2026-09-02)
- Recepción de donaciones en AVAX/Base por la plataforma (hoy solo presentación de saldos en esas cadenas)
- Persistir las preferencias del donante (cashback / % a pdJ) entre donaciones
  — hoy se eligen en cada donación
- Reglas antifraude específicas para las opciones (p. ej. mínimo donable con
  % > 0) — definir en implementación
- Direct GoodDollar claiming inside the platform beyond the existing button flow
- Email notifications for donations
- Donor recognition/leaderboard
- Recurring donations

---

## 8. Notes

### Wallet Address
```
0x9c7218a253d1565fc5f2149ba51f0f55f0f27f07
```
- EVM wallet on Celo, AVAX and Base (misma dirección; sus saldos se presentan en las tres cadenas)
- Managed by Vladimir Támara
- Mantiene ahorros en XAUT/XAUt0 (por eso el balance presenta saldos previos)

### Token addresses (verificadas)

**Celo mainnet (Blockscout):**

| Token | Address (Celo) | Decimals |
|-------|----------------|----------|
| CELO | nativo | 18 |
| USDT | `0x48065fbBE25f71C9282ddf5e1cD6D6A887483D5e` | 6 |
| USDC | `0xcebA9300f2b948710d2653dD7B07f33A8B32118C` | 6 |
| G$ (GoodDollar) | `0x62B8B11039FcfE5aB0C56E502b1C372A3d2a9c7A` | 18 |
| XAUt0 | `0xaf37E8B6C9ED7f6318979f56Fc287d76c30847ff` | 6 |

**Avalanche C-Chain (43114):**

| Token | Address (AVAX) | Decimals |
|-------|----------------|----------|
| USDC | `0xb97ef9ef8734c71904d8002f8b6bc66dd9c48a6e` | 6 |
| USDT (USDt) | `0x9702230a8ea53601f5cd2dc00fdbc13d4df4a8c7` | 6 |
| XAUt0 | `0x2775d5105276781B4b85bA6eA6a6653bEeD1dd32` | 6 |

(verificado por RPC: symbols `USDC`, `USDt` y `XAUt0`)

**Base (8453):**

| Token | Address (Base) | Decimals |
|-------|----------------|----------|
| USDC | `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` | 6 |

(verificado por RPC: symbol `USDC`; no hay XAUt0 nativo en Base; el USDT
bridged queda fuera por ahora)

### GoodDollar Claim Flow (guía 3 del curso Web3 & UBI)
1. User installs GoodDollar Wallet / se conecta
2. User verifies identity (facial verification, ~cada 6 meses)
3. User claims daily G$ (~150 G$/día; tiene precio de mercado)
4. User sends G$ to the Lensenia wallet (o a la campaña en la plataforma)

### Existing Components to Reuse
- `DonateModal` (core: `apps/nextjs/components/DonateModal.tsx`)
- `GoodDollarClaimButton` (`apps/nextjs/components/GoodDollarClaimButton.tsx`)
- Patrón de agregados por `transaction` + triggers de caché

---

> *"Let us not become weary in doing good, for at the proper time we will reap a harvest if we do not give up."* (Galatians 6:9)

---

**Related:** https://gitlab.com/pasosdeJesus/m/-/work_items/35 (motor gdcluster),
https://github.com/pasosdeJesus/learn.tg/issues/155 (fondos cluster),
https://github.com/pasosdeJesus/learn.tg/issues/202 (PaymentModal),
infraestructura de donación existente
