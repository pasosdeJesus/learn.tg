# R-#265: Several wallets per user

Let one learner own **several wallets** (in-app + external, or an old and a new one)
without getting a separate account, progress, scholarships or credentials for each.

**Link:** https://github.com/pasosdeJesus/learn.tg/issues/265
**Created:** 2026-09-24
**Status:** **Draft** — written for the operator to refine; nothing implemented yet.
**Type:** Feature / Identity
**Priority:** Medium-High — today the same person with the in-app wallet and an external
wallet ends up with two accounts and the work of one of them is invisible from the other
(the operator asked for it on 2026-09-23, while testing offline on Android: "we should
allow one user to have several wallets").
**Dependencies:** R-#227 / R-#233 (session-only auth), R-#244 / R-#245 / R-#246 (in-app
wallet), R-#259 (per-user privacy switches), R-#256 (offline copies are per wallet),
R-#163 (referral program), R-#153 (data at rest), R-#260 (learner/geo base model in `m`).

---

## 1. Objective

The wallet is the identity of the platform. This requirement does **not** change that; it
adds the missing piece: **one `usuario` may hold N wallets**, and any of them can sign in
to the same account.

1. **Link a wallet from inside the account**: signed in with wallet A, the learner signs
   a message with wallet B (SIWE) and B is added to the same account.
2. **Sign in with any linked wallet**: wallet B alone reaches the same account (same
   progress, scores, credentials, privacy switches, referral code).
3. **One principal wallet per account**: the one used for on-chain payouts and
   credentials, so the account has a single public address by default.
4. **Unlink** a secondary wallet, with the rules and the consequences stated in the UI.

## 2. What exists today (measured 2026-09-24)

| Piece | Where | Notes |
|---|---|---|
| Wallet ↔ user table | `billetera_usuario` (`id`, `billetera varchar(60)`, `usuario_id`, `answer_fib`, `nonce`, …) | `UNIQUE index_billetera_usuario_on_billetera` (**one wallet belongs to exactly one user**) and `UNIQUE (usuario_id, billetera)`. There is **no** unique on `usuario_id` alone, so the table already admits several wallets per user; nothing creates them |
| Sign-in | `apps/nextjs/app/api/auth/auth-options.ts` (`authorize()`) | Unknown wallet → INSERT `usuario` + `billetera_usuario` (one account per wallet): `nombre` = address, `nusuario` = truncated address, `email` = `<addr>@localhost`, `fechacreacion` = now (also the referral "older than the referred" rule). A known wallet → UPDATE `usuario` |
| API identity | `lib/authenticateUser.ts` | The NextAuth session cookie (`sub` = wallet) is the only credential; `walletAddress` is an untrusted hint that must match the subject. Every route resolves the user **from the wallet** |
| Blast radius | 30 files in this repo + the `app/api` submodule | `git grep -l billetera_usuario`; also `auth-options`, `admin/users*`, `admin/user/[id]`, `check-crossword`, `crossword`, `add-donation`, `pastor-bonus`, `credential` … |
| Per-user data | `usuario` | `learningscore`, `profilescore`, `mostrar_cursos_publico`, `mostrar_cursos_sensibles_publico`, `church_id`, `religion_id`, verification fields |
| Per-wallet data | `guide_usuario` (per `usuario_id`), `transaction` (`usuario_id` + `wallet`), `credential_emission` (`usuario_id` + token), `billetera_usuario.answer_fib` | Progress and earnings live in the account; the SBT lives **in the address** |
| Offline copies | `lib/offline-course-db.ts` (`belongsToWallet`) | A downloaded course records the wallet that downloaded it, and another wallet does not see it |
| On-chain rules | `LearnTGVaultsV5`, `CeloUbi`, `PasosDeJesusCredentials` | Anti-double-pay and cooldowns are per **address** (per guide, per course, per day); UBI is per address. **Two wallets of the same person can claim twice** unless this requirement says otherwise |

## 3. Design (to be refined by the operator)

### 3.1 Model

- Keep `billetera_usuario` as the wallet ↔ user map and **keep the unique wallet**
  (a wallet can never belong to two people).
- Add the notion of **principal wallet**: `billetera_usuario.principal BOOLEAN` (or
  `usuario.billetera_principal_id`), exactly one per user, enforced by a partial unique
  index or a trigger (same pattern as `one_principal_per_church`).
- Optionally `billetera_usuario.linked_at` / `linked_by` for audit in `userevent`.

### 3.2 Linking and unlinking

- **Link**: authenticated as the account + SIWE with the new wallet (`POST /api/wallet/link`,
  session cookie + signature; no new `usuario` is created).
- **Sign-in**: `authorize()` keeps creating an account for a genuinely new wallet (that is
  how the platform onboards), **but** when the SIWE arrives signed in as another account the
  route offers "link" instead. Open question: on a plain sign-in with an unknown wallet, do
  we offer to link it to the account the browser already has a session for?
- **Unlink**: only a secondary wallet, never the last one; what happens to the SBTs already
  minted to that address is an operator decision (they stay on-chain; the profile can stop
  showing them or keep them as historical).

### 3.3 What follows the user and what follows the wallet (operator decides)

| Data | Options |
|---|---|
| Progress (`guide_usuario`), scores, verification, privacy switches, referral code | **Per user** (they follow the account; that is the point of the requirement) |
| SBT / credential | Minted to the **principal** wallet, or to the wallet that completed the course; the profile shows all of them |
| Scholarships / UBI | **Anti-abuse decision**: the contract checks per address, so N wallets could claim N times. Options: (a) accept it, (b) the backend marks the guide as paid per user and refuses a second claim, (c) only the principal wallet can claim |
| Referral | Referrer/referred are users; linking must not create a self-referral (A links B and then B "refers" A) — needs an explicit rule |
| Offline copies | `belongsToWallet` accepts any wallet of the account; the record should also store the `usuario_id`; cleanup when the last wallet goes away |
| `pastor_bonus` / church roles | Per user and per church (unchanged); decide what happens when the linked wallet was already a pastor |

### 3.4 Limits and abuse

- Maximum wallets per user (suggested 2-3) and rate limit on linking.
- Linking does **not** merge two verified profiles: if the second wallet already had an
  account with progress, the operator decides whether to (a) refuse, (b) migrate
  `guide_usuario`/`transaction`/`credential_emission` to the surviving account, or (c) keep
  both accounts and only allow signing in with either.
- Self.xyz / verifier checks: per user, not per wallet.

### 3.5 Admin

- The admin UI lists every wallet of a user and allows linking/unlinking (both actions
  logged in `userevent`).

### 3.6 Privacy: switching wallets and transfers that cannot be traced (operator, 2026-09-24)

Notas del operador para cuando esta funcionalidad exista, pensando en la privacidad de
quien tiene que proteger su afiliación (R-#259) o simplemente no quiere que un observador
público ate su actividad entre billeteras:

1. **Recomendar cambiar de billetera.** Con varias billeteras por usuario, y aun con
   varias billeteras dentro de la billetera in-app, la app puede **sugerir** usar una
   billetera distinta para la actividad que se quiere desligar de la identidad principal
   (p. ej. reclamar UBI, donar o completar un curso sensible), explicando qué sigue a la
   persona (progreso, becas, credenciales) y qué queda en la dirección.
2. **Envío seguro entre billeteras in-app, difícil o imposible de rastrear.** Ofrecer una
   función para mover fondos de una billetera in-app a otra que use **mecanismos de
   tiempo** (p. ej. retraso/ventana de ejecución, lotes o montos normalizados) y de la
   **plataforma** (la propia billetera como intermediaria, firma y difusión desde la
   plataforma) para que un observador no pueda establecer **qué billetera le envió a
   cuál**. Debe declararse su límite honesto: on-chain la traza existe, así que el objetivo
   es que **no sea atribuible** con la información pública, no que sea indetectable.
   Requiere decisión sobre custodia en el momento de la transferencia, límites de monto,
   trazabilidad contable (transparencia) y cómo se combina con las reglas anti-abuso de
   §3.3 y §3.4.

## 4. Acceptance criteria (draft, to refine)

- [ ] A signed-in learner can link a second wallet by signing a SIWE message with it; the
      wallet appears in the account and the session keeps working.
- [ ] The linked wallet can sign in by itself and reaches the same account: same progress,
      scores, privacy switches, referral code and credentials list.
- [ ] Exactly one principal wallet per user (enforced in the database), never zero.
- [ ] A wallet still belongs to **one** user only (`UNIQUE (billetera)` kept); no API can
      move it without the signature of that wallet and the session of the receiving account.
- [ ] Linking never duplicates progress or a scholarship for the same guide in the
      database (`guide_usuario` one row per user and guide) and the anti-abuse decision of
      §3.3 about the on-chain checks is implemented and documented.
- [ ] Unlinking a secondary wallet is possible and safe (the account keeps at least one);
      the UI states what happens to the credentials already minted to it.
- [ ] The offline store accepts any wallet of the account and is cleaned when the last
      wallet is unlinked or deleted.
- [ ] The admin can see and manage the wallets of a user.
- [ ] Migrations reversible, `make type` and the affected `make test*` targets green,
      `node bin/audit-api-auth.mjs` at `0 failed` for the new route(s).
- [ ] User guide and `ARCHITECTURE.md` updated (how to link, what follows the user, what
      stays in the wallet).

## 5. Open questions for the operator

1. Do we offer "link this wallet to my account" to anyone who signs in with an unknown
   wallet while having a session, or only from an explicit screen in the profile/settings?
2. Can the person use both wallets to claim scholarships (the contract is per address), or
   do we enforce one claim per user?
3. If the second wallet already has an account with progress, do we merge, refuse or keep
   two accounts?
4. What happens to SBTs minted to a wallet that is later unlinked (keep them in the public
   profile, hide them, offer to re-mint to the principal)?
5. How many wallets per user, and is the in-app wallet always one of them?

## 6. Out of scope

- Changing what the wallet *is* (the account is still identified by the session's wallet).
- Multi-signature accounts or shared accounts between different people.
- Anonymous/pseudonymous credentials (R-#259 §5 keeps that separate).
- Migrating the identity to the `m` base model (R-#260); if that lands first, the "several
  wallets" relation is added there instead.

## 7. Related

- https://github.com/pasosdeJesus/learn.tg/issues/227, /233 (session cookie, no token)
- https://github.com/pasosdeJesus/learn.tg/issues/244, /245, /246 (in-app wallet)
- https://github.com/pasosdeJesus/learn.tg/issues/259 (privacy switches follow the user),
  /256 (offline copies are per wallet)
- https://github.com/pasosdeJesus/learn.tg/issues/163 (referrals), /153 (encryption),
  /260 (learner/geo base model), /264 (subscriptions)
- `doc/siwe-auth-flow.md`, `doc/wallet-auth.md`, `apps/nextjs/app/api/doc/API.md`
