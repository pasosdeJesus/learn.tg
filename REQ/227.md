# R-#227: Session-first API auth — stop using CSRF as the auth token

**Enlace:** https://github.com/pasosdeJesus/learn.tg/issues/227

**Status:** En progreso — Fase 1 implementada + regresión de token resuelta (2026-09-09)
**Created:** 2026-09-09

---

## Implementation status

### Fase 1 — session-first (2026-09-09, branch `227session`)

- [x] `authenticateUser` valida la **session cookie primero** (JWT, `sub` == wallet)
      y solo cae al token legacy si no hay sesión válida — `lib/authenticateUser.ts`
- [x] El token de `billetera_usuario` queda como **legacy fallback** (Rails lo
      valida solo contra ese token; no se puede retirar/rotar sin coordinar Rails)
- [x] **Opción B (Fase 2): token de API DEDICADO** — `authorize()` guarda un
      token aleatorio (256 bits, no el CSRF); `GET /api/auth/token` lo expone
      por session cookie; el cliente lo guarda en `learn.tg.authToken`
      (`ConnectWalletButton` + helper e2e). Validado en dev (2026-09-09):
      token ≠ CSRF; cookie → 200; token sin cookie → 401 con
      `AUTH_SESSION_ONLY=1`
- [x] Flag `AUTH_SESSION_ONLY=1` para forzar solo-sesión y medir dependencias
      (⚠️ **retirar de dev/prod**: era una bandera temporal de medición; el
      inventario de dependencias ya está hecho y la bandera rompe clientes
      legacy legítimos — Rails, specs sin cookie — con 401 aunque el token sea
      válido. Mantenerla activa solo retrasa la Fase 2, que exige el camino
      legacy hasta migrar Rails. Visto en la práctica: `referral claim` /
      `check-crossword` / `premium purchase` 401 sin cookie pese a token
      correcto.)
- [x] Logs de diagnóstico `DEBUG_AUTH` gated en `authenticateUser` y en
      `authorize()` (submódulo `learn.tg-api`)
- [ ] **Pendiente Fase 2:** dejar de rotar el token en `authorize()` y retirar
      la columna — SOLO después de migrar Rails (o darle token dedicado),
      porque `servidor/app/helpers/application_helper.rb` autentica
      exclusivamente con `billetera_usuario.token == request[:token]`
- [x] Origin/Sec-Fetch-Site checks para mutaciones — `apps/nextjs/middleware.ts`
      (2026-09-09): cross-site/same-site no seguros → 403; sin `Sec-Fetch`
      (no-navegador) se permite con verificación de `Origin`. Validado en dev y
      producción: V1 cross-site 403, V2 sin headers 400, V3 Origin ajeno 403
- [x] Reintento idempotente en el modal tras 401 (R-224) — verify ya es
      idempotente por `(crypto, hash)` UNIQUE

Validación: evidencia dev+prod en R-#224 §6 (rotación tolerada con cookie);
suite unit app **171/171** tras el cambio. En dev con `AUTH_SESSION_ONLY=1`
el repro confirma: token viejo+con cookie → 200 (session-first), token
(viejo **o vigente**) sin cookie → 401 (legacy deshabilitado).

### Inventario de dependencias residuales del token (Fase 2, 2026-09-09)

Medido en dev con `AUTH_SESSION_ONLY=1` + revisión de código:

| Consumidor | Tipo | ¿Depende del token? | Acción Fase 2 |
|---|---|---|---|
| Navegador web (DonateModal, `lib/admin-fetch.ts`, hooks, home `/en/`) | web con cookie | No (session-first) | Migrado a token dedicado `learn.tg.authToken` (commit `378470c`) |
| `e2e/specs/donate-campaign-real|celo-real` (GETs de perfil/user-transactions) | spec node | Sí | Adjuntar `Cookie` del SIWE en TODAS las llamadas (no solo verify) + token dedicado |
| `e2e/specs/donate-course-real`, `donate-gd-real` y similares | spec node | Sí | Token dedicado vía `/api/auth/token` en `siweSignIn` (commit `378470c`) |
| `e2e/smoke/auth-cookies` | smoke → **Rails** | Token vs Rails (independiente del flag) | Fase 2: dar a Rails sesión o token dedicado |
| **Rails `servidor/`** (`application_helper.rb`, `proyectosfinancieros_controller`) | backend externo | Token-only vs `billetera_usuario` | Migrar a sesión compartida o token dedicado antes de retirar la columna |
| Motores server→server | ninguno | No | — |


### Regresión reportada (2026-09-02): "Failed to load course" + Global Disciples no visible en `/en` — RESUELTA

En dev y producción (con el código más reciente), al conectar la wallet e ir a
`/en/` se listan varios cursos pero **no** el de Global Disciples, y aparece el
toast de error **"Failed to load course"**. Los specs E2E lo replican
(`premium-course-checkout` aborta con `FATAL: AxiosError: Request failed with
status code 401` en el `PATCH /api/profile`).

Causa raíz confirmada por código + E2E: **Opción B (Fase 1) cambió el token de
API de CSRF → token dedicado, pero varios consumidores del frontend y specs
siguen enviando el CSRF como `token`**, que ya no coincide con
`billetera_usuario.token`. En el navegador, el home `/en/` (`app/[lang]/page.tsx`)
usa `getCsrfToken()` para `filtro[busconBilletera]=true&walletAddress=…&token=…`
contra Rails (`proyectosfinancieros.json`, verifica `billetera_usuario.token ==
token`), y para `/api/profile` (religion_id → filtro Christian que muestra GD).
Con el CSRF obsoleto: Rails devuelve 401 → "Different tokens" → el listado
cae al `catch` y dispara el toast; y/o `religion_id` nunca se resuelve →
`christian=false` → GD queda oculto.

**Resolución (commit `378470c` "random token", validado en dev 2026-09-07):**
todos los consumidores del frontend y de los specs migraron a
`learn.tg.authToken` (token dedicado) con fallback a `getCsrfToken()` como
legacy. Verificación E2E contra `https://learn.tg:9001`:

- `premium-course-checkout` → **0 failures** (GD: Buy visible → Checkout).
- `full-flow` Step 6 `/en` → "No error toasts", **5 course links** (antes 3 y
  sin GD).

Consumidores migrados:

- Frontend: `app/[lang]/page.tsx` (listado + `refreshCourseVault`),
  `app/[lang]/profile/page.tsx` (update-scores), `app/[lang]/[pathPrefix]/page.tsx`,
  `app/[lang]/[pathPrefix]/[pathSuffix]/test/page.tsx` (crossword),
  `components/DonateModal.tsx`, `components/DeleteVerifiedDataDialog.tsx`,
  `lib/hooks/useScholarshipData.ts`.
- Specs/smoke: `premium-course-checkout`, `prerequisites`, `pastor-bonus-verify`,
  `referral-payout`, `referral-premium`, `pastor-journey`, `interview-date`,
  `verified-city-gate`, `vault-both-donate`, `donate-course-real`,
  `donate-gd-real`, `donate-campaign-real`, `donate-campaign-celo-real`,
  `donate-campaign-celo-prod-stress`, `premium-courses`, `auth-cookies`,
  `celo-claim`, `donate-course`, `donate-gd`, `donate-campaign`,
  `e2e/diag/repro-auth-rotation`.

*Nota:* `lib/hooks/useCourse.ts` (página de detalle) ya estaba migrado.

### Problemas reportados pendientes

#### 1. Donación a vault de curso registrada sin modal de "donación recibida"

- **Síntoma:** al donar al vault de un curso, la donación **queda registrada**
  (el backend la recibe, `/api/add-donation` responde 200) pero **no se muestra
  el modal de "Donation completed"** con el desglose de distribución
  (`resultDistribution`).
- **Síntoma adicional (dato del usuario):** al confirmar el pago en la billetera
  **se refresca la página entera**. Ese reload es lo que impide ver el modal de
  resultado (que vive en estado React del `DonateModal`): tras la recarga el
  estado se pierde, aunque la donación ya quedó persistida en el backend.
- **Contraste:** donar a la campaña **Lensenia** sí muestra el modal con
  desglose tras confirmar en la billetera. Es decir, el camino
  `campaign-donation` no dispara el refresh; el camino `course-donation` (vault
  de curso, ERC-20 USDT/SLEARN vía `useContractPayment`) sí.
- **Hipótesis de refresh (a confirmar):** `components/WalletEventListener.tsx`
  hace `signOut({ redirect: true, callbackUrl: '/' })` ante
  `accountsChanged([])` o `disconnect`; y `ConnectWalletButton` hace
  `window.location.reload()`. Si la billetera (móvil / Rabby / OneKey) emite un
  `accountsChanged` vacío o `disconnect` durante/tras la confirmación ERC-20,
  la app recarga y el modal se pierde. Falta distinguir cuál de los dos triggers
  se dispara (log de `[WalletEventListener]` o de `accountsChanged`) y por qué
  no ocurre en el flujo de campaña.
- **Flujo afectado (hipótesis secundaria):** `components/DonateModal.tsx` →
  `useContractPayment` → `onBackendCallback` → `POST /api/add-donation` → el
  hook llama `onSuccess?.({ increment, usdtHash, slearnHash, ...backendResult })`
  y `DonateModal` pone `setShowResult(true)` + `setResultDistribution(...)`.
  A registrar el 200 pero no verse el modal, el sospechoso es que el `showResult`
  se pierde por el reload descrito arriba (no necesariamente un bug en
  `setShowResult`).
- **Pendiente de diagnóstico:** confirmar con `[PayDiag]`/consola qué devuelve
  `/api/add-donation` (¿incluye `distribution`?), si `onSuccess` se dispara, y
  qué evento de wallet dispara la recarga (`accountsChanged` vacío vs
  `disconnect`).

### 1a. Diagnóstico — trigger confirmado (2026-09-08, spec `diag-donation-refresh`)

Reproducción controlada en dev (`learn.tg:9001`) con un provider EIP-1193 que
captura handlers y emite el evento tras abrir un curso con vault:

```
[DiagEmit] accountsChanged → 6 handler(s)
[WalletEvtDiag] accountsChanged {"inFlight":false,"sessionAddr":"0x84272a6d", detail:{count:0,empty:true}}
[WalletEvtDiag] accountsChanged -> signOut(redirect:true)
→ página recarga/navega a "/"  (REPRODUCIDO)
```

**Conclusión:** el refresh que pierde el modal de "Donation completed" es el
`signOut({ redirect: true, callbackUrl: '/' })` de `WalletEventListener.tsx`
disparado por `accountsChanged([])` (y también `disconnect`, mismo handler). El
backend NO es el problema: `POST /api/add-donation` responde 200 con
`distribution` completa (verificado con `vault-both-donate.spec.mjs`).

**Por qué campaña (Lensenia) sí muestra el modal:** su flujo
(`handleNativeDonate`) confirma con `eth_sendTransaction` y no dispara el
evento `accountsChanged` en la billetera probada; el flujo ERC-20 de
`useContractPayment` sí lo provoca (o el usuario confirma de forma que la
billetera emite el evento).

**Fix propuesto (a validar):** en `WalletEventListener`, cuando el evento llega
con una donación en vuelo (`window.__donationInFlight === true`, marcado por
`DonateModal`) **no** hacer `signOut(redirect)` si el wallet aún reporta la
misma cuenta conectada tras el evento (re-chequeo `eth_accounts`); o bien
debounce del `accountsChanged([])` (esperar y re-verificar `eth_accounts`
antes de firmar la desconexión). Nunca ignorar a ciegas: una desconexión real
debe seguir limpiando la sesión.

### 1b. Fix implementado (2026-09-08)

`components/WalletEventListener.tsx`: los handlers de `accountsChanged([])` y
`disconnect` ya **no firman desconexión a ciegas**. Tras el evento esperan
`debounceMs = 400` y re-verifican con `eth_accounts`:
- si la billetera sigue conectada (cuenta presente) → el evento era transitorio
  (confirmación de tx) → **se conserva la sesión** y no hay recarga → el modal
  de resultado sobrevive.
- si sigue sin cuenta → desconexión real → `signOut({ redirect: true })` como
  antes.

Verificado por E2E de regresión: disparar `accountsChanged([])` sin que cambie
la cuenta ya **no** recarga la página. Queda la validación manual en el sitio de
desarrollo donando a un vault con la billetera real.

**Refactor de repetición (2026-09-08):** el patrón "token dedicado con fallback
CSRF" que se había duplicado se centralizó en:
- App (`lib/auth-token.ts`): `getApiToken()` / `getStoredApiToken()` /
  `apiAuthQuery()`; consumido por el home `/en/`, profile, curso, crossword,
  `DonateModal`, `CeloUbiButton`, `CheckoutModal`, `DeleteVerifiedDataDialog`,
  `NotificationsBell`, `UserLanguageSync`, `useCourse`, `useScholarshipData`.
- E2E (`e2e/helpers/siwe-auth.mjs`): `dedicatedApiTokenAxios()` y
  `dedicatedApiTokenFetch()`; consumidos por los specs que antes repetían el
  bloque (`prerequisites`, `pastor-bonus-verify`, `referral-*`,
  `pastor-journey`, `interview-date`, `verified-city-gate`, `vault-both-donate`,
  `premium-course-checkout`, `donate-*-real`, smoke de donaciones,
  `repro-auth-rotation`).


---

## 1. Objective

Change API authorization so that **the NextAuth session (JWT in an HttpOnly
cookie) becomes the primary mechanism**, and stop using the **SIWE nonce/CSRF
as a persistent authentication token** (the `billetera_usuario.token`
column). CSRF remains only as the SIWE handshake nonce.

Direct motivation: real users get **401 "Unauthorized"** right after
confirming an on-chain transaction (reported on donations with the OneKey
wallet), and several E2E specs fail for the same reason (R-#224). Root cause:
the token **rotates on every SIWE sign-in** — logging in with the same wallet
from another tab/device invalidates the token of an already-open page, so a
money operation already signed on-chain stays unregistered. That must not
happen.

## 2. Context / Constraints

- **We already moved away from RainbowKit + wagmi** due to the session-loss
  problems documented in [issue #167](https://github.com/pasosdeJesus/learn.tg/issues/167)
  (session persistence — partial login with SIWE+NextAuth+OneKey/RainbowKit)
  and [issue #168](https://github.com/pasosdeJesus/learn.tg/issues/168)
  (RainbowKit QR crash), replaced by the in-house
  `ConnectWalletButton`/`useAuthAddress` implementation ([issue #186](https://github.com/pasosdeJesus/learn.tg/issues/186) — `doc/wallet-auth.md`).
- **RainbowKit/wagmi is not coming back.** The fix lives in the session layer
  (NextAuth + `authenticateUser`), keeping the in-house wallet hooks.
- Ecosystem reference pattern: the official SIWE+NextAuth example
  (RainbowKit `with-next-siwe-next-auth` / Auth.js) validates
  `siweMessage.nonce === csrfToken` **only inside `authorize()`** and then
  authorizes with the session; it never persists CSRF as an API token.
- OWASP CSRF Cheat Sheet: *"A CSRF token is not an access token"*; rotating
  the token on every login is an anti-pattern that hurts UX without a security
  gain; and tokens must not travel in URL/query params (log-leak risk).

## 3. Design decisions

| Decision | Rationale |
|---|---|
| **Session first, DB token as fallback (transition)** | `authenticateUser` validates the session cookie (JWT, `sub` = wallet, signed with `NEXTAUTH_SECRET`); if there is no cookie it falls back to the `billetera_usuario` token — old specs/tools keep working during the migration |
| **CSRF stays only for the SIWE login** | The dual use goes away: the nonce is no longer persisted as an API credential (stop populating `billetera_usuario.token`, then migrate/drop the column) |
| **Identity = session `sub`; request `walletAddress` only routes** | Prevents impersonation: the wallet param must match the session `sub` (and exist in `billetera_usuario`) |
| **CSRF for mutating APIs without a token in the body** | Same-origin + JSON/fetch: `SameSite=Lax` (session cookie) + `Origin`/`Sec-Fetch-Site` checks on unsafe methods; NextAuth keeps its internal CSRF for `/api/auth/*` |
| **Money operations resilient to 401** | Payment/donation verifies of already-signed on-chain txs must be retryable (idempotent via the `(crypto, hash)` UNIQUE constraint); if the session expired, re-authenticate by cookie and retry — never lose the hash record |
| **No wallet libraries reintroduced** | `wallet-auth` (R-#186) stays; only the way the server decides who the user is changes |

## 4. Specification

### 4.1 `authenticateUser` (new order)

1. Request has `token`/`walletAddress`: if the session cookie is valid and its
   `sub` == wallet (lowercase) → OK (no DB-token lookup).
2. No session → validate the DB token as a **legacy fallback** (same code as
   today), with gated logging to detect residual usage.
3. `401` responses always carry a reason in logs (tag `[auth]`, no PII,
   enabled via `DEBUG_AUTH=1`).

### 4.2 SIWE login

- Kept: CSRF as the nonce of the signed message (step 2 of the flow in
  `doc/siwe-auth-flow.md`), domain/chain/signature verification in
  `authorize()`.
- Change: in step 7 the nonce is **no longer rotated/stored as an API
  credential** (kept or marked obsolete in `billetera_usuario.token`).

### 4.3 CSRF layer for mutations (same origin)

Middleware/helpers for unsafe methods on cookie-authenticated routes:
- `Origin` (when present) == expected origin; if absent, fall back to
  `Referer`; reject when `Sec-Fetch-Site: cross-site` and the method is not
  safe.
- Session cookies: `Secure; HttpOnly; SameSite=Lax` (+ `__Host-` prefix in
  production if the proxy allows it); `NEXTAUTH_SECRET` stable across
  deploys/redeploys (rotating it invalidates every session).

### 4.4 Money routes (priority)

- `donations/{slug}/verify`, `add-donation`, `gdcluster/donations/verify`,
  `premium/purchase`, `claim-*`, `scholarship`, `pastor-bonus`: authorize by
  session; if a 401 happens after an on-chain confirmation, the client must
  **re-authenticate and retry the verify** (idempotent); if the backend
  already recorded the hash it responds without duplicating.

### 4.5 Compatibility / migration

- During the transition `authenticateUser` accepts both paths (env flag
  `AUTH_SESSION_ONLY=1` to force session-only and measure dependencies).
- E2E/specs: fetch-based SIWE flows already keep cookies → keep passing; the
  ones sending only `token` (no cookie jar) are updated to session/cookie.
- Docs to sync: `doc/siwe-auth-flow.md` (drop "CSRF = API token"),
  `doc/wallet-auth.md`, `doc/api-security.md`, R-#224.

## 5. Acceptance criteria

- [ ] A donation/purchase signed on-chain is **never** left unregistered by a
      401: if the token is stale the session cookie authorizes (or the flow
      re-authenticates and retries) — verified on dev with two
      tabs/devices logging in with the same wallet
- [ ] `authenticateUser` authenticates by session cookie without touching
      `billetera_usuario.token`; the DB token stays only as fallback and is
      later removed
- [ ] CSRF no longer appears as a credential in URL/query/body of new calls
- [ ] Mutations reject `cross-site` (Origin/Sec-Fetch-Site) and pass
      same-origin
- [ ] Unit suite (app + engines), smoke and E2E green, including
      `donate-campaign-*`, `premium-course-checkout`, `profile-data`,
      `guide-claims` (the R-#224 family) without reintroducing RainbowKit
- [ ] `DEBUG_AUTH` gated, no PII in logs
- [ ] `NEXTAUTH_SECRET` single/stable and `SameSite=Lax` + `Secure` cookies
      verified in production

## 6. Verification plan

1. Repro of the current bug: wallet A logged in (tab 1) → same wallet logged in
   (tab 2) → donate from tab 1 → today 401; after the fix: OK via cookie.
2. `DEBUG_AUTH=1` on the 4+ specs of R-#224 and on the real-donation run.
3. `audit-api-auth.mjs` + `doc/api-security.md` updated to the new model.

---

## 7. Notes / References

- [issue #186 (wallet-auth)](https://github.com/pasosdeJesus/learn.tg/issues/186) /
  `doc/wallet-auth.md`: in-house connect implementation (RainbowKit out) —
  keep. *Note: #186 has no local REQ file; its content lives in
  `doc/wallet-auth.md`.*
- [issue #167](https://github.com/pasosdeJesus/learn.tg/issues/167) (session
  loss / partial login — OneKey + SIWE+NextAuth+RainbowKit) and
  [issue #168](https://github.com/pasosdeJesus/learn.tg/issues/168) (RainbowKit
  QR crash): context for not reintroducing the RainbowKit ecosystem.
- R-#224: the family of SIWE-401 specs to be closed with this change.
- OWASP CSRF Cheat Sheet; Auth.js session strategies; official SIWE+NextAuth
  example (nonce==CSRF pattern only at login).
