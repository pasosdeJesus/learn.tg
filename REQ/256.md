# R-#256: Offline course download (free and purchased)

**Link:** https://github.com/pasosdeJesus/learn.tg/issues/256 — create it with
`cd apps/nextjs && bin/m req:create "Offline course download"` if the number differs;
rename this file to the number the issue gets.

**Status:** Implemented (2026-09-22, branch `mvppwa`) — the per-course store, the
download button, `/offline` reading the store, the privacy rule for category B
courses, the cleanup on switch-off/disconnect/wallet deletion, the 24 h
revalidation and the server-side validation from the guide markdown are in code,
with unit tests for the store, the download and the answer parser, a component
test for the button, and an E2E spec (`offline-course-download`, skips itself until
the branch is deployed). The manual airplane-mode pass on the deployed build is
pending.
**E2E run (2026-09-24, dev site with `make prod`, branch `mvppwa`):** the full browser
suite reports **33 of 36 specs green** and `offline-course-download` **failed** on the
first pass and on both retries (stable, not timing). What passed with evidence: the SW
declares `ignoreVary`, the course was saved without pressing anything (5 guides), the
stored crossword carries no answers, the never-visited guide was read offline and, in
the first pass, the downloaded crossword opened. What fails: **the content of the
downloaded pages is not deterministic** — the page shell (header, navigation, title)
comes from the `learntg-pages` cache, but the guide markdown / the puzzle read from the
store do not always make it to the screen ("La guía sin conexión no mostró su
contenido" appeared on a retry, and the crossword reported 0 cells in every run).
Pending fix (blocking the merge): resolve the identity without the network (`useSession`
never settles offline, so `useAuthedApi.ready` can stay false) and paint the guide and
the puzzle **from the store independently of the fetch**, then leave the spec green in
three consecutive runs.
**Where to reproduce it** (measured 2026-09-24): this spec needs the **service worker**, so
it only runs against a production build (`make prod` on the dev site, or `make all` +
`bin/start` locally and `SITE_URL=http://localhost:4000`); `next dev` disables the caches.
The half that is "the content does not paint" is debuggable in this VM without any build: a
component test that mounts the guide page and the crossword page with the API mocked to
**fail** and `navigator.onLine = false` must show the guide and the puzzle from the store —
that is the fix this note asks for.
**Fixed (branch `mvppwa`):** the identity is resolved without the network and both pages
paint from the store:
- `lib/hooks/useAuthedApi.ts`: offline is treated as **resolved** (`identityPending` no
  longer waits for `status === 'loading'`), because `useSession()` cannot settle without a
  connection; waiting left `ready` false forever. The identity still comes from the local
  data (session address, in-app wallet, `localStorage` fallback).
- `lib/hooks/useCourse.ts`: offline it builds the course from the **downloaded record**
  (`courseFromDownloaded`) instead of attempting the catalog, and reads `navigator.onLine`
  synchronously so the first pass of its effect does not hit the network while
  `useOfflineStatus` is still updating. All the downloaded guides are listed, so a guide
  that was never opened online still resolves its number and route. It also **never drops
  the course already on screen**: a ref remembers that a course was loaded, and the
  offline/failure path only reports an error when there is nothing to show. Without that,
  the effect re-running when the device went offline (the session is not available then)
  emptied the course, the crossword lost its grid and the submit button stayed disabled —
  measured with the `offline-crossword` spec on a local dev server.
- **The identity fallback is stored in lowercase** (`lib/in-app-siwe.ts`,
  `components/ConnectWalletButton.tsx`): the cold-session fallback
  (`learn.tg.sessionAddress`) was written with the EIP-55 checksum in the in-app path and
  lowercase in the session path, so without a session the identity could flip between two
  spellings — which changes the `crossword-state-${address}` key and the authenticated
  requests. `doc/siwe-auth-flow.md` §3 already states that every address is stored and
  compared in lowercase.
- The guide page and the crossword page no longer gate on `ready` when offline
  (`if (!ready && !offlineNow) return`); the guide is read from the `guides` store and the
  puzzle from the downloaded record (`getStoredPuzzle`), and the puzzle page says it is
  missing instead of showing a connection error when it was never downloaded.
- **Tests (runnable without a build, which is what the note asked for):**
  `app/[lang]/[pathPrefix]/[pathSuffix]/__tests__/page.offline.test.tsx` (guide painted from
  the store with `ready: false` and `onLine = false`),
  `app/[lang]/[pathPrefix]/[pathSuffix]/test/__tests__/page.offline.test.tsx` (downloaded
  puzzle opens; missing puzzle is reported) and `lib/hooks/__tests__/useCourse.test.ts`
  (course from the downloaded record with no network call, and the online-failure fallback).
  `doc/pwa-developer-guide.md` documents the rule.
**Verified on the development site (2026-09-24, branch deployed):**
`offline-course-download` **0 failures** — the course is saved without pressing anything,
the record keeps all 5 guides, a never-visited guide is read offline from the saved copy
and **its downloaded crossword opens offline (37 cells)**. The spec needed one change to
get there: it now **signs in** before downloading, because `/api/crossword` requires a
session and an anonymous download stored an empty puzzle (and a stored puzzle without
clues is no longer stored at all). `offline-guide` is also green again (**4700
characters**, a free course: it used to measure the `/offline` fallback page), and
`offline-crossword` is green.
**§3.10 implemented (2026-09-24):** the record keeps the course presentation
(`subtitulo`, `resumenMd`) and the per-guide progress at download time, the course page
shows the dated snapshot (`offline-progress`) even without a connection, and the chain
actions (donations, UBI) are replaced by `offline-chain-actions` ("needs a connection") on
the course and guide pages. Covered by unit/component tests and by new assertions in
`offline-course-download` (the course page offline), which run on the next deploy.
**Correction on the first deployment (2026-09-25):** the E2E on the dev site showed the
offline **course** page failing to show `offline-progress` (timeout of 30 s) while the
guide page and the downloaded crossword were fine. Cause: the page decides with
`Number(course.porPagar) <= 0` (the catalog returns `null` for free courses, and the
tests already used `porPagar: null`), but `courseFromDownloaded` built the record with
`undefined` → `NaN` → the offline page considered the copy unreadable and the panel
returned `null` before painting anything. Fixed in `courseFromDownloaded`
(`porPagar: null`) and in the course page: the panel is also readable when the course came
from the device (`fromDevice`), because a stored copy is itself the proof of access
(`belongsToWallet` keeps it tied to the address) and without a network there is no way to
check the purchase again; for the same reason the dead "Buy this course" button is
replaced offline by `offline-purchased` ("Available offline"). Pinned by
`lib/hooks/__tests__/useCourse.test.ts` (`Number(porPagar)` is 0), two page tests and a
diagnostics dump in the spec's failure path.
**Remaining:** none in the criteria list; the manual airplane-mode pass on a phone stays as
the human check.
**Revisión 2026-09-25 (código y pruebas):** se verificó `courseFromDownloaded`
(`porPagar: null` para cursos gratuitos), `fromDevice` y el panel de progreso en
`lib/hooks/useCourse.ts`, y que los specs `offline-course-download`, `offline-guide` y
`offline-crossword` quedaron verdes en el sitio de desarrollo tras la corrección del
2026-09-25. **Cerrable**, salvo el paso manual en modo avión (humano).
**Prueba en Chrome móvil (operador, 2026-09-25) y correcciones:** con red, entrar a `/en`
**no** descargaba la biblioteca y solo se bajaba el curso que se abría; sin conexión el
índice del curso decía `guide 1`, `guide 2`… sin títulos y sin numeración. Causas y arreglos:
(1) `OfflineLibrarySync` no esperaba a que la identidad estuviera resuelta
(`useAuthedApi().ready`), así que las peticiones viajaban sin `walletAddress` y cada
`/api/guide` respondía 401: la sincronización completa fallaba en silencio. Ahora espera
(efecto con `ready` en las dependencias, botón deshabilitado mientras no esté listo), un
fallo parcial **no** sella la marca de 6 h (`offlineLibrarySyncedAt` solo se escribe si
`failed` está vacío) y un fallo total avisa con un toast en vez de callarse;
(2) el registro guarda el **título** de cada guía (`DownloadedGuide.titulo`, alimentado por
`guideTitles` del descriptor) y el índice del curso y el de `/offline` muestran
`1. <título>`, `2. <título>`…; una copia sin títulos se refresca en la siguiente
sincronización. Cubierto por `lib/__tests__/offline-course-download.test.ts`,
`lib/__tests__/offline-course-sync.test.ts`, `lib/hooks/__tests__/useCourse.test.ts` y
`components/__tests__/OfflineDownloadAll.test.tsx` (espera a la identidad, no sella con
fallos).
**Ahorro de datos visible (decisión del operador, 2026-09-25):** con el ahorro de datos del
navegador o una red 2G la sincronización automática **no** corre (bajar decenas de guías con
un plan limitado no es una decisión que la app deba tomar sola), pero el freno ya no es
silencioso: la lista de cursos lo explica (`connectionHold()` + aviso
`offline-connection-hold`, en inglés y español) y el enlace **Check now** sigue descargando
porque es una petición explícita del estudiante. El freno se relee al cambiar la conexión,
así que la sincronización automática se reanuda sola.
**Created:** 2026-09-21
**Type:** Feature / Offline (PWA)
**Priority:** High for the target audience — the students with the weakest connectivity
are the ones who most need the course material to be there before they go offline.
**Dependencies:** R-#240 (service worker, manifest, offline fallback), R-#242 (offline
crossword queue), R-#241 (guide cache), R-#244 (PWA + in-app wallet)

---

## 1. Objective

Let a learner **download a whole course** — its guides **and** its crosswords — so it is
readable and answerable offline **without having opened each page first**, refreshed
when the connection returns, and removable to free space:

- **Free courses**: anyone with the app can download them.
- **Purchased courses**: only while that wallet has access; another wallet on the same
  device must not see the content.
- The stored copy is revalidated at least **once every 24 hours** when online: if a
  guide's content or answers changed, the copy is refreshed and the user is told.

## 2. What exists today

- `lib/offline-guide-db.ts` caches the markdown of a **visited** guide (IndexedDB
  `learn-tg-offline` → `guides`) and `useCachedGuide` serves it when offline.
- `lib/offline-queue-db.ts` + `lib/hooks/useOfflineQueue.ts` queue submissions made
  offline and replay them (R-#242); `lastRejection` surfaces a server rejection.
- `lib/offline-catalog.ts` keeps the last course list seen and `/offline` lists the
  cached guides.
- The service worker (`next.config.ts`) caches visited pages (`/[en|es]/*`,
  NetworkFirst 24 h), `_next/static`, images and `api/*` GET (NetworkFirst 1 h); the
  `/offline` and `/[en|es]` shells are precached.
- **Missing**: an explicit per-course download, the crossword **without answers** for
  guides never fetched, the entitlement check per wallet, the revision/24 h
  revalidation, deletion, and the UI (size, "remove").

## 3. Design

1. **A per-course store.** New object store `courses` in `learn-tg-offline`:
   `{ key, courseId, lang, prefix, downloadedAt, revision, guides: [{ suffixRuta, markdown, puzzle }], bytes }`.
   `key` is `{lang}/{prefijoRuta}`; one record per course and language.
2. **Automatic sync on the course page** (`app/[lang]/[pathPrefix]/page.tsx`):
   opening the course with a connection saves it (or refreshes an outdated copy) and a
   notice counts the steps ("Saving guides and crosswords 3/12"), then reports the
   effect ("Course saved for offline reading" / "The course changed: the saved copy was
   refreshed"). There is no "Download for offline" button (operator decision,
   2026-09-23, after testing on an iPhone: the button was not enough); the status shows
   the size it takes and a "Remove download" link. It is only synced when the wallet may
   read the course (free, or purchased by this wallet).
   `navigator.storage.estimate()` is used to warn about space on cheap phones.
3. **The crossword payload carries no answers.** Downloading a puzzle must store the
   layout and the clues, **never** the solution: no `letter` in the grid cells and no
   `word` in the placements. Reuse the **existing** `GET /api/crossword`
   (`apps/nextjs/app/api/crossword/route.ts`, in the `app/api` submodule): it already
   resolves the guide (`getCourseIdByPrefix`/`getGuideIdBySuffix`) and returns cells
   with `letter: ''` and no answers, so the work is to make its parameters
   offline-friendly (`courseId`/`guide` or the prefix/suffix pair) and keep stripping
   the solution — **not** to create a second endpoint.
4. **Validation without `answer_fib`.** `POST /api/check-crossword` must accept a
   guide the user never fetched online: the answers are derived from the **guide
   markdown** (the `(answer)` of each numbered question, the same regex as the parser)
   instead of `billetera_usuario.answer_fib`, **without changing** the response for the
   online path. The reward rules (profile score ≥ 50, cooldown, vault balance) are
   untouched.
5. **Entitlement snapshot.** The record stores the wallet address and the purchase
   state at download time. On open, if the current wallet is not the one that
   downloaded a paid course, the content is not rendered (and the record can be
   dropped). "Disconnect" and "delete wallet" remove the downloaded paid content.
6. **Revalidation every 24 h.** Each course carries a `revision` (a hash of the
   guides' content served by the API). When the app opens with a connection and the
   copy is older than 24 h (or the revision differs), the copy is refreshed in the
   background and the user is told ("the course was updated"). Free courses are
   refreshed on any visit; paid ones only while the wallet still has access.
6b. **The privacy switch of R-#259 applies to the downloaded list too.** The
   `contenido_sensible` flag decides what the **device** shows: a downloaded
   category B course is not listed offline (nor in the `/offline` page) unless the
   learner enabled the category B switch, and "Disconnect"/"delete
   wallet" removes it. The risk is local, not public: a shared or inspected phone
   must not reveal the affiliation just by opening the app offline. Ties to R-#259
   §3.3 ("local stores") and to the whitepaper rule that the flag is honored by
   everything that lists courses.
7. **The `/offline` page and the course list read from the store**: a downloaded
   course lists its guides (not only the visited ones) and shows when it was last
   refreshed.
8. **Queue integration (R-#242):** an answer given offline for a downloaded guide is
   queued and replayed as today; the result of the review must reach the user (in-page
   message today; the `notifications` table variant is the pending half of R-#242).
9. **Storage hygiene:** "delete the app" and the browser's own eviction must not lose
   queued answers silently: pending submissions are kept in their own store and the
   download of a course never deletes them (R-#240 §4b item 9).
10. **The course presentation offline** (operator decision, 2026-09-23): the downloaded
    record also keeps the course **presentation** (title and introduction) and its
    **statistics as of the download**, with the date of that snapshot, so the course page
    is not an empty shell offline. What makes no sense offline are the actions that depend
    on the chain or on the session — donating (USDT/SLEARN/G$) and claiming UBI — so they
    are hidden or disabled with a one-line reason ("you need a connection") and come back
    when the connection returns.

## 4. Acceptance criteria

- [x] A free course can be downloaded from the course page and read offline, guides
      **and** crosswords, including guides the user **never opened online**
      (the downloaded HTML lives in the `guides` store, the document is warmed into
      `learntg-pages`; see `doc/pwa-developer-guide.md` §Downloaded courses).
- [x] The offline crossword payload carries no answers (unit-tested and checked by the
      E2E spec reading the IndexedDB record: `placements[].word === '-'`, no `letter`).
- [x] Offline the user fills the crossword and submits: the button queues the answer,
      says it is saved and that it will be reviewed when there is a connection, and
      nothing is validated locally (R-#242).
- [x] On reconnect the queue drains, `POST /api/check-crossword` validates it (also for
      a guide never fetched online, deriving the answers from the guide markdown and
      matching by clue: `packages/rewards/src/lib/guide-answers.ts`), and the user gets
      the result plus the guide/course progress.
- [x] A purchased course can be downloaded while that wallet has access; another wallet
      on the same device does not see it (`belongsToWallet`, `/offline` filters).
- [x] **Without a connection the copy does not expire** and **with a connection it is
      revalidated every 24 h**. Aclaración del operador (2026-09-24) sobre su nota del
      2026-09-23: quería decir que la copia no pierde validez sin conexión (ni a los 7 días
      ni nunca mientras no haya red); lo implementado eran un `REVALIDATION_MS` de 7 días y
      una caché de páginas de 7 días, que habrían dejado de servir una guía tras una semana
      desconectado. Ahora `REVALIDATION_MS` vuelve a **24 h** y las reglas de página,
      `_next/static` e imágenes en `next.config.ts` **no caducan por edad** (`maxEntries`
      sigue acotando; los chunks tienen que sobrevivir o la página guardada no se hidrata).
      Si una guía cambió, la copia se refresca cuando hay red y se le dice al usuario.
- [x] **Every guide the student can access ends up cached** (operator decision,
      2026-09-23, after testing: offline only the visited guide was there):
      `OfflineLibrarySync` (in the layout) downloads all free courses plus the paid ones
      this wallet bought, skipping current copies; the category B ones still need the
      R-#259 switch. It reports with a notice **only when something is really missing**
      ("Checking what is missing or out of date 1/12"), and the course list shows the
      count of saved courses plus a discreet **Check now** link (operator decision,
      2026-09-23: replace the button with a notice that shows the effect).
- [x] **The iPhone defects of the 2026-09-23 report are covered**: the guide pages are
      written into the `learntg-pages` cache directly (`warmPageCache`, which no longer
      needs the service worker to be controlling the page — that is what made a
      downloaded guide fall into `/offline` on iOS), and `next/image` requests
      (`/_next/image?url=…`) are cached, so the course list keeps its images offline.
      Verified by unit tests and the `offline-course-download` E2E spec.
- [x] Offline, a downloaded course still opens: the course page falls back to the
      stored record, the guide to the `guides` store and the **crossword** to the
      stored puzzle (`getStoredPuzzle`), so a downloaded puzzle no longer ends in
      "You are offline" (operator report, 2026-09-23).
- [x] **The Android defects of the 2026-09-23 report are covered**: with no connection the
      crossword page no longer asks the server (nor requires the wallet to be validated
      online) — it takes the downloaded puzzle straight from the device and the answer goes
      to the offline queue, which is what the operator asked for ("in offline mode the
      requirements from the wallet could be lowered at least with the in-app wallet"); when
      the puzzle of that guide was never downloaded the page **says so** instead of showing
      a connection error; and the global `ErrorBoundary` now logs the cause and shows it
      outside production, and resets on navigation (the operator only saw "Connection
      Error" and the app stayed trapped on that page). Covered by unit tests
      (`ErrorBoundary.test.tsx`, the crossword page suite).
- [x] Reconnecting anywhere drains the offline answer queue and tells the result
      (`components/OfflineQueueSync.tsx` in the layout), not only inside the crossword
      page (operator report, 2026-09-23).
- [x] Disconnect and "delete wallet" remove the downloaded paid courses
      (`clearPrivateCourseCopies()` in `WalletSelector`, `WalletEventListener`,
      `ConnectWalletButton`, `WalletDialog`).
- [x] A downloaded course with `contenido_sensible` is **not listed offline** unless the
      learner enabled the visibility switch (R-#259), and turning that switch off
      removes the copy: the device does not reveal the affiliation on its own.
- [x] Downloaded paid content is **not** readable from the generic HTTP cache only:
      the explicit store is the durable copy and it is deletable.
- [x] The course page shows the space it takes, lets the user remove the download, and
      warns when the device is running out of space.
- [x] Unit tests for the store, the download, the revision comparison and the answer
      parser; component test for the download button; E2E spec
      `offline-course-download` (download a free course → airplane mode → open a guide
      never visited online → check the stored puzzle has no answers).
- [x] `make test-e2e-offline` keeps passing on a production build: green on the
      2026-09-21/22 dev site (`offline-guide` 5/5, `offline-crossword` and
      `offline-course-download` skip themselves with a reason until this branch is
      deployed there).
- [x] **Offline, the course page shows its presentation and the statistics as of the
      download** (from the downloaded record, with the date of that snapshot), and the
      actions that need the chain or the session — donations and the UBI claim — are hidden
      or disabled with a one-line reason and come back with the connection
      (§3 item 10). Implementado el 2026-09-24: el registro guarda `subtitulo` y `resumenMd`
      (presentación) y el avance de cada guía al descargar (`guideStatus` →
      `DownloadedGuide.completed/receivedScholarship/receivedSlearnScholarship`);
      `useCourse` los expone (`fromDevice`, `downloadedAt`) y la página los pinta, con el
      avance fechado en el panel (`data-testid="offline-progress"`). Sin conexión, el bloque
      de donación y los botones de GoodDollar/UBI se sustituyen por
      `data-testid="offline-chain-actions"` ("necesitas conexión"). Cubierto por
      `lib/__tests__/offline-course-download.test.ts`,
      `lib/hooks/__tests__/useCourse.test.ts`,
      `components/__tests__/OfflineCourseDownload.test.tsx` (avance fechado) y
      `app/[lang]/[pathPrefix]/[pathSuffix]/__tests__/page.offline.test.tsx` (el botón de UBI
      se reemplaza por el motivo); el E2E `offline-course-download` comprueba la página del
      curso sin conexión (avance + no vacía + motivo de donar/UBI).

## 4b. Decisions taken while implementing

**Verified against the deployed dev site (2026-09-22)** with
`make test-e2e-spec SPEC=offline-course-download`: **7/7, 0 failures** — the course
downloads, the record keeps all 5 guides (not only the visited ones), the stored
crossword carries **no answers** (read straight from IndexedDB), a never-visited
guide keeps its content and **that guide opens with the network off** ("Showing the
saved copy"). The `Vary` obstacle was real: Next's HTML carries
`Vary: rsc, next-router-state-tree, …`, so the `/(en|es)/*` rule needs
`matchOptions: { ignoreVary: true }` (added to `next.config.ts`, deployed, and the
spec checks the deployed `/sw.js` for it before asserting).

**Operator test (2026-09-23, `/var/www/adJ-ia/tmp/ru.txt`) and what it changed.** Three
real gaps, all fixed in this branch:

1. Offline, a **visited** course still could not be entered and a **downloaded puzzle**
   fell into "You are offline": the course page needed `/api/course-catalog` and the
   puzzle page needed `/api/crossword`. Both now fall back to the stored copy
   (`useCourse`, `getStoredPuzzle`), and the download warms **both** documents of each
   guide (`$suffix` and `$suffix/test`).
2. Only the visited guide was on the device: the app now **syncs every accessible
   course** on its own while online, with a visible control and progress.
3. After reconnecting from the course list nothing was drained or announced: the queue
   now drains in the layout on any page and reports the result with a toast.

Two unrelated findings from the same report are **not** R-#256 and stay open: unlocking
the in-app wallet while offline ends in "Failed to fetch" (the unlock path needs the
network), and OKX web3 wallet cannot pick country/religion/interview date in the profile
(Chrome and OneKey do; the OKX provider behaves differently).

- The **guide content** is not duplicated into the `courses` record: it stays in the
  existing `guides` store (one copy per guide, and `useCachedGuide` reads it without
  changes). The record keeps the guide list, the puzzles and the metadata.
- **Downloading warms the page cache** (`warmPageCache`). Without it, offline
  navigation to a never-visited guide would land on the `/offline` fallback even
  though the content is stored. Warming is not enough on its own: the `Vary` header
  of Next's HTML breaks the cache match, which is what `ignoreVary` fixes.
- The server **prefers the markdown-derived answers whenever every placement's clue
  matches a question**, falling back to `answer_fib` otherwise. This fixes a
  pre-existing bug (a stale `answer_fib` from another guide marked correct answers
  wrong) and is what makes an offline answer verifiable. If neither source is
  consistent with the placements, the route answers `422` with
  `invalidPuzzle` instead of marking the answer wrong.

## 5. Out of scope

- Downloading the whole platform or content the user has no access to.
- Video/audio lessons and their offline storage.
- Background sync while the app is closed (iOS limits it; the revalidation happens when
  the app is opened with a connection).
- Changing the reward rules or the scholarship formula.
- The accounting tool of the business course (that is R-#258) and the per-course
  entitlement model beyond the snapshot above.

## 6. Related

- R-#240 §4b (origin of this scope), R-#241, R-#242, R-#244
- `doc/pwa-developer-guide.md` (service worker and cache table),
  `doc/pdj-wallet-testing.md` §5 (offline tests), `doc/api-security.md` (new routes must
  authenticate)
