# R-#244: MVP — PWA with in-app wallet

**Link:** https://github.com/pasosdeJesus/learn.tg/issues/244

**Status:** In progress (2026-09-15, branch `mvppwa`) — the two packages, the
learn.tg integration and the PWA shell are implemented, and the app unit suite
plus `typecheck` are green. Only the build/manual verification (step 5) is
pending, because it needs the branch deployed to the dev site (which serves
`main`). Measured evidence: [§10](#10-session-log-2026-09-15).
**Created:** 2026-09-15
**Type:** Feature / MVP
**Priority:** Highest
**Dependencies:** R-#236, R-#237, R-#238, R-#240
**Index:** see the [REQ map](#index-of-the-pwa--in-app-wallet-requirement-set) at the end

---

## 1. Objective

Deliver a minimum viable PWA with an in-app wallet, working on the dev site
(`https://learn.tg:9001`). This is a subset of R-#236 to R-#243, focused on a
demonstrable end-to-end flow.

## 2. Scope (MVP)

Legend: ✅ done and verified · 🟡 done in part · ⬜ not started.

**Included, from R-#236 (core package) — ✅ `packages/pdj-wallet`:**

- `createWallet`, `importWallet`, `unlockWallet`, `lockWallet`, `deleteWallet`,
  `hasWallet`, `getWalletInfo` — ✅ implemented.
- `signMessage`, `signSIWE` — ✅ implemented (the SIWE path is what the MVP
  uses).
- `IndexedDBStorage`, `MemoryStorage` — ✅ implemented.
- `getInAppWalletProvider` (EIP-1193) — ✅ implemented.
- `signTransaction`, `signTypedData` — ✅ implemented and exported by the core
  (`src/index.ts`), but **not exercised by the MVP**: the demo only signs in,
  it never broadcasts a transaction. They are out of the MVP's acceptance
  criteria, not out of the library.

**Included, from R-#237 (Next package) — ✅ `packages/pdj-wallet-next`:**

- `useInAppWallet`, `InAppWalletSetup`, `InAppWalletUnlock` — ✅ implemented
  (with `en`/`es` i18n).

**Included, from R-#238 (learn.tg integration) — ✅ wired:**

- `WalletSelector` in learn.tg — ✅ created as
  `components/WalletSelector.tsx` and mounted by `components/Header.tsx:136`
  (the external path still delegates to `ConnectWalletButton`).
- `useAuthAddress` extended — ✅ done (`inAppAddress`, `isInAppUnlocked`,
  address precedence `sessionAddress || inAppAddress || storedAddress`).
- `lib/hooks/useWalletProvider.ts` — ✅ new: the effective EIP-1193 provider
  (in-app when unlocked, otherwise `window.ethereum`).
- `usePublicClient` / `useWalletClient` (`lib/hooks/useWallet.ts`) and
  `useWriteContract` (`lib/hooks/useWriteContract.ts`) — ✅ rewired to
  `useWalletProvider`.
- `Header` updated — ✅ done.

**Included, from R-#240 (service worker) — ✅ implemented in code:**

- `next-pwa` enabled for learn.tg (`disable: false`) with page/API caches and
  `fallbacks.document = /offline` (`next.config.ts`). It was already a
  dependency (`next-pwa@^5.6.0`) and already wrapped by `withPWA`.
- A learn.tg manifest linked from the root layout: `public/manifest.webmanifest`
  (`start_url` `/`, icons in `public/icons/learntg-*`) is referenced from
  `app/layout.tsx` metadata, while `public/manifest.json` (diligent-records,
  linked only from `app/[lang]/diligent-records/page.tsx:83-86`) stays untouched.
- `app/offline/page.tsx` (route `/offline`), `lib/hooks/useOfflineStatus.ts` and
  `components/OfflineBanner.tsx` (mounted in `components/Layout.tsx`) — ✅ done,
  with 6 unit tests.
- Remaining: the build/manual verification of the PWA (install, offline
  reload), which needs the branch deployed (step 5).

**Excluded (post-MVP):**

- `exportMnemonic`, `exportPrivateKey`.
- `FileStorage` (the core ships only `MemoryStorage` + `IndexedDBStorage`).
- Offline submission queue (R-#242).
- Offline progress sync (R-#241).
- Install prompt and documentation (R-#243).
- Full E2E migration to the wallet core (R-#239).

## 3. Ordered implementation checklist (this branch)

| # | Step | Deliverable | Est. | Status |
|---|------|-------------|------|--------|
| 1 | `packages/pdj-wallet` core (R-#236) | `createWallet` / `unlockWallet` / `signSIWE` + `MemoryStorage` / `IndexedDBStorage` + unit tests | 2-3 h | ✅ 18 passed / 1 skipped in 8.2 s |
| 2 | `packages/pdj-wallet-next` (R-#237) | `useInAppWallet` + `InAppWalletSetup` / `InAppWalletUnlock` | 1 h | ✅ 14 passed in 7.2 s |
| 3 | learn.tg integration (R-#238) | `WalletSelector` in `Header`, `useAuthAddress` and wallet hooks extended | 1 h | ✅ `WalletSelector` mounted in `Header`; app unit suite + `typecheck` green (§10) |
| 4 | PWA shell (R-#240) | `next-pwa` enabled, learn.tg manifest, `/offline`, `OfflineBanner` | 1 h | 🟡 implemented in code (+6 unit tests); build/manual verification pending |
| 5 | MVP verification (this REQ) | demo flow of §5 + `make test` + `typecheck` | 30 min | ⬜ pending |

## 4. Commands that actually work in this repo

The two new packages each ship their own `vitest.config.ts` (a plain object with
an alias map onto `apps/nextjs/node_modules`), which is exactly what `rewards`
and `gdcluster` still lack. Run them from the app, which owns the dependency
graph:

```bash
cd apps/nextjs

# Core wallet — verified 2026-09-15: 18 passed, 1 skipped, 8.2 s
./node_modules/.bin/vitest run --root ../../packages/pdj-wallet \
  --config ../../packages/pdj-wallet/vitest.config.ts

# Next.js package — verified 2026-09-15: 14 passed, 7.2 s
./node_modules/.bin/vitest run --root ../../packages/pdj-wallet-next \
  --config ../../packages/pdj-wallet-next/vitest.config.ts

# learn.tg integration tests
make test-hooks test-components

# Full unit suite
make test

# Minimal E2E (once the spec exists, see R-#245)
CHROME_PATH=/usr/local/bin/chrome make test-e2e-spec SPEC=in-app-wallet
```

Fixed and measured on 2026-09-15 (branch `mvppwa`):

- Aliases for both linked packages were added to `apps/nextjs/vitest.config.ts`
  (`@learn-tg/pdj-wallet-next` first, then `@learn-tg/pdj-wallet`, which must
  stay in that order because Vite matches aliases by prefix), so the app suite
  resolves the linked package again: `make test-hooks` is **60 passed /
  2 skipped** (it was 1 failed suite).
- The `useAuthAddress` mocks in `lib/hooks/__tests__/useGuideData.test.ts` now
  carry `inAppAddress` and `isInAppUnlocked`, so
  `tsc --noEmit -p tsconfig.test.json` is clean.

Remaining caveats:

- `pnpm test` inside a package still goes through `pnpm`, which corepack
  resolves to 11 here (the repo is pinned to pnpm 10). The package `test`
  scripts call `../../apps/nextjs/node_modules/.bin/vitest` directly, so the
  same command works without pnpm.
- `packages/rewards` and `packages/gdcluster` still have no `vitest.config.ts`
  and their `package.json` `test` script points to a non-existent file.
- The E2E suites run against the **dev site, which serves `main`**, so they
  validate the branch only after it is deployed (§10).

## 5. Demo flow (acceptance)

Not runnable yet: steps 4-6 need the `Header` wiring (§3 step 3) and steps 2,
8 and 9 need the PWA shell (§3 step 4).

1. Open `https://learn.tg:9001` in Chrome mobile.
2. Install the PWA (Add to Home Screen).
3. Open the PWA.
4. Tap "Use in-app wallet".
5. Create a wallet with a PIN.
6. Sign in via SIWE (in-app wallet).
7. Navigate to `/en/gdcluster/guide1`.
8. Turn off Wi-Fi and mobile data.
9. Reload: guide 1 is still visible.
10. Turn the connection back on.

## 6. Acceptance criteria

- [ ] The PWA installs on Chrome mobile.
- [ ] The user can create an in-app wallet.
- [ ] The user can sign in via SIWE with the in-app wallet.
- [ ] Guide 1 is readable offline.
- [x] `make test` passes (2026-09-15): `test-lib` 169 passed / 1 skipped,
  `test-hooks` 74 / 2, `test-api` 172, `test-components` 120 / 3,
  `test-pages` 45, `test-db` 3 → **583 passed / 6 skipped, exit 0**.
- [x] `pnpm typecheck` passes (`tsc --noEmit` and
  `tsc --noEmit -p tsconfig.test.json`, both clean).
- [ ] The demo works on `https://learn.tg:9001`.

The four unchecked items need the branch **deployed to the dev site** (which
serves `main`) plus the operator's build window; they are the goal of step 5.

Already satisfied (measured 2026-09-15):

- [x] `packages/pdj-wallet` unit suite: 18 passed / 1 skipped in 8.2 s.
- [x] `packages/pdj-wallet-next` unit suite: 14 passed in 7.2 s.
- [x] Core package `tsc -b` and Next package `tsc -b` clean.
- [x] Both packages linked into `apps/nextjs`
  (`node_modules/@learn-tg/pdj-wallet*`), declared in `package.json:82-83` and
  built by `make engines-dist` (`Makefile:19`) and `bin/dev` (`bin/dev:21`).

## 7. Risks

- **`next-pwa` with the Next.js 16 App Router**: it generates the worker fine, but
  its **auto-registration does not work** in the App Router (it injects
  `register.js` into the webpack `main.js` entry, which does not exist there, and
  the injection is skipped silently). Confirmed on the dev site 2026-09-15:
  `/sw.js` 200, `getRegistrations()` empty, no registration code in any client
  chunk, while a manual `register('/sw.js')` worked. Mitigation applied:
  `components/ServiceWorkerRegistrar.tsx` registers it from the app (verified in
  Chrome: 1 registration, activated, controlled). `serwist` remains the
  alternative (R-#242) if next-pwa breaks with a future Next version.
- **Manifest collision**: `public/manifest.json` is diligent-records'
  (`start_url` `/en/diligent-records`, linked only from
  `app/[lang]/diligent-records/page.tsx:83-86`). Mitigation: learn.tg now has
  `public/manifest.webmanifest` linked from the root layout; the diligent one is
  untouched.
- **The service worker caches API GET responses for 1 h** (R-#240 §2). They are
  wallet-scoped URLs, but the data stays in the device cache; review before
  production (R-#243) and consider `NetworkOnly` for the sensitive endpoints.
- **~~The app test suite cannot resolve the linked package~~** — fixed: the
  `@learn-tg/pdj-wallet*` aliases are in `apps/nextjs/vitest.config.ts` and the
  `useGuideData` mocks carry the new fields (§4, §10).
- **IndexedDB unavailable** in some browsers. Mitigation: fall back to
  `MemoryStorage` (non-persistent). Note that `fake-indexeddb` is declared in
  `packages/pdj-wallet/package.json` devDependencies but is **not installed**, so
  `src/__tests__/indexeddb.test.ts` self-skips today (1 skipped).
- **Wallet creation slow on low-end devices**: PBKDF2 runs 600,000 iterations.
  Mitigation: show progress.

## 8. Related

- Dependencies: R-#236, R-#237, R-#238, R-#240.
- Post-MVP: R-#239, R-#241, R-#242, R-#243.
- Testing plan: R-#245.

## 9. Testing plan (MVP)

**Unit tests (already green):**

```bash
cd apps/nextjs && ./node_modules/.bin/vitest run \
  --root ../../packages/pdj-wallet --config ../../packages/pdj-wallet/vitest.config.ts
./node_modules/.bin/vitest run \
  --root ../../packages/pdj-wallet-next --config ../../packages/pdj-wallet-next/vitest.config.ts
make test-hooks test-components   # green since the aliases were added (§4)
```

**Minimal E2E spec:** `e2e/specs/in-app-wallet.spec.mjs` (see R-#245) — creates a
wallet in `/en/test/wallet`, unlocks it, signs SIWE and verifies the session
cookie. Written; it **skips** until the branch is deployed, because the dev site
serves `main`.

**Manual demo:** documented in `doc/pdj-wallet-testing.md` (the single doc used
by R-#245 §7; it also covers the manual PWA steps: service worker, manifest,
installability and offline navigation), following §5. Written.

**Time:** seconds (unit) + ~30 s (E2E) + minutes (manual). **Memory:** <1 GB.

---

## Index of the PWA / in-app wallet requirement set

| # | Title | Depends on | Status (2026-09-15) |
|---|-------|------------|---------------------|
| R-#236 | `packages/pdj-wallet` — core wallet | — | ✅ MVP subset implemented |
| R-#237 | `packages/pdj-wallet-next` — Next.js hooks and components | R-#236 | ✅ MVP subset implemented |
| R-#238 | Integrate the in-app wallet in learn.tg | R-#236, R-#237 | 🟡 wired; `WalletEventListener` lock handling pending |
| R-#239 | Use the `pdj-wallet` core in E2E | R-#236 | ⬜ pending |
| R-#240 | Service worker + manifest + offline fallback | — | 🟡 code done; build/manual verification pending |
| R-#241 | Offline guide reading | R-#240 | 🟡 visited-guide cache done; offline progress queue pending |
| R-#242 | Offline crossword | R-#238, R-#241 | ⬜ pending |
| R-#243 | PWA complete (installable, documentation) | R-#240 | 🟡 install prompt + docs done; install verification pending |
| R-#244 | MVP — PWA with in-app wallet (this REQ) | R-#236, R-#237, R-#238, R-#240 | 🟡 steps 1-4 coded; step 5 (build/manual) pending |
| R-#245 | Testing plan for the `pdj-wallet` packages | R-#236, R-#237 | 🟡 units + docs done; spec skips until deploy |

**Implementation order for the MVP:**

1. R-#236 (core) — 2-3 h. ✅
2. R-#237 (Next.js) — 1 h. ✅
3. R-#238 (integration) — 1 h. ✅
4. R-#240 (service worker) — 1 h. ✅ code (build/manual pending)
5. R-#244 (MVP verification) — 30 min. ⬜ (needs a deploy)

**Total:** ~6 h.

## Notes

1. In learn.tg the package names are `@learn-tg/pdj-wallet` and
   `@learn-tg/pdj-wallet-next`; graduation to `@pasosdejesus/*` inside `m` is
   documented in those REQs.
2. `pdj-wallet` contains the whole core; this REQ documents the subset used by
   the MVP.
3. `next-pwa` for the MVP; `serwist` is evaluated in R-#242.
4. The EIP-1193 provider is the key piece: it lets `usePublicClient`,
   `useWalletClient` and `useWriteContract` work unchanged (or nearly so) when
   the in-app wallet is unlocked.

---

## 10. Session log (2026-09-15)

Branch `mvppwa`, with R-#227 / R-#233 Phase 2 already in place: the NextAuth
session cookie is the only credential (no API token anywhere) and the dev site
(`https://learn.tg:9001`) runs with **Rails stopped**, so every sign-in path
below must be the session cookie.

**Done in this session:**

1. `packages/pdj-wallet` — core, framework-agnostic (AES-256-GCM + PBKDF2
   600k), with `MemoryStorage` / `IndexedDBStorage` and the EIP-1193 provider.
2. `packages/pdj-wallet-next` — `useInAppWallet`, `InAppWalletSetup`,
   `InAppWalletUnlock`, `en`/`es` i18n.
3. learn.tg wiring: both packages linked into `apps/nextjs`
   (`node_modules/@learn-tg/pdj-wallet*`), declared in `package.json:82-83`,
   built by `make engines-dist` (`Makefile:19`) and `bin/dev` (`bin/dev:21`);
   `lib/hooks/useWalletProvider.ts` created and `useWallet.ts` /
   `useWriteContract.ts` / `useAuthAddress.ts` rewired;
   `components/WalletSelector.tsx` created.

**Measured results (final, 2026-09-15):**

| Check | Command | Result |
|-------|---------|--------|
| Core package | `vitest run --root ../../packages/pdj-wallet` | ✅ 18 passed / 1 skipped, 8.2 s |
| Next package | `vitest run --root ../../packages/pdj-wallet-next` | ✅ 14 passed, 7.2 s |
| Package builds | `tsc -b` in both packages | ✅ clean |
| App unit suite | `make test` | ✅ 583 passed / 6 skipped, exit 0 (`test-lib` 169/1, `test-hooks` 74/2, `test-api` 172, `test-components` 120/3, `test-pages` 45, `test-db` 3) |
| Types | `pnpm typecheck` | ✅ both `tsc --noEmit` and `tsc --noEmit -p tsconfig.test.json` clean |
| New unit tests | `useOfflineStatus` (3), `OfflineBanner` (3), in-app lock in `WalletEventListener` (3), `useCachedGuide` (6), `offline-guide-db` (4), `offline-queue-db` (5), `useOfflineQueue` (5), `InstallPrompt` (5), `ServiceWorkerRegistrar` (7), `WalletDialog` (7), `WalletSelector` (6) | ✅ 60 passed |
| Wallet MVP on the **dev site** (`mvppwa` deployed 2026-09-15) | `make test-e2e-spec SPEC=in-app-wallet` | ✅ 0 failures (17.7 s): wallet created, persisted/locked after reload, unlocked with the PIN, SIWE session cookie with that address, `GET /api/profile` 200 |
| PWA assets on the **dev site** | `curl`/fetch | ✅ `/sw.js` 200 JS, `/manifest.webmanifest` 200 `application/manifest+json`, `/offline` 200, icons 200, `/en/test/wallet` 200 |
| Worker **registration** | Chrome against the app | ✅ 1 registration, activated, page controlled (was: 0 — next-pwa's auto-register does not work in the App Router, see §7) |

**Next actions:** the wallet half of step 5 is verified on the dev site
(`in-app-wallet` spec, 0 failures). The PWA half must be verified on a
**production build** (the site after deploying this branch, or a dev instance
served with `make all` + `bin/start`): next-pwa disables cache and precache in
`next dev`, and the PWA is now off in development by design (R-#240 §2), so
installability and offline cannot be tested on a dev server. Then install the app
on Chrome mobile and check the offline reload. Everything else is implemented and
green.

**E2E status of the dev site** (these runs were made while it still served `main`;
since 2026-09-15 it serves this branch. Rails stopped):

- Full run 2026-09-15 09:29-10:10: **smokes 20/20** ✅ (`referral-premium`
  passed this time: its earlier `esperado 0/0` failure was the intermittent
  `distribution` payload, not the reward) and **browser 27/30**.
- The three browser failures were then fixed and re-verified one by one
  (**all now 0 failures**):
  1. `verified-city-gate` (22.9 s) and `interview-date` (126.2 s): both 401'd on
     authenticated API calls because they authenticated with `walletAddress`
     only and never sent the NextAuth **session cookie** (R-#233 Phase 2
     leftover). Fixed by threading the cookie (`apiPatch`/`apiGet` now take it).
  2. `pastor-journey` (154.4 s): the 44 SLEARN bonus could not be paid because
     the dev **churches fund had 40.08 SLEARN**. The spec now tops it up from the
     local test wallet first (`ensureChurchesFund`, "Funded churches fund with
     19.92 SLEARN, tx 0x7460078c…") and the run ends with score 93 > 90.
- `ux-mobile-menu` and `donate-course-real`, which failed in the previous run,
  passed in this one, confirming both were load flakes.
- Second full run (11:52-12:43), with the fixes in the tree: **smokes 18/20** and
  **browser 28/31**. The three fixed specs passed **inside the suite**
  (`verified-city-gate`, `interview-date` 125 s, `pastor-journey` 130 s, the last
  one topping up the fund itself). `e2e/specs/in-app-wallet.spec.mjs` was picked
  up and skipped cleanly (page not deployed). Remaining failures, all
  environmental/timing and none from this branch:
  1. `referral-premium`: the fund was at 16.93 SLEARN, so no 44 SLEARN bonus and
     the pastor's `transfer` reverted (`0xe450d38c`, insufficient balance). Fixed
     the same way as `pastor-journey` (`ensureChurchesFund`); re-run alone it
     **passes** (fund topped up) but then **skips** on the dev claim rate-limit
     (10/day per IP, exhausted by the day's runs).
  2. `celo-claim`: nginx **502** while downloading `/`, the missional course and
     `/en/web3-and-ubi/guide3` (dev server busy/cold). Run `bin/warmup.mjs`
     before the suite.
  3. `nav-session-diag`: `Execution context was destroyed, most likely because of
     a navigation` (Puppeteer race; it passed earlier the same day).
  4. `gas-insufficient-panel`: the Donate button never appeared on
     `/en/gdcluster/ranking` in 24 s (the spec already retries 12 times; ranking
     data/cold load).
  5. `admin-dashboard`: `FLICKER DETECTED: 3/3` (in the earlier run it was 502s on
     the admin endpoints).
- `smoke/rails-health.spec.mjs` is skipped by design (`RAILS_CHECK=1` enforces
  it), which is expected while Rails is stopped.

