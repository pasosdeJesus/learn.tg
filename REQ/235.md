# R-#235: Standard authenticated-API mechanism (one robust path for every page)

**Link:** https://github.com/pasosdeJesus/learn.tg/issues/235

**Status:** ✅ Implemented (2026-09-15) — the hook and every app page/consumer are migrated. Verification: `pnpm vitest` green (**529 passed / 6 skipped**), `bin/audit-api-auth.mjs` **54 passed / 0 failed**, `pnpm typecheck` clean, and E2E `fresh-wallet-first-connect` **green on the dev site** (0 anonymous `/api/scholarship` calls; all scoped to the connected wallet). `components/ConnectWalletButton.tsx` is no longer a legacy-token holder: R-#233 Phase 2 removed the `GET /api/auth/token` fetch and the `learn.tg.authToken` write, so it now only calls the auth endpoints (csrf/callback) and stores `sessionAddress`. Still **outside** the hook, by design (documented, low impact): `lib/admin-fetch.ts` (a plain module, not a component) and `components/shared/PhotoUpload.tsx` / `components/shared/ChurchSelector.tsx` (param-passing paths), which build the `walletAddress` hint from `localStorage.sessionAddress` directly, without `ready`/`mismatch`. Remaining: the full E2E suite.

### Regression found and fixed during E2E (2026-09-14)

The first E2E run after the migration **failed**: `ready` was `true` on the very
first client render — before mount (localStorage is only readable after mount,
R-#218) and while `useSession()` was still `status: 'loading'` — so the home page
fired anonymous `/api/scholarship` calls before the wallet resolved
(`control=2, repro=3` anonymous), i.e. the exact false "cooldown"/0% bug.

Fix in `useAuthedApi.ts`:

- `identityPending = !mounted || (status === 'loading' && !storedAddress)`
- `ready = !mismatch && !identityPending && (!!wallet || !connectedHint)`

So every page now waits for the identity to resolve before querying. Re-run:
`/api/scholarship` 3 calls per phase, **0 anonymous**, no false cooldown
(`e2e/specs/fresh-wallet-first-connect.spec.mjs`).
**Created:** 2026-09-14
**Type:** Refactor / Reliability
**Related:** R-#233 (https://github.com/pasosdeJesus/learn.tg/issues/233) — this is the client-side part that lets the API token be retired.

---

## 1. Objective

Give **every page and component one single, standard mechanism** for
authenticated, same-origin API calls, replacing the ad-hoc per-page
`walletAddress`/`token` query building and the cold-session workarounds. The
mechanism must be robust for real users **without environment or test hacks**
(a real user does not have `MOCK_COURSE_LIST` or any special flag).

## 2. The mechanism

`lib/hooks/useAuthedApi.ts`:

- **Identity**: the NextAuth session address, with the `localStorage` fallback
  for the cold-session bug (https://github.com/pasosdeJesus/learn.tg/issues/208,
  NextAuth #5719), via `useAuthAddress`.
- **`ready`**: false while a connected user's address has not resolved yet, so
  pages **wait** instead of firing an anonymous request (the cause of the false
  "cooldown" and 0% progress).
- **`mismatch`**: detects a partial login (session vs localStorage fallback
  point to different wallets) so pages stop instead of mixing state.
- **`withWallet`**: appends only the identity hint (`walletAddress`) to a URL.
- **`authedGet` / `authedPost` / `authedPatch` / `authedDelete`**: same-origin
  requests that authorize through the **session cookie**. Mutations add
  `walletAddress` to both the query and the body (routes read it from either
  place). **No token is ever sent by the app.**

Rationale: the session cookie is the credential (R-#227/R-#233); the token was
only a legacy fallback for non-browser clients. Dropping it from the client
removes R-#227 OWASP deviations 1 (credential in URL) and 2 (credential in
`localStorage`).

## 3. Migrated (2026-09-14)

- `app/[lang]/page.tsx` (course list)
- `app/[lang]/[pathPrefix]/page.tsx` (course detail)
- `app/[lang]/[pathPrefix]/[pathSuffix]/page.tsx` (guide)
- `app/[lang]/[pathPrefix]/[pathSuffix]/test/page.tsx` (crossword)
- `app/[lang]/profile/page.tsx`
- `app/[lang]/cluster/page.tsx`
- `app/[lang]/ref/[code]/page.tsx`
- `lib/hooks/useCourse.ts`, `lib/hooks/useScholarshipData.ts`
- `lib/admin-fetch.ts`
- Components: `NotificationsBell`, `UserLanguageSync`, `CeloUbiButton`,
  `DeleteVerifiedDataDialog`, `CheckoutModal`, `DonateModal`,
  `VerificationScheduler`, `shared/ChurchSelector`, `shared/PhotoUpload`,
  `ReferralProfileSection`.

Removed the `MOCK_COURSE_LIST` E2E helper (the course list is served by Next,
same-origin, so no port/mock is needed).

## 4. Pending

- [x] Retire the legacy token that `ConnectWalletButton` / `WalletEventListener`
      used to fetch and persist — done in R-#233 Phase 2 (2026-09-15): neither
      component touches `learn.tg.authToken` anymore.
- [x] Migrate the non-browser E2E specs to the session cookie — done in R-#233
      Phase 2: they send only `walletAddress`, and Node-side calls send the
      browser's `Cookie` header.
- Re-run the full E2E suite (dev + production) after deploying.

## 5. Verification

- `pnpm typecheck`: OK.
- Unit suite: **529 passed / 6 skipped** (`make test`, all six sub-targets green).
- `bin/audit-api-auth.mjs`: **54 passed / 0 failed**.
- Engine suites: rewards **56/56**, gdcluster **91/91**.
- E2E: `fresh-wallet-first-connect` green on dev (0 anonymous `/api/scholarship`
  calls) and `full-flow` Step 10 now fills the crossword (URL fix). The full
  suite runs after the deploy.

## 6. Rails-free runtime (2026-09-15)

This mechanism is what lets the public site run **without Rails**: every page and
component builds same-origin requests (`/api/...`) authorized by the NextAuth
session cookie, so there is no cross-origin dependency, no token, and no Rails
session. The only remaining Rails touch points in the repo are
`apps/.env.example` and the `rails-*` smoke specs (not app code). Full evidence,
conditions and follow-ups live in
[R-#233 §8](https://github.com/pasosdeJesus/learn.tg/issues/233); the
verification with Rails stopped on dev is recorded in §5.
