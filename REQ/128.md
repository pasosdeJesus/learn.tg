# R-#128: Premium Course Payments with Dynamic Pricing

Enable users to purchase premium courses using the already-deployed `SLEARN.processPayment()` function (from I129). Add dynamic pricing based on user's country (HDI). No contract changes needed — all logic is already in SLEARN.sol from I129.

**Depends on:** I129 (SLEARN token launch, contracts deployed and operational), #155 (ClusterFunds for GD course), #196 (LearnTGVaultsV5 for scholarship split)

**Status (2026-09-21, revisado):** los ítems de UI de §2 y el asiento contable de §9 están implementados y
**verificados en local**: `make type` (0 errores, fuente + pruebas), la suite completa `make test`
(**1006 passed / 6 skipped, 143 archivos, 0 fallas**) y `node bin/audit-api-auth.mjs`
(**54 passed / 0 failed**, 0 advisories nuevos). Tras el arreglo de unidades de abajo se repitieron `make type`,
`make test-api` (220) y `make test-components` (174 passed / 3 skipped): verdes. Lo nuevo de esta rama: precio junto
al botón Buy (§2.1), "Mis cursos premium" en el perfil (§2.3, `components/PremiumCoursesSection.tsx` + 5 pruebas),
los flujos de SLEARN de los pagos en el panel público (`premiumSlearn`) y la contabilidad por curso en el panel de
administración (`GET /api/admin/premium-purchases` + widget, §9), pruebas de ruta para
`courses/premium/{price,mine}` y `courses/[courseId]/access` (15 casos) y tres specs E2E: los smokes
`premium-price`/`premium-purchase` y `premium-course-checkout` ampliado con `FUND_FRESH=1`.

**Defecto encontrado y corregido durante esta revisión:** `slearn_amount_paid` se guardaba en **centésimas**
(100 = 1.00 SLEARN, columna INTEGER) mientras `usdt_amount_paid` era DECIMAL(10,2), y los dos consumidores
nuevos publicaban ese número como si fueran SLEARN legibles, mostrando **100×** el monto
(`components/PremiumCoursesSection.tsx` y el agregado `premium` de `app/api/transparency/route.ts`). Primero se
convirtió en los lectores; **ahora la columna misma se unificó a DECIMAL(10,2)**
(migración `20260921180000_premium_course_slearn_amount_decimal`, conversión ÷100 de los valores existentes,
reversible y verificada), así que `premium_purchase.ts` guarda SLEARN legibles y los lectores ya no convierten.

**Estado (2026-09-21, tarde): con esto se cierran los ítems funcionales del requisito.** El último ("acceso tras la
compra" a nivel UI) quedó verificado: el E2E de navegador con `FUND_FRESH=1` **completa la compra de verdad** (0 fallas)
gracias a un **puente RPC real opt-in** en el spec. Antes fallaba porque la manejaba un **mock que no difunde**
(`eth_sendTransaction` devolvía `0xcdcdcd…cd`), y el backend la rechazaba con razón (`400`, "Backend verification
failed: Transaction receipt … could not be found"). El arreglo, en `e2e/helpers/e2e-auth.mjs` +
`e2e/specs/premium-course-checkout.spec.mjs`, es el puente `__rpcReal` que ya usaba
`donate-campaign-celo-modal.spec.mjs` —con soporte de **calldata** para las aprobaciones/transferencias ERC-20 del
modal— que el spec **enciende justo antes de pagar** (antes de financiar la billetera el saldo real es 0 y el modal
mostraría el panel de gas). Lo verificado tras el despliegue está abajo.

**Verificado contra el dev site DESPLEGADO (`make all` + `bin/start`, 2026-09-21):** el agregado `premium` viajó —
**101 compras, 18.86 USDT y 2705.81 SLEARN** (el orden de magnitud confirmó la conversión ÷100; sin ella serían
270 581) — y el smoke `transparency` lo afirmó como aserción real. **Ese bloque ya no es público** (ver la nota de
abajo): los mismos números viven ahora en `GET /api/admin/premium-purchases` y el panel publica `premiumSlearn`. El
historial devuelve `categoria`/`subcategoria` (comprobado con la sesión de la fixture:
`pay-course/usdt/payment/course_purchase`); el precio junto al botón Buy se ve en la UI del sitio desplegado
("0.70 USDT · or 13.86 SLEARN (10% off)", §2.1) y el slider llega a 100% SLEARN; los smokes quedan **20/21** (el fallo es
`referral-payout` paso 5, ajeno a este cambio: comprobé que el **vault del curso 2 sí tiene fondos**, 1026.06 USDT +
1956.33 SLEARN y `active=true`, así que la hipótesis que imprime el spec es falsa; `rails-health` imprime `[FAIL]` pero
sale 0 por diseño); y por ser build de producción se pudieron correr por fin los **offline**: `offline-guide` 5/5 y
`offline-crossword` 7/7, 0 fallas (service worker registrado y controlando, guía legible sin conexión, cola en
IndexedDB que se vacía al volver la conexión).

**Verificado contra el dev site en la revisión del revisor (2026-09-21):** las tres mezclas de pago del smoke `premium-purchase` — **100% USDT 9/9**, **50/50 9/9** (0.35 USDT + 6.93 SLEARN) y **100% SLEARN 9/9** (13.86 SLEARN) — y los smokes sin fondos `premium-price` 6/0, `transparency` 10/0 (el bloque `premium` degrada a un `ok` informativo hasta el despliegue), `landing-page` 3/0 y `leaderboard` en verde. Las dos mezclas con SLEARN dejaron la fixture en 2991.09 (gasto exacto de 23.91).

**Cambios posteriores a esa corrida (2026-09-21, unidad de SLEARN y API de historial):** migración
`20260921180000_premium_course_slearn_amount_decimal` (`slearn_amount_paid` → DECIMAL(10,2), con los valores
existentes convertidos; `make type` y `make test` verdes) y `GET /api/user-transactions/[id]` devolviendo
`categoria`/`subcategoria`. **El despliegue debe incluir la migración** (`bin/m db:migrate` en el dev site) además
del rebuild de motores, o el sitio seguiría leyendo la columna como centésimas.

**Supersedes:** #148 task 1 (premium course payments with SLEARN) — this is now the authoritative payment spec; #148 tracks the remaining post-launch features (burn, stable-sl swap, church distribution, transparency, governance). The referral program is tracked in #163.

---

## Summary of Key Decisions

| Decision | Details |
| :--- | :--- |
| **Payment method** | Mixed USDT + SLEARN (same `SLEARN.processPayment()` used for donations) |
| **Price calculation** | Linear `p = A*hdi + B`, calibrated to Sierra Leone ($2) and Colombia ($5). SLEARN price = USDT price × 22 × 0.90 (10% discount) |
| **Distribution percentages (Standard)** | Premium payment distribution (50% pdJ, 10% vault, 10% reward, 10% missional, 5% UBI, 10% referrals, 5% churches) — per SLEARN-WHITEPAPER.md §3.1.2. Referrals raised to 10% (funded from pdJ, NOT from scholarships). |
| **Distribution percentages (GD)** | Global Disciples course: 40% pdJ, 10% Cluster/Country Fund, 10% vault, 10% reward, 10% missional, 5% UBI, 10% referrals, 5% churches. The cluster 10% is routed off-chain via `ClusterFunds.sol` (#155) (see §1.3). |
| **SLEARN burning** | Handled inside `SLEARN.processPayment()` (user's SLEARN are burned, reserve USDT released) |
| **Access control** | After payment, a record in `premium_course_usuario` grants access to the course guides |
| **UI** | Show price in SLEARN, allow user to choose mixed payment (slider) |
| **HDI Source** | `m_hdi` table (pais_id, year, hdi) — already populated from OWID CSV |

---

## 1. Backend Changes (learn.tg)

### 1.1 Dynamic Price Calculation

**File:** `app/api/courses/premium/price/route.ts` (new)

**Input:** `courseId`, `userWallet` (JWT authenticated)

**Output:** `priceUSDT`, `priceSLEARN`, `hdiUsed`, `country`

**Logic:**
```typescript
// 1. Get user's country from KYC (self.xyz verification)
const country = await getUserCountry(userWallet);

// 2. Get HDI for country from m_hdi table
const hdi = await getHDI(country.pais_id, 2022); // Use latest year

// 3. Calculate USDT price with the two-point linear formula
//    p = A * hdi + B, calibrated so:
//      Sierra Leone (hdi 0.467) → $0.7
//      Colombia     (hdi 0.788) → $3
//    (updated 2026-09: GD course prices lowered from $2/$5)
const A = (3 - 0.7) / (0.788 - 0.467);
const B = 0.7 - A * 0.467;
const priceUSDT = A * hdi + B;

// Round to 2 decimal places
const roundedPriceUSDT = Math.round(priceUSDT * 100) / 100;

// 4. Convert to SLEARN (10% discount for paying in SLEARN)
const rate = await slearnContract.usdtToSlearnRate(); // 22
const slearnDiscount = 0.10;
let priceSLEARN = roundedPriceUSDT * rate * (1 - slearnDiscount);

// 5. Apply minimum SLEARN (to avoid dust)
// Minimum 1 SLEARN unit = 0.01 SLEARN (since 2 decimals)
const minSLEARN = 1;
priceSLEARN = Math.max(priceSLEARN, minSLEARN);
```

**Mixed payment rule (USDT + SLEARN):** the SLEARN paid is valued at the
discounted rate `rate * (1 - slearnDiscount)`. A payment of `u` USDT plus
`s` SLEARN is valid iff:

```
u + s / (rate * (1 - slearnDiscount)) >= priceUSDT
```

Equivalently, the full SLEARN price (100% SLEARN) is `priceUSDT * rate * (1 - slearnDiscount)`.

**Response:**
```json
{
  "priceUSDT": 0.70,
  "priceSLEARN": 13.86,
  "hdiUsed": 0.467,
  "country": "Sierra Leone"
}
```

### 1.2 Purchase Endpoint

**File:** `app/api/courses/premium/purchase/route.ts` (new)

**Input:** `courseId`, `usdtAmount`, `slearnAmount`, `referralAddress` (optional)

**Process:**
```typescript
// 1. Verify user is authenticated (JWT from SIWE)
// 2. Calculate expected price (same as price endpoint)
// 3. Verify mixed payment:
//    usdtAmount + slearnAmount / (rate * (1 - slearnDiscount)) >= expectedPriceUSDT
// 4. Verify user has sufficient SLEARN balance (if slearnAmount > 0)
// 5. Call SLEARN.processPayment() with:
//    - payer: user wallet
//    - usdtAmount: user's USDT contribution
//    - slearnAmount: user's SLEARN contribution
//    - courseId: premium course ID
//    - percentages: based on course type (GD vs standard)
//    - referralAddress: if user came from referral
// 6. Wait for transaction receipt
// 7. Record enrollment in premium_course_usuario table
// 8. Return success + transaction hash
```

### 1.3 Premium Distribution Percentages

#### Standard Courses (all premium except GD)

| Destination | Percentage | Notes |
| :--- | :--- | :--- |
| pdJ Treasury | 50% | Operations & creators |
| Course Vault | 10% | Implied (`100 - totalPct`) |
| Student Reward | 10% | Minted as SLEARN via `mintAndReserve` |
| Missional Courses | 10% | 5% USDT, 5% SLEARN |
| UBI | 5% | UBI wallet |
| Referrals | 10% | Referral wallet — 10% of the payment to the referrer |
| Churches | 5% | Registered and verified churches |
| **Total** | **100%** | |

Maps to `SLEARN.processPayment()` args:
`pdJ=50, reward=10, missional=10, ubi=5, referral=10, churches=5` (vault = 10%).

#### Global Disciples Course

The 10% that would otherwise go to pdJ is routed to the Cluster/Country Fund.
`processPayment` has **no cluster destination**, so this is done off-chain
(Option B) via `ClusterFunds.sol` (#155):

1. Backend receives the user's USDT/SLEARN payment.
2. Backend routes **10%** of the payment to `ClusterFunds.sol`:
   - First `transfer()` the 10% USDT and SLEARN from the backend wallet to
     `ClusterFunds.sol` (calldata-stuffing pattern, **no `approve`** — the
     `IERC20` ABI has no `approve`; `processDonation`/`processCountryDonation`
     only record, they do not pull tokens).
   - Then call `processDonation(clusterWallet, ...)` if the user's church is in
     a cluster (`church.cluster_wallet`),
   - else `processCountryDonation(countryCode, ...)` for the user's country
     (`church.country_id`) if it is in the pilot (Sierra Leone / Colombia),
   - else `processCountryDonation('SL', ...)` to Sierra Leone.
3. Backend calls `SLEARN.processPayment()` for the remaining 90%, with pdJ
   reduced (40% of the original total instead of 50%) and the buyer's reward
   raised to 12% of the processed 90% (≈10.8% of the original, per #214).

| Destination | % of the 90% processed | % of the original |
| :--- | :--- | :--- |
| pdJ Treasury | 40% | 36% |
| Student Reward | 12% | 10.8% |
| Missional Courses | 10% | 9% |
| UBI | 5% | 4.5% |
| Referrals | 10% | 9% |
| Churches | 5% | 4.5% |
| Course Vault (remainder) | 18% | 16.2% |
| **Subtotal** | **100%** | **90%** |
| Cluster/Country Fund (off-chain) | — | 10% |
| **Total** | | **100%** |

> **Implementation note (measured 2026-09-21):** `processPayment` uses integer
> percentages and has no cluster slot, so the cluster 10% is routed off-chain
> first (Option B) and the 90% remainder is processed with
> `pdJ=40, reward=12, missional=10, ubi=5, referral=10, churches=5`; the vault
> receives the leftover. That is what
> `packages/rewards/src/routes/__tests__/premium-purchase.test.ts` asserts
> (`args: [WALLET, 2250000n, 0n, 1n, 40n, 12n, 10n, 5n, 10n, 5n]`), and the
> standard course path asserts `50n` in the pdJ slot (see §7 task 3.6). The
> design target recorded before was `40/10/10/10/10/5/10/5 = 100` with a 10%
> vault; the shipped split is the one in this note.

### 1.4 Database Schema

**New table:** `premium_course_usuario`

```sql
CREATE TABLE premium_course_usuario (
    id SERIAL PRIMARY KEY,
    usuario_id INTEGER NOT NULL REFERENCES usuario(id),
    course_id INTEGER NOT NULL REFERENCES cor1440_gen_proyectofinanciero(id),
    purchased_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    usdt_amount_paid DECIMAL(10,2),
    slearn_amount_paid DECIMAL(10,2), -- SLEARN legibles (migración 20260921180000)
    transaction_hash VARCHAR(66) NOT NULL,
    expires_at TIMESTAMP, -- NULL = lifetime access
    UNIQUE(usuario_id, course_id)
);
```

> **Unidades (unificadas el 2026-09-21).** La columna nació como `INTEGER` en
> centésimas (100 = 1.00 SLEARN) mientras `usdt_amount_paid` era `DECIMAL(10,2)`:
> la misma ruta escribía una en centésimas y la otra en unidades, y cada lector
> tenía que dividir por 100 (la primera versión del perfil mostraba 1386.00 SLEARN
> en vez de 13.86). La migración `20260921180000_premium_course_slearn_amount_decimal`
> la pasa a `DECIMAL(10,2)` convirtiendo los valores existentes (`÷ 100`) y es
> reversible. Ahora `premium_purchase.ts` guarda SLEARN legibles, igual que
> `transaction.amount` con `crypto='slearn'`, y ni el perfil ni el panel de
> transparencia convierten. Los valores de la BD local se verificaron en la
> conversión (554→5.54, 3960→39.60, 582→5.82, 166→1.66, 69→0.69) y el `down`
> revierte exactamente.

> **`GET /api/user-transactions/[id]`** devuelve además `categoria` y
> `subcategoria` (`lib/user-transactions.ts`): sin ellas, una compra pagada en
> USDT y SLEARN aparece como dos filas indistinguibles de cualquier otro pago.

---

## 2. Frontend Changes

### 2.1 Course Card Component

**File:** `components/CourseCard.tsx` (the badge + buy button are rendered inline on the course page; there is no separate `CourseCard` component)

- [x] Add "Premium" badge for premium courses (`porPagar > 0`)
- [x] Show "Buy this course" button (opens `CheckoutModal` with USDT/SLEARN mix)
- [x] Show price in SLEARN on/near the buy button (fetch from `/api/courses/premium/price`) — 2026-09-21: the course page renders it under the button (`data-testid="premium-price"`, USDT + SLEARN with the 10% discount) and `premium-course-checkout` asserts it

### 2.2 Purchase Modal

**File:** `components/CheckoutModal.tsx` (new) — reuse the existing donation
modal (`components/CourseDonation.tsx`) as the base: same approve + transfer +
backend-call flow, adapted for premium purchase (price endpoint + purchase
endpoint instead of the donation endpoint).

**Features:**
- [x] Display price in USDT and SLEARN (10% discount on the SLEARN portion)
- [x] Slider to choose how much SLEARN to use (0 to max SLEARN balance)
- [x] Show remaining amount to pay in USDT
- [x] Button to approve USDT and SLEARN to the backend wallet
- [x] Button to execute purchase
- [x] Loading states and error handling
- [x] Success page with transaction hash — already implemented in `CheckoutModal`: a result screen with the distribution breakdown, the `processPayment` explorer link and an OK button (verified 2026-09-21; `premium-course-checkout` asserts the tx link)

**UI flow:**
```
┌─────────────────────────────────┐
│  Premium Course: Web3 & UBI     │
│  Price based on your country    │
│  (Sierra Leone): $0.70 USDT     │
│  Or 13.86 SLEARN (10% off)      │
├─────────────────────────────────┤
│  Pay with SLEARN:               │
│  ├───●───┼───┼───┤ 6.93 SLEARN  │
│  Remaining: 6.93 SLEARN         │
│  Will pay with USDT: $0.35      │
├─────────────────────────────────┤
│  [Approve USDT] [Approve SLEARN]│
│  [Purchase Course]              │
└─────────────────────────────────┘
```

> Cifras con el tipo del dev (22) y el 10% de descuento del SLEARN; el ejemplo de
> mezcla es 50/50. El precio junto al botón Buy se sirve con
> `data-testid="premium-price"` y `e2e/specs/premium-course-checkout.spec.mjs` lo
> exige.

### 2.3 User Profile - Premium Courses

**File:** `components/PremiumCoursesSection.tsx` (2026-09-21; rendered at the end of
`app/[lang]/profile/page.tsx`, next to `ReferralProfileSection`, instead of a separate page)

- [x] List all premium courses purchased by the user (API `courses/premium/mine`; the section renders `titulo` per row and the whole block is hidden when there is nothing to show) — test: `components/__tests__/PremiumCoursesSection.test.tsx`
- [x] Show purchase date, amount paid (USDT/SLEARN)
- [x] Link to course (if still accessible) — links to `/[lang]{prefijoRuta}`, where access is already enforced server-side
- [x] Show expiry date (if applicable) — **no expiry applies**: a premium purchase grants permanent access, so the row links to the course instead

> **Unidades (unificadas el 2026-09-21):** ambas columnas son DECIMAL(10,2) en
> unidades legibles. La columna nació en **centésimas** (100 = 1.00 SLEARN), lo que
> obligaba a cada lector a dividir por 100 (y la primera versión mostró 1386.00
> SLEARN en vez de 13.86, con la prueba codificando el error). La migración
> `20260921180000_premium_course_slearn_amount_decimal` convierte la columna y sus
> valores; ver §1.4.

### 2.4 Course Access Control (Server-Side)

Premium course guides must only be visible to users who purchased the course.
The `premium_course_usuario` table is the source of truth for access.
Enforcement is server-side so a direct API call cannot bypass it.

**Enforcement in `app/api/guide/route.ts`:**
1. `authenticateUser()` resolves `usuario_id` from wallet + token.
2. Load the course `porPagar`. If `0` or NULL, the course is free and access is
   allowed.
3. If premium, require a row in `premium_course_usuario`
   (`usuario_id` + `course_id`).
4. No row, return `403` with `{ error: 'purchase required' }`.

**Frontend behavior:**
- `app/[lang]/[pathPrefix]/[pathSuffix]/page.tsx`: on no-access, show the
  purchase modal (or link to the course page) instead of the guide content.
- `app/[lang]/[pathPrefix]/page.tsx` (course listing): premium courses not yet
  purchased show "Buy" instead of a guide link; purchased and free courses show
  the guide list.
- Guide navigation (`useGuideNavigation`) excludes guides the user cannot
  access, based on the same `premium_course_usuario` check.

**Free courses are unaffected** (`porPagar = 0` or NULL).

#### Verified worship city (all paid courses)

The price of every paid course depends on the buyer's country (`pais_id` →
`m_hdi`), and `pais_id` is **self-reported** — there is no country
verification. The verifier-confirmed church city stands in for it:

- `usuario.verified_city_id` when the place of worship has a centro poblado,
  or
- `usuario.verified_place_of_worship_location` (free text) otherwise — e.g.
  Freetown has no centro poblado in the catalog, so the verifier confirms the
  text location (the admin "Ciudad Verificada" toggle falls back to this
  field automatically).

**Every premium course purchase requires a verified worship city**, enforced
server-side by `canPurchasePremiumCourse` in `lib/course-access.ts` on both the
`purchase-eligibility` endpoint and `POST /api/courses/premium/purchase`
(403 with reason key `verified_city_required`).

#### Global Disciples course eligibility (GD pilot)

The GD course (id=10 `/gdcluster`, id=11 `/redgd`) has additional rules. The
pilot forms clusters in **Colombia and Sierra Leone** through **non-Zionist
churches**. Final access model (clarified 2026-08-13):

1. **Non-Christians cannot see the GD course** in the course lists (`/en`,
   `/es`). The course card is hidden unless `usuario.religion_id = 2`
   (Cristianismo).
2. **Any Christian can see the presentation** and open it
   (`/en/gdcluster` / `/es/redgd`), see its content, and see the ranking link.
   The presentation links to `gdcluster/ranking` / `redgd/ranking`, where
   **anyone** (public) can donate.
3. **Guides are premium** (purchase required), like any other premium course.
   The course has **6 guides** (`G1`..`G6`); guides not yet published show
   their availability date in the title (migration
   `20260813180000_restore_gd_guides_and_subtitle`).
4. **The "Buy" button is only enabled** when the user is a **Christian**
   (`religion_id = 2`) **and**:
   - from a pilot country (`usuario.pais_id` in `[170, 694]` = Colombia, Sierra
     Leone), **and**
   - has a **verified worship city** (see above — verified_city_id or
     verified_place_of_worship_location), **and**
   - the user answered **no** to the single Gaza question in their own profile
     (`usuario.position_israel_gaza='no'`, i.e. does NOT support Israel in the
     Gaza genocide). Supporting Israel there → considered Zionist → no Buy.

A US Christian, or a Sierra Leone/Colombia member who answered `yes` (or left
blank) to the Gaza question, sees the same as a US Christian: presentation +
ranking link, **no Buy**.

This replaces the earlier three-question check (covenant/remnant/gaza answered
at church registration): the Zionist gate is now a single per-user Gaza
question answered in the profile. The non-Zionist check is a **purchase
eligibility** gate, not a guide-viewing gate (guides are gated by
`premium_course_usuario` only).

**Course completion gate:** the cluster tool (`POST /api/cluster`) requires the
pastor to have **taken the GD course** (a `premium_course_usuario` row for
course 10 or 11) before creating a cluster.

**Implementation:**
- `lib/course-access.ts`:
  - `canAccessCourse(db, userId, courseId)` — premium-only guide gate
    (`premium_course_usuario`); no course-specific GD guide gate.
  - `canPurchasePremiumCourse(db, userId, courseId)` — purchase eligibility
    for **every** paid course: verified worship city required; GD adds the
    pilot gates via `canPurchaseGDCourse`.
  - `canPurchaseGDCourse(db, userId)` — GD pilot eligibility (Christian,
    pilot country, verified worship city, single per-user Gaza question).
  - `hasVerifiedWorshipCity(user)` — `verified_city_id` or
    `verified_place_of_worship_location`.
- `app/api/courses/[courseId]/purchase-eligibility/route.ts` (new) — returns
  `{ eligible, reason }`; enforces `canPurchasePremiumCourse` for all courses.
- `POST /api/courses/premium/purchase` — denies purchases with `403` when
  `canPurchasePremiumCourse` fails (all paid courses, not only GD).
- Course page (`/[lang]/[pathPrefix]/page.tsx`) — hides Buy / shows reason for
  ineligible GD users.
- Course listing (`/[lang]/page.tsx`) — hides GD cards for non-Christians.
- `POST /api/cluster` — requires the pastor to have purchased the GD course.
- Profile page (`/[lang]/profile/page.tsx`) — asks the single Gaza question;
  `/api/profile` persists `position_israel_gaza`.
- Migration `20260813190000_add_gaza_position_to_usuario` — adds
  `usuario.position_israel_gaza`.

---

## 3. HDI Data Source

### 3.1 Database Table

`m_hdi` table already exists (migration `20260804121336_hdi`):

```sql
CREATE TABLE m_hdi (
    id SERIAL PRIMARY KEY,
    pais_id INTEGER NOT NULL REFERENCES msip_pais(id),
    year INTEGER NOT NULL,
    hdi DECIMAL(4,3) NOT NULL,
    source VARCHAR(50) DEFAULT 'UNDP',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(pais_id, year)
);
```

### 3.2 Example Prices by Country (HDI 2023, rate 22, 10% SLEARN discount)

| Country | HDI | Price USDT | Price SLEARN (100% SLEARN) |
| :--- | :--- | :--- | :--- |
| Sierra Leone | 0.467 | $0.70 | 13.86 |
| Ghana | 0.628 | $1.85 | 36.63 |
| Colombia | 0.788 | $3.00 | 59.40 |
| United States | 0.938 | $4.07 | 80.59 |

- **Lowest in `m_hdi`:** South Sudan (0.388) → $0.13.
- **Highest in `m_hdi`:** Iceland (0.972) → $4.32.

`priceUSDT = A * hdi + B` where `A = (3 − 0.7) / (0.788 − 0.467)`, `B = 0.7 − A * 0.467`. `priceSLEARN = priceUSDT * 22 * 0.90`.

> Updated 2026-09: prices lowered (GD course now $0.7 in Sierra Leone and
> $3 in Colombia).

---

## 4. Testing & Verification

### 4.1 Unit Tests

- [x] Price calculation for various countries (SL, GH, CO, US) — `packages/rewards/src/lib/__tests__/premium-pricing.test.ts`
- [x] Mixed payment amount validation
- [x] Edge cases: insufficient balance, minimum payment
- [x] (2026-09-21) Route level: `app/api/courses/premium/price`, `app/api/courses/premium/mine` and `app/api/courses/[courseId]/access` (15 cases: validation, 401, 200/403, price per HDI and the SLEARN discount)

### 4.2 Integration Tests

- [x] End-to-end: purchase with 100% USDT — verificado 2026-09-21 contra el dev site con `e2e/smoke/premium-purchase.spec.mjs`: `PURCHASE=1 SLEARN_PCT=0` → 9/9 (precio 0.70 USDT, transferencia al backend del sitio, `processPayment` on-chain, 8 destinos de distribución, acceso al curso y fila en `mine` con 0.70 USDT)
- [x] End-to-end: purchase with 100% SLEARN — **verificado 2026-09-21** contra el dev site: `PURCHASE=1 SLEARN_PCT=100` → **9/9** (precio 0.70 USDT / 13.86 SLEARN, transferencias del pastor al backend, `processPayment` on-chain `0x71b43a2504…`, 8 destinos, acceso al curso y fila en `mine` con 13.86 SLEARN). La fixture se recargó a 3015.00 SLEARN y quedó en 2991.09 (gasto exacto: 23.91)
- [x] End-to-end: purchase with mixed payment (50/50) — **verificado 2026-09-21**: `PURCHASE=1 SLEARN_PCT=50` → **9/9** (pagó 0.35 USDT + 6.93 SLEARN, `processPayment` on-chain `0x836629364c…`, 15 destinos —los dos tramos USDT y SLEARN—, acceso al curso y fila en `mine` con 0.35 USDT + 6.93 SLEARN)

  > Las tres mezclas quedan verificadas a nivel API. Las dos últimas además verificaron el arreglo de unidades de punta a punta: en ese momento la columna guardaba centésimas (693 y 1386) y la fila de `mine` imprimía 6.93 y 13.86 SLEARN en vez de 693.00/1386.00. La migración `20260921180000` unificó después la columna a DECIMAL(10,2), así que hoy guarda 6.93/13.86 directamente.
  >
  > Comandos: `SITE_URL=https://learn.tg:9001 PURCHASE=1 SLEARN_PCT=50 node e2e/smoke/premium-purchase.spec.mjs` (y `SLEARN_PCT=100`; `0` para la de USDT). El smoke pide a la fixture solo lo que la mezcla necesita y, si no alcanza, lo informa como saltado en lugar de fallar. No necesita desplegar la rama: solo ejercita APIs que el dev site ya tiene.
  >
  > La versión en navegador (`e2e/specs/premium-course-checkout.spec.mjs`, `FUND_FRESH=1`) sigue pendiente: es la que además comprueba la UI (precio junto al botón, resultado con hash, "Purchased", la compra en el perfil y `guide1` sin muro), y requiere que la rama esté desplegada en el dev site. Con las tres mezclas ya verificadas por API, basta correrla una vez (su defecto es `SLEARN_PCT=100`).

- [x] Verify enrollment recorded in database
- [x] Verify user can access course after purchase — **verificado 2026-09-21 contra el dev site con `FUND_FRESH=1`**:
  el E2E `premium-course-checkout` **0 fallas** (compra real firmada y difundida por la billetera del pastor nuevo tras
  financiarla) y, tras la compra, la pantalla de resultado con el hash, "Purchased" en la página del curso, la compra
  listada en el perfil ("My premium courses") y **`/en/gdcluster/guide1` abierto sin muro (4457 caracteres)**. La ruta
  también está cubierta por unidad (`courses/[courseId]/access` → 200/403, 4 casos). Requirió un puente RPC real
  opt-in en el spec (`window.__e2eRealRpc` + `__rpcReal` en `e2e/helpers/e2e-auth.mjs`): el mock devolvía el hash falso
  `0xcdcd…` y el backend la rechazaba con razón
- [x] Verify correct price for different countries (test with mock KYC) — `e2e/smoke/premium-price.spec.mjs` (2026-09-21): crea un perfil de Sierra Leona y otro de Colombia y compara `/api/courses/premium/price` (SL 0.70 USDT / 13.86 SLEARN, CO 3.00 USDT / 59.40 SLEARN, HDI alto paga más y el SLEARN conserva el 10%). Corre contra el dev site sin fondos ni Chrome: `SITE_URL=https://learn.tg:9001 node e2e/smoke/premium-price.spec.mjs`. **Re-verificado por el revisor el 2026-09-21: 6 passed / 0 failed** contra `https://learn.tg:9001` (tipo implícito medido 19.80 = 22 × 0.90, montos dentro de la calibración)
- [x] Verify GD course uses different distribution (10% to ClusterFunds)

### 4.3 Regression Tests

Estado real (revisión 2026-09-21): en local se verificó la suite unitaria (`make test`: 1006 passed / 6 skipped, 0
fallas) y el audit de auth; y contra el dev site `https://learn.tg:9001` se corrieron hoy los smokes sin fondos
—`premium-price` 6/0, `transparency` 10/0 (el bloque `premium` degrada hasta que se despliegue), `landing-page` 3/0
y `leaderboard` en verde—. Los que gastan fondos (`celo-claim`, `referral-*`, `premium-purchase` con `PURCHASE=1`)
no se corrieron en esta revisión.

- [x] Free courses unaffected — `e2e/smoke/landing-page.spec.mjs` (corrido 2026-09-21 en el dev site: 3/0)
- [x] Existing scholarships unaffected — `e2e/smoke/celo-claim.spec.mjs`, `e2e/specs/guide-claims.spec.mjs` (**specs que la cubren**; no se corrieron hoy porque gastan fondos del dev)
- [x] Leaderboard still works — `e2e/smoke/leaderboard.spec.mjs` (corrido 2026-09-21 en el dev site: verde)
- [x] Transparency dashboard shows premium payments — bloque implementado (2026-09-21), con prueba de ruta
  (`transparency/__tests__/route.test.ts`, 2 casos nuevos) y aserción en `e2e/smoke/transparency.spec.mjs` que se
  activa cuando el build desplegado trae el agregado (hoy el dev site responde sin `premium`: degrada a un `ok`
  informativo, no a una falla)

---

## 7. Implementation Phases

### Phase 1 — Price Calculation + DB

**Goal:** Users can see their personalized price. No payments yet.

| # | Task | Depends on | Status |
|---|---|---|---|
| 1.1 | `GET /api/courses/premium/price` — two-point linear pricing | `m_hdi` ✅ | ✅ |
| 1.2 | Migration: `premium_course_usuario` table | — | ✅ |
| 1.3 | Use `porPagar` on `cor1440_gen_proyectofinanciero` (no new column) | — | ✅ |
| 1.4 | Price display on course card ("Buy with SLEARN" button) | 1.1 | ✅ 2026-09-21: price (USDT + SLEARN con 10% dto.) bajo el botón, `data-testid="premium-price"` |
| 1.5 | Unit tests: price calculation (SL, GH, CO, US) | 1.1 | ✅ |

**Delivers:** Price endpoint working. Course cards show premium status.

---

### Phase 2 — Purchase Flow

**Goal:** Users can pay and access premium courses.

| # | Task | Depends on | Status |
|---|---|---|---|
| 2.1 | `POST /api/courses/premium/purchase` — call `SLEARN.processPayment()` and record enrollment | 1.2, 1.3 | ✅ |
| 2.2 | `CheckoutModal` — slider for USDT/SLEARN mix, approve + purchase flow | 2.1 | ✅ |
| 2.3 | Course access control on `/api/guide` + `/[lang]/[pathPrefix]/[pathSuffix]` — check `premium_course_usuario` | 2.1 | ✅ |
| 2.4 | Profile page: "My Premium Courses" section | 1.2 | ✅ 2026-09-21: `components/PremiumCoursesSection.tsx` (sección, no página aparte) + 5 pruebas |
| 2.5 | Integration tests: 100% SLEARN, 100% USDT, mixed | 2.1–2.3 | ✅ las tres mezclas verificadas contra el dev site el 2026-09-21 (9/9 cada una, §4.2); pendiente solo la variante en navegador (`FUND_FRESH=1`), que requiere el despliegue |

**Delivers:** Complete purchase flow for standard courses. GD course blocked by #155.

---

### Phase 3 — Global Disciples Distribution

**Goal:** GD course payments route 10% to the Cluster/Country Fund via `ClusterFunds.sol` (Option B).

| # | Task | Depends on | Status |
|---|---|---|---|
| 3.1 | Backend routes 10% to `ClusterFunds.sol` (cluster → country in pilot → Sierra Leone) before `processPayment` | #155 Phase 1 | ✅ |
| 3.2 | `processPayment` for the remaining 90% with reduced pdJ (40% of original) | 3.1 | ✅ |
| 3.3 | GD purchase eligibility (`canPurchaseGDCourse`): Christian + pilot + verified worship city + non-Zionist pastor | — | ✅ |
| 3.4 | `POST /api/courses/premium/purchase` denies ineligible GD purchases (403) | 3.3 | ✅ |
| 3.5 | Course listing hides GD for non-Christians; course page gates Buy by eligibility | 3.3 | ✅ |
| 3.6 | Regression: standard courses still use 50% pdJ | 2.1 | ✅ cubierto por unidad: `packages/rewards/src/routes/__tests__/premium-purchase.test.ts` asserta `50n` en el slot pdJ para un curso no-GD y `40n` para GD. No hay E2E del caso estándar porque **hoy no existe ningún curso premium que no sea GD** (solo los ids 10/11 tienen `porPagar > 0`); el E2E espera a que exista uno |

**Delivers:** GD course purchases support the cluster system and enforce pilot
eligibility.

---

## 8. What's Already Done

| Component | Status |
|---|---|
| `SLEARN.sol` deployed with `receive()` function | ✅ |
| `m_hdi` table with HDI data | ✅ Migration `20260804121336_hdi` |
| `transaction` table | ✅ |
| GD course DB records (id=10) | ✅ Migration `20260804134734` |
| Price shown next to the Buy button | ✅ 2026-09-21, `app/[lang]/[pathPrefix]/page.tsx` |
| Result screen with the tx hash | ✅ `components/CheckoutModal.tsx` (verified 2026-09-21) |
| "My premium courses" in the profile | ✅ 2026-09-21, `components/PremiumCoursesSection.tsx` + test (SLEARN en DECIMAL(10,2) desde la migración 20260921180000) |
| Contabilidad comercial de cursos de pago | ✅ 2026-09-21, **no pública**: `GET /api/admin/premium-purchases` (`authenticateAdmin`) + `components/admin/PremiumPurchasesWidget.tsx` en `/[lang]/admin`. El panel público publica solo los flujos de SLEARN de esos pagos (`premiumSlearn`: quemado y acuñado) |

---

## 9. Acceptance Criteria

- [x] Price endpoint returns correct amount based on user's country HDI
- [x] Price is higher for high-HDI countries, lower for low-HDI countries
- [x] User can purchase premium course using mixed USDT + SLEARN
- [x] SLEARN paid is burned; reserve USDT released and redistributed
- [x] User can access course immediately after purchase
- [x] Non-purchasers cannot view premium course guides (server-side 403)
- [x] Purchased courses appear in user's profile — "My premium courses" (`components/PremiumCoursesSection.tsx`), 2026-09-21
- [x] Per-course premium accounting is not public and lives in the admin panel — 2026-09-21: `GET /api/admin/premium-purchases` (`authenticateAdmin`) + the "Premium purchases" widget of `/[lang]/admin`; the smoke `transparency` asserts that `premium` is **absent** from the public panel. What the public panel publishes is `premiumSlearn` (SLEARN burned in course payments and minted back as rewards), which is the token-flow part the whitepaper backs (reserves, supply, coverage). The per-course block that had been put there was **moved at the operator's request** (2026-09-21): transparency is for trust in SLEARN, not for opening learn.tg's books. In the same pass `GET /api/metrics` (the internal dashboard's data, adjacent to this accounting) had its commented-out auth replaced with `authenticateAdmin`, and `/[lang]/metrics` now checks the verifier server-side
- [x] Works for users with zero SLEARN (100% USDT)
- [x] Works for users with sufficient SLEARN (100% SLEARN)
- [x] Graceful error handling for insufficient balance, network issues, etc.
- [x] GD course uses 10% to ClusterFunds (#155) instead of 50% to pdJ
- [x] Standard courses use 50% to pdJ (no ClusterFunds)
- [x] Referrals earn 10% of the course price when a referred user buys (funded from pdJ, not scholarships)
- [x] Non-Christians cannot see GD course in course lists (`/en`, `/es`)
- [x] Christians anywhere can see GD presentation + ranking link
- [x] GD Buy button shown only to eligible members (Christian + pilot country + verified worship city + non-Zionist pastor)
- [x] Every paid course requires a verified worship city (403 server-side for unverified users; reason `verified_city_required`)
- [x] GD purchase denied server-side (403) for ineligible users

---

## 10. Out of Scope

- Discount by profileScore (removed by decision)
- Subscription-based premium access (recurring payments)
- Discount codes or promotions
- Team/group purchases
- Refund mechanism (handled manually if needed)
- Exchange SLEARN for Leones (SLE) via stable-sl (separate project)

---

## 7. Dependencies Checklist

- [x] I129 completed (SLEARN.sol deployed on mainnet)
- [x] I129 completed (LearnTGVaultsV3 deployed on mainnet)
- [x] SLEARN contract has `receive()` function with mixed payment support
- [x] User KYC data includes country (`usuario.pais_id`, not self.xyz for MVP)
- [x] `m_hdi` table populated with HDI data
- [x] #155 (ClusterFundsV2) deployed for GD course distribution — celoSepolia `0xcA9c6A...` and mainnet `0x3F52...`; the app operates with V2
- [x] #196 (LearnTGVaultsV5) deployed for scholarship splitting

---

> *"For which of you, intending to build a tower, does not sit down first and count the cost, whether he has enough to finish it?"* (Luke 14:28)

---

**Created:** 2026-04-08
**Updated:** 2026-08-06 — Divided into 3 phases. Phase 1 (price calc) unblocked; Phase 2 (purchase) unblocked for standard courses; Phase 3 (GD) blocked by #155.
**Updated:** 2026-08-12 — Implemented Phase 1 + Phase 2 backend (price endpoint, purchase endpoint, access control, `premium_course_usuario` migration). Pricing uses two-point linear formula calibrated to SL=$2 / CO=$5. Checkout modal + profile + GD routing (Phase 3) pending.
**Updated:** 2026-08-13 — Finalized GD access model and implemented it end-to-end: `canPurchaseGDCourse` (Christian + pilot country + church member + registered non-Zionist pastor), `purchase-eligibility` endpoint, purchase-endpoint 403 gate, course listing hides GD for non-Christians, course page gates Buy by eligibility. GD 10% ClusterFunds routing (Option B, `transfer()` no approve) confirmed against #155. Unit tests updated.
**Updated:** 2026-08-13 — Simplified the Zionist gate to a single Gaza question; restored the GD course to 6 guides with future availability dates and new course subtitles (migration `20260813180000_restore_gd_guides_and_subtitle`); cluster creation (`POST /api/cluster`) requires the pastor to have taken the GD course.
**Updated:** 2026-08-13 — Moved the single Gaza question to the per-user profile: migration `20260813190000_add_gaza_position_to_usuario`, `canPurchaseGDCourse` now reads `usuario.position_israel_gaza` (drops the church/pastor requirement), profile page asks the question, `/api/profile` persists it.
**Updated:** 2026-08-14 — Distribution percentages updated: referrals 5% → 10% funded from pdJ (55% → 50%), NOT from scholarships (see #148). Checked off implemented UI (§2.1 badge + buy button, §2.2 checkout modal features).
**Updated:** 2026-08-23 — Reasons for denying a purchase are now stable keys localized on the client (`lib/course-access-msg.ts`, shown in the user's language). Purchase eligibility generalized to **every paid course** via `canPurchasePremiumCourse`: a verified worship city is required (the price depends on the self-reported country; the verifier-confirmed church city — `verified_city_id` or `verified_place_of_worship_location` — stands in for it). GD keeps its pilot gates.
**Updated:** 2026-09-21 (segunda pasada) — Revisión del trabajo de la rama `mvppwa`: se verificaron `make type`, la suite completa (`make test`: 991 passed / 6 skipped, 139 archivos, 0 fallas) y `bin/audit-api-auth.mjs` (54 passed / 0 failed); se corrigió el error de unidades del SLEARN en `PremiumCoursesSection` y en el agregado `premium` de `/api/transparency` (se publicaban centésimas como SLEARN, 100× el monto) con sus pruebas; se alineó §1.3 con los porcentajes que realmente se envían a `processPayment` (GD: pdJ 40, reward 12 sobre el 90% procesado, vault = remanente), se actualizó el maquetado de §2.2 y se marcaron las casillas que sí están cubiertas. Detalle del plan en §11.
**Status:** Fases 1-3 implementadas (precio, compra, distribución GD); la UI de §2 (precio junto al botón, "Mis cursos premium", panel de transparencia) está lista y verificada en local. Antes de cerrar: las mezclas 100% SLEARN y 50/50 (§4.2, requieren recarga de SLEARN de la fixture por el operador), la corrida con `FUND_FRESH=1` en el dev site tras desplegar (acceso tras la compra + bloque `premium` del panel) y el commit del submódulo `apps/nextjs/app/api`. Dependency #155 (ClusterFundsV2) deployed. Note (https://gitlab.com/pasosdeJesus/m/-/work_items/35): the `POST /api/courses/premium/purchase` and `check-crossword` handlers live in the `@learn-tg/rewards` engine (thin re-exports); `price`/`mine`/`purchase-eligibility` remain in core.

---

## 11. Plan para completar lo que falta

Ordenado por dependencia. El agente no escribe Git ni mueve tokens reales (`AGENTS.md` §6-§7), así que los pasos
1, 2, 3 y 5 son del operador; el 4 y el 6 puede correrlos el agente en cuanto el 3 y el 5 estén hechos. Estado al
2026-09-21: 1 y 2 están **staged** en el índice (falta el commit) y los smokes sin fondos ya se corrieron (§4.3).

| # | Paso | Quién | Verificación |
|---|---|---|---|
| 1 | **Commitear el submódulo `apps/nextjs/app/api`** (ya staged): `transparency/route.ts` (agregado `premium` + conversión de centésimas), `transparency/__tests__/route.test.ts` y las tres carpetas nuevas de pruebas (`courses/premium/price`, `courses/premium/mine`, `courses/[courseId]/access`); después actualizar el puntero en el repo principal | operador (esta VM no escribe Git, `AGENTS.md` §7) | `git -C apps/nextjs/app/api status` limpio y el puntero movido. **Antes de desplegar (paso 5): sin este commit el agregado `premium` no llega al sitio** |
| 2 | **Commitear el resto de la rama** (ya staged, más `REQ/261.md` que está sin trackear): REQ, `PremiumCoursesSection` + prueba, specs `premium-price`/`premium-purchase`, `premium-course-checkout`, docs con los conteos nuevos | operador | `git status` limpio |
| 3 | ~~Recargar SLEARN a la fixture~~ — **hecho 2026-09-21**: el operador envió 3000 SLEARN desde el backend dev (`0x01a72816…`) y la fixture quedó con 3015.00 antes de las pruebas y 2991.09 después de las dos mezclas (gasto exacto 23.91) | operador | ✅ `balanceOf(0x84272a6d…)` = 2991.09 |
| 4 | ~~Correr las mezclas pendientes~~ — **hecho 2026-09-21**: `SLEARN_PCT=50` → 9/9 (0.35 USDT + 6.93 SLEARN, tx `0x836629364c…`) y `SLEARN_PCT=100` → 9/9 (13.86 SLEARN, tx `0x71b43a2504…`), ambas con acceso al curso y fila en `mine` | agente | ✅ §4.2 tickeado |
| 5 | **Desplegar la rama en el dev site** (`learn.tg:9001`) con los motores reconstruidos (`engines-dist`) y **aplicar la migración en la misma ventana** (`bin/m db:migrate`, `20260921180000_premium_course_slearn_amount_decimal`). Orden a evitar en cualquiera de las dos direcciones: el motor nuevo escribiendo SLEARN legibles en la columna INTEGER antigua (6.93 → 7) o el motor viejo escribiendo centésimas en la columna nueva (693.00). El cambio de `categoria`/`subcategoria` del historial viaja en el mismo despliegue (vive en `app/api`). Si el sitio se sirve con `bin/dev` (dev server en vez de `make all` + `bin/start`), hay que calentar con `bin/warmup.mjs` antes de los specs; con build de producción no hace falta (`doc/environments.md` §Dos modos) | operador | el dev site sirve el precio junto al botón, `/api/transparency` responde con `premium`, la columna es `numeric(10,2)` y el historial trae `categoria`/`subcategoria` (hoy responde sin ellos: re-comprobado 2026-09-21) |
| 6 | ~~Correr `make test-smoke` en el dev site y el E2E de UI con fondos~~ — **hecho 2026-09-21**: los smokes quedaron **20/21** (el único fallo, `referral-payout` paso 5, es ajeno) y el E2E de UI `premium-course-checkout` con `FUND_FRESH=1 SLEARN_PCT=100` dio **0 fallas**: compra real, pantalla de resultado con hash, "Purchased", la compra listada en el perfil y `guide1` accesible (4457 caracteres). El bloque `premiumSlearn` de `transparency` pasa a aserción real cuando se despliegue el cambio de A+B | agente | ✅ "acceso tras la compra" tickeado; §4.3 revisado |
| 7 | **Curso premium estándar (no-GD)**: hoy solo los cursos 10/11 tienen `porPagar > 0`, así que la regresión 3.6 no tiene E2E. Cuando exista un curso de pago no-GD, correr la compra contra él y confirmar 50% pdJ (ya assertado por unidad) | operador + agente | §7 tarea 3.6 con E2E |
| 8 | **Cerrar**: pasar el estado del encabezado a "cerrado" cuando 6 y 7 estén verdes (el 4 y el 6 ya están; queda el 7, que necesita un curso de pago no-GD) | operador | — |
