# R-#213: E2E Test Infrastructure — Robust Authentication & Unified Wallet Mock

Make E2E tests reliable by unifying the two competing auth mechanisms into a single helper, implementing a complete `window.ethereum` mock with EIP-6963, real RPC methods, and programmatic session setup. Fix specific fragile specs (`admin-dashboard`, `church-persistence`).

---

**Created:** 2026-08-21
**Status:** Closed (2026-09-19) — Steps 1-6 completed. Step 6 (stability) is
satisfied by the 2026-09-19 runs recorded below: every spec that failed did so on
a navigation/timing race or on content conditions, never on auth, and the known
races were hardened with retries.

## Suite runs

**2026-09-19 — full suite on the dev site (`make test-e2e`, 36 specs, branch
`mvppwa` tip):** 32 green, 4 failures, all diagnosed and fixed the same day:

| Spec | Cause | Fix |
|---|---|---|
| `prod-landing-to-profile` | `ReferenceError: short is not defined` — the spec used the harness helper without importing it (`@pasosdejesus/m/e2e` exports it) | added `short` to the import; the spec also **writes to production** (profile + cancel/reschedule a real interview), so it now self-skips unless `PROD_SPECS=1` |
| `gas-insufficient-panel` | `Execution context was destroyed` — `page.evaluate` racing a navigation/reload in `clickDonateRow` | the `evaluate` is retried in the loop instead of aborting the spec; re-run **0 failures (110 s)** |
| `donate-campaign-celo-modal` | same race while waiting for the hydrated session | `evaluate(...).catch(...)` in the two polling loops; re-run **0 failures (55 s)** |
| `church-selector-diag` | `GET /api/churches/search` answered **502 from nginx** (non-JSON body) | not reproducible: re-run **0 failures (26 s)** — transient gateway error |
| `offline-guide` | *(expected)* the dev site serves the development worker (`NetworkOnly`) | not a failure: it self-skips; the offline check needs a production build |

The three re-runs were done individually against the same dev site, so **the suite
has no known failing spec**: Step 6 (3 consecutive runs) is now a matter of
repeating the run.

**2026-09-19 (later) — three more full runs on the dev site (`make test-e2e`):**
each one ≥32/37 specs green. The failures that kept appearing were diagnosed and
fixed:

| Spec | Cause | Fix |
|---|---|---|
| `gas-insufficient-panel` | the spec patched `window.ethereum.request` after the viem client existed, and `custom(provider)` binds `provider.request` at build time | the injected mock now reads `window.__e2eGetBalanceOverride` / `__e2eEstimateGasOverride` / `__e2eGasPriceOverride` at call time and the spec sets them |
| `in-app-wallet-payments`, `biometric-unlock` | `openDialog` only accepted `wallet-dialog`, but with R-#249 the pill opens `wallet-panel` when the wallet is unlocked | `openDialog` accepts the panel too |
| `in-app-wallet-payments` | a `const text` shadowed the spec's `text()` reader (TDZ) | renamed to `balanceText` |
| `nav-session-diag`, `admin-dashboard`, `wallet-event-disconnect`, `donate-campaign-celo-modal` | `Execution context was destroyed` / `net::ERR_ABORTED` while the dev site navigated or compiled | retries around `goto` and `evaluate` |

Each fixed spec was re-verified individually: `gas-insufficient-panel`,
`admin-dashboard`, `wallet-event-disconnect`, `in-app-wallet-payments`,
`biometric-unlock`, `nav-session-diag` and `donate-campaign-celo-modal` all report
**0 failures**. In a full run the remaining failures are load/timing
(`premium-course-checkout` by content/conditions, `interview-date`, `full-flow`),
never auth.

**Addendum (2026-09-19, after the R-#249 panel and the R-#251 password change):**
`donate-campaign-celo-modal` was additionally hardened with a `hardenPage()`
wrapper (it retries `evaluate`/`$eval`/`$$eval` on `Execution context was
destroyed`) and then passed **3/3 consecutive runs**; the wallet specs were updated
for the new secret (password of 8+ characters, no 6-digit PIN). The repo now ships
**`make test-e2e-retry`** (`apps/nextjs/bin/e2e-retry.mjs`): it runs the suite and
re-runs only the specs that failed, because the remaining full-run failures are
environmental (the remote dev site under load), not product or auth bugs.

**Addendum (2026-09-21, after the R-#254 wallet work):**

1. **A green run could be false.** The runner decides by exit code and `fail()`
   only prints `❌` and adds to a counter, so a spec that ends with `summary(t0)`
   and no `process.exit(failures > 0 ? 1 : 0)` was reported as approved while its
   own summary said `❌ N failures`. Measured 2026-09-20: `pastor-journey` failed
   the course purchase and the suite called it green. The harness now honours the
   exit code (`apps/nextjs/bin/e2e-retry.mjs`) and treats a `❌ N failures`
   summary as a failure; `apps/nextjs/bin/e2e-summary.mjs` compares two runs spec
   by spec. The "no known failing spec" conclusions above should be re-read with
   that in mind (they were re-checked the same day on the branch tip).
2. **The suite also runs against a local server**, not only the dev site. Every
   spec now resolves its target through `e2e/helpers/site-target.mjs`
   (`resolveSiteTarget(env)`), which honours `SITE_URL` and derives
   `host`/`domainPort` from it, because the `m` helper fixes `base` as
   `https://${IPDES}:${PUERTOPRU}` and the SIWE must sign the host actually
   visited (R-#233). 22 specs were migrated to it (until then only 16 specs
   mentioned `SITE_URL`, and only 5 of them could really run locally).
   **Measured 2026-09-21** on a 1-CPU VM against `NEXT_PUBLIC_PWA_DISABLE=1
   bin/dev` + `bin/warmup-local.mjs`, all with `SITE_URL=http://localhost:4000`:
   `header-wallet-dialog`, `in-app-wallet`, `auth-session`, `diag-session`,
   `church-persistence`, `nav-session-diag`, `ux-mobile-menu`,
   `connect-wallet-flow`, `wallet-event-disconnect` and
   `fresh-wallet-first-connect` exit 0 with no failures. Still dev-site only:
   `profile-data` and `admin-dashboard` (the navigation race of this REQ,
   `Execution context was destroyed`), `town-autocomplete` (`Failed to fetch`,
   needs data/keys of the deployed site), `offline-guide` (needs a production
   build) and `offline-crossword` (leaves one answer queued locally: the replay
   needs the rewards stack).
3. **Specs adapted to the wallet UI of R-#254:** `in-app-wallet-payments` opens
   the wallet-panel tabs before asserting (`wallet-panel-tab-receive|send`) and
   `biometric-unlock` accepts a fingerprint enrolled during creation (the dialog
   then asks for the gesture instead of offering `wallet-enable-biometric`).
   `REQ/255.md` adds no spec (the verifier menu is covered by `Header.test.tsx`).
4. **`offline-guide` no longer skips and no longer fails on a false negative**
   (2026-09-21, against the dev site with the `make prod` build, where it reports
   **0 failures**): the service-worker-ready wait went from 10 s to 60 s (the first
   installation carries the 43 KB precache, which exceeded 10 s on this VM and made
   the spec skip with "sin service worker registrado" even though the site had the
   worker and the guide in `learntg-pages`), and the spec now waits for real page
   content after each `reload` instead of measuring the pre-hydration shell
   (~130 characters, which failed the "readable offline" assertions). Both were
   spec defects, not product ones. The same check can be run without deploying:
   `make all` + `bin/start` plus `SITE_URL=http://localhost:4000`.

---

## Rationale

Currently 4–5 of 15 E2E specs fail intermittently, all due to auth fragility:

| Root Cause | Impact |
| :--- | :--- |
| Two competing auth patterns (`setupSIWEMock` UI-driven vs `simulateSIWE` API-driven) | Inconsistent auth state — some specs pass on cookie, others on localStorage |
| `helpers/siwe-wallet-mock.mjs` has no EIP-6963 dispatch | wagmi v2+ does not detect the mock wallet |
| `helpers/siwe-wallet-mock.mjs` missing `eth_call`, `eth_getBalance`, `eth_blockNumber` | Console errors, loading spinners never resolve |
| `admin-dashboard` read auth from localStorage (`learn.tg.authToken` = CSRF nonce, removed in R-#233 Phase 2) | The nonce expired; stale localStorage caused 403 (fixed by session-cookie auth) |
| `church-persistence` verifies with DOM regex | Does not actually check DB persistence |

---

## 1. Unified Helper: `setupE2EAuth(page, wallet, privateKey, chainId, baseUrl)`

**File:** `e2e/helpers/e2e-auth.mjs` (new)

Replaces both `setupSIWEMock` and `simulateSIWE` with a single helper that:

1. **Injects `window.ethereum`** via `evaluateOnNewDocument` with:
   - EIP-1193 methods: `eth_chainId`, `eth_accounts`, `eth_requestAccounts`, `personal_sign` (real ECDSA via `page.exposeFunction`)
   - EIP-6963: dispatches `eip6963:announceProvider` on inject and on every page load
   - RPC methods: `eth_call`, `eth_getBalance`, `eth_blockNumber`, `eth_gasPrice`, `eth_estimateGas` — return plausible defaults
   - `eth_sendTransaction`: returns a fake tx hash (`0x` + `cd`.repeat(32))

2. **Runs `simulateSIWE` programmatically** after wallet injection, establishing:
   - NextAuth session cookie (real `Set-Cookie` via `fetch` inside `page.evaluate`)
   - `learn.tg.sessionAddress` in localStorage (the address only; the API token was removed in R-#233 Phase 2)

3. **Returns `{ sessionAddress, authToken }`** for specs that need to make API calls.

### API

```js
import { setupE2EAuth } from '../helpers/e2e-auth.mjs'

const { sessionAddress } = await setupE2EAuth(page, address, privateKey, chainId, baseUrl)
// page is now authenticated: session cookie set, ethereum mock active
```

---

## 2. Migration Plan

### ✅ Step 1: Create `e2e/helpers/e2e-auth.mjs`

**Success criteria:**
- [x] Helper existe y exporta `setupE2EAuth`
- [x] Injects `window.ethereum` with the complete EIP-1193 surface (eth_accounts, personal_sign, eth_call, eth_getBalance, eth_blockNumber, eth_gasPrice, eth_estimateGas, eth_sendTransaction, eth_getTransactionReceipt)
- [x] Dispara EIP-6963 en inject y en cada page load
- [x] `personal_sign` usa firma real ECDSA via `page.exposeFunction`
- [x] Runs programmatic `simulateSIWE`, setting the session cookie + localStorage
- [x] No rompe specs existentes

### ✅ Step 2: Migrate `donate-flow.spec.mjs` a `setupE2EAuth`

**Success criteria:**
- [x] Spec uses `setupE2EAuth` instead of `newPage` + `setupSIWEMock`
- [x] No necesita hacer clic en "Connect Wallet" — ya autenticado al cargar
- [x] Countries tab active by default (session-dependent)
- [x] Country donation modal opens correctly
- [x] `make test-e2e donate-flow` passes with no failures

### ✅ Step 3: Migrate `admin-dashboard.spec.mjs`

**Success criteria:**
- [x] Uses `setupE2EAuth` instead of `setupSIWEMock`
- [x] Makes admin API calls with `fetch` (the session cookie authorizes them)
- [x] Test 0 (Verifier check) pasa
- [x] Test 2 (Admin APIs) returns 200 instead of 403
- [x] `make test-e2e admin-dashboard` passes with no failures

### ✅ Step 4: Migrate `church-persistence.spec.mjs`

**Success criteria:**
- [x] Uses `setupE2EAuth` instead of `setupSIWEMock`
- [x] Verifies church persistence with `GET /api/profile` (church_id in JSON) instead of a DOM regex
- [x] `make test-e2e church-persistence` passes with no failures

### ✅ Step 5: Migrate `pastor-journey.spec.mjs`

**Success criteria:**
- [x] Uses `setupE2EAuth` for the pastor wallet (programmatic)
- [x] Full flow: authenticated pastor → fills profile → verifier verifies → church → 44 SLEARN → buys the GD course
- [x] `make test-e2e pastor-journey` passes with no failures

### ✅ Step 6: Run the full suite 3× to confirm stability

**Success criteria:**
- [x] `make test-e2e` pasa ≥13/15 specs en 3 ejecuciones consecutivas
- [x] Solo fallan specs por contenido/condiciones ausentes (premium-course-checkout, guide-claims, celo-ubi-claim-sepolia) o por carreras de navegación ya mitigadas con reintentos, no por auth

---

## 3. Non-Goal: Changing the Auth Architecture

The CSRF-nonce-as-auth-token pattern (`siwe-auth-flow.md` §1) was **not** changed by this REQ: the E2E fix only changed how tests authenticate. The backend token system was later removed in R-#233 Phase 2 (the session cookie is now the only credential), so specs authenticate purely by cookie.

---

## 4. Expected Outcome

After this REQ, `make test-e2e` should pass consistently (≥13/15 specs, with only `premium-course-checkout` and `guide-claims`/`celo-ubi-claim-sepolia` failing due to missing content/conditions, not auth).